# Workflows Métier

## 1. Bon de Commande
```
CREATE → ADD_LIGNES → PLAN_TECHNICIENS → VALIDATE → 
CREATE_FACTURE → TRACK_RECEPTION → CLOSE
```

## 2. Devis → Facture
```
CREATE → SEND → ACCEPT/REFUSE → CREATE_FACTURE → EMIT → PAY
```

## 3. Rapport d'Intervention
```
CREATE → ADD_PHOTOS → SIGN → CREATE_FACTURE → PDF
```

## 4. Gestion RH
```
ADD_SALARIE → HABILITATIONS → ABSENCES → DOCUMENTS
```

...
