# Inventaire de parité — ERP Chantier (ancienne app → `web/`)

Checklist de ce que l'ancienne application fait réellement, relevée le 24/09/2026 dans
`src/pages/app.js` (20 298 lignes), `src/api/**`, `src/integrations/**` et `supabase/**`.
C'est **elle** qui dit ce qui manque à la nouvelle application. Le détail (code verbatim,
vecteurs chiffrés, numéros de ligne complets) est dans [`inventaire-brut/`](inventaire-brut/) ;
les règles de calcul prescriptives sont dans [`regles-metier.md`](regles-metier.md).

## Comment la lire

- Une section par **module cible** de la nouvelle app (dossier `web/src/modules/<module>`).
  Les modules après « Hors périmètre de cette nuit » ne sont pas visés tout de suite, mais leurs
  lignes restent ici pour que rien ne se perde.
- Dans chaque section : **Écrans et fonctionnalités**, **Règles métier**, **Données**,
  **Cas limites et corrections cachées**, **Défauts connus de l'ancienne app**.
- Chaque ligne porte un **identifiant stable** (`CLI-07`) : on ne renumérote jamais, on ajoute
  à la suite. Un test ou un commit qui couvre une ligne cite son identifiant.
- **Source** : `app.js:NNNN` = `src/pages/app.js`, ligne ; les fichiers TS sont cités depuis
  `src/` ; `schéma §x` et `ts §x` renvoient aux sections de `inventaire-brut/schema.md` et
  `inventaire-brut/couche-ts.md` ; `RM-xx` renvoie à `regles-metier.md`.

### Légende

| Case | Sens |
|---|---|
| `- [ ]` | à faire |
| `- [x]` | fait — la ligne cite sa **preuve** (nom du test, ou écran + geste qui le montre) |
| `- [~]` | partiel — la ligne dit ce qui manque |
| `- [-]` | volontairement écarté — renvoi obligatoire à une entrée de `DECISIONS.md` |

Pour les « Défauts connus », `- [ ]` signifie « **décision à prendre** » : soit on corrige
(la ligne passe à `[x]` avec son test), soit on reproduit à l'identique ou on écarte (`[-]`,
avec l'entrée `DECISIONS.md`). Les sections traitées cette nuit sont cochées avec leur preuve.

---

## 1. auth-roles — connexion, rôles, droits

### Écrans et fonctionnalités

- [x] **AUTH-01** Connexion e-mail + mot de passe (`signInWithPassword`) ; succès → `location.replace('/')` (pas `href` : « Précédent » ne ramène pas au formulaire). _Source : `pages/login.html:256-359`_ — ✔ `auth-roles/components/PageConnexion.tsx` (`signInWithPassword`, `<Navigate replace>`), `tests/e2e/parcours.e2e.ts` (connexion, « mauvais mot de passe »)
- [x] **AUTH-02** Session déjà ouverte à l'arrivée sur la page de connexion → redirection immédiate vers l'app. _Source : `login.html`_ — ✔ `PageConnexion.tsx` : session ouverte → `<Navigate to="/" replace />`
- [x] **AUTH-03** « Mot de passe oublié ? » → `resetPasswordForEmail` vers `/nouveau-mot-de-passe.html` ; message **neutre** (« Si un compte existe pour X, un lien… »). _Source : `api/client.ts#demanderReinitialisation`, `login.html`_ — ✔ `PageConnexion.tsx` « Mot de passe oublié ? » → `api/compte.ts#demanderReinitialisation` (retour `/nouveau-mot-de-passe`), message neutre — `components/compte.essai.tsx` (« répond de façon neutre »)
- [x] **AUTH-04** Page « nouveau mot de passe » ouverte par la session `recovery`. _Source : `pages/nouveau-mot-de-passe.html`_ — ✔ `PageNouveauMotDePasse.tsx` (`/nouveau-mot-de-passe`) : session « recovery » requise, sinon « lien expiré » ; 8 caractères, deux saisies identiques — `compte.essai.tsx`, `comptes/domain/comptes.essai.ts`
- [x] **AUTH-05** Cartouche de connexion : date du jour, **aucune société** affichée (on ne sait pas encore chez qui l'on entre). _Source : `login.html`_ — ✔ `PageConnexion.tsx` : date du jour (Paris), aucune société — `compte.essai.tsx` (« affiche la date du jour »)
- [x] **AUTH-06** Garde de route : pas de session → `/login.html`. _Source : `integrations/auth-guard.ts#protectRoute`, `main.ts`_ — ✔ `RouteProtegee.tsx#RouteConnectee` (anonyme → `/connexion`)
- [x] **AUTH-07** Démarrage ordonné : matrice des droits installée → sociétés visibles → rôle par société → annuaire des comptes → rendu. _Source : `main.ts`, `integrations/session.ts#chargerSession`_ — ✔ `api/session.ts#chargerSession` séquentiel : matrice → profil → sociétés et rôle par société → accès client ; l'annuaire n'est plus un préalable (D-AUTH-01) — `api/session.essai.ts` (« matrice d'abord… », ordre relevé)
- [x] **AUTH-08** Compte sans société → « Ce compte n'est rattaché à aucune société. Demandez une invitation à un administrateur. » _Source : `main.ts`_ — ✔ `app/PageSansSociete.tsx` (« …rattaché à aucune société active. Demandez à un administrateur de vous inviter. »)
- [x] **AUTH-09** Délai de démarrage de 15 s → message « La couche de données n'a pas répondu… ». _Source : `app.js:19126-19187`_ — ✔ `domain/demarrage.ts#avecDelai` (15 s, message de l'ancien écran, pas de réessai — D-AUTH-02) — `domain/demarrage.essai.ts`, `hooks/demarrage.essai.tsx` (« au bout de 15 s… sans trois essais »)
- [x] **AUTH-10** Session expirée (PGRST301 / 401 / JWT) détectée au chargement → retour à la connexion ; `SIGNED_OUT` vide le cache. _Source : `app.js:557`, `main.ts#watchAuthState`_ — ✔ toute lecture ou écriture qui reçoit PGRST301/302/303, 401 ou « JWT » ferme la session localement, vide le cache et renvoie à la connexion avec « Votre session a expiré » ; `SIGNED_OUT` / changement de compte vident le cache (D-AUTH-02, D-019) — `hooks/demarrage.essai.tsx`, `SessionProvider.essai.tsx`
- [x] **AUTH-11** Déconnexion depuis le menu utilisateur. _Source : `app.js:1308` (`logOut` → `seDeconnecter`)_ — ✔ `MenuUtilisateur.tsx` « Se déconnecter » → `deconnecter()` vide tout le cache (`qc.clear()`)
- [x] **AUTH-12** Menu utilisateur : nom, rôle, version construite copiable. _Source : `app.js:895, 902, 908`_ — ✔ nom, rôle et version construite (`<meta name="version-construite">` posé par `vite.config.ts`, heure de Paris) copiable d'un clic — `MenuUtilisateur.tsx`, `VersionConstruite.tsx`, `components/repli.essai.tsx` (D-AUTH-12)
- [x] **AUTH-13** « Voir en tant que » : l'**admin réel** seul simule un autre rôle (6 rôles, icônes) ; mémorisé dans `localStorage["erp.role.simule"]`, restauré au changement de société, effacé si le rôle réel n'est pas admin. _Source : `app.js:101, 908` ; `session.ts:317`_ — ✔ `domain/selection.essai.ts`, `domain/permissions.essai.ts`, `app/Layout.essai.tsx` (« voir en tant que ») — clé `erp.web.simulation` ; les six boutons et leurs icônes de l'ancien menu du nom (D-VIS-04), `components/repli.essai.tsx`
- [x] **AUTH-14** Le rôle simulé ne change **rien** en base (RLS au rôle réel) : il ne sert qu'à masquer. Bandeau visible quand on simule (absent de l'ancienne app — décidé : D-010). _Source : `session.ts:317`_ — ✔ `BandeauSimulation.tsx`, `app/Layout.essai.tsx` (bandeau « Aperçu en tant que ») — D-010
- [x] **AUTH-15** Menu latéral filtré : onglet visible ⇔ `voir` sur son module (table §1.6). _Source : `app.js:93` (`navPourRole`), `integrations/permissions.ts#MODULE_PAR_NAV`_ — ✔ `app/Layout.essai.tsx` (« menu principal par rôle », matrice réelle)
- [x] **AUTH-16** Onglet courant non autorisé après changement de rôle/société → premier onglet autorisé. _Source : `app.js:19126-19187`_ — ✔ après un changement de rôle simulé ou de société, une page devenue interdite bascule sur le premier onglet autorisé ; l'accès direct par l'URL garde « Accès refusé », y compris pour un module hors abonnement (D-AUTH-03, D-R4-09) — `hooks/RepliOnglet.ts`, `components/repli.essai.tsx`
- [x] **AUTH-17** « Mon nom » (Réglages › Mon compte) → `profiles.nom` (politique `profiles_update_self`). _Source : `app.js:12277`, `queries/acces.ts#definirMonNom`_ — ✔ `/mon-compte` (`PageMonCompte.tsx`, lien du menu utilisateur, ouvert à tous — D-SOC-06) : nom affiché + mot de passe ; `profiles_update_self` — `compte.essai.tsx`, `tests/rls/comptes.essai.ts` (« renomme son propre profil »)
- [x] **AUTH-18** Invitation d'un salarié à créer son compte (zone de la fiche salarié) : envoyer, renvoyer, annuler ; visible seulement avec `utilisateurs/creer` (admin). _Source : `app.js:16391, 16431, 16460`_ — ✔ Réglages › Comptes (`comptes/components/BlocInvitations.tsx`, D-SOC-07) : inviter, renvoyer, annuler ; masqué sans `utilisateurs/creer` — `comptes/components/comptes.essai.tsx`
- [x] **AUTH-19** Rôle proposé à l'invitation : `conducteur` si une fiche conducteur existe, sinon `technicien` ; rôles invitables : technicien, conducteur, secrétaire, lecture, admin (admin avec confirmation) ; `sous_traitant` exclu. _Source : `app.js#roleProposePourSalarie`, Edge `inviter-salarie`_ — ✔ `comptes/domain/comptes.ts#rolePropose` + `ROLES_INVITATION` (sans `sous_traitant`), confirmation pour admin — `comptes/domain/comptes.essai.ts`, `comptes.essai.tsx` (« rôle proposé par sa fiche »)
- [x] **AUTH-20** Changer le rôle d'un compte (`membres_societe.role`), issues `change | inchange | absent | refuse` ; proposé par `confirm` quand on coche « Conducteur de travaux ». _Source : `app.js:16183`, `queries/acces.ts#definirRoleDuCompte`_ — Preuve : Réglages › Comptes (`comptes.essai.tsx`) + proposition à la coche « Conducteur » de la fiche RH (`PropositionRole`, `definirRole` de `comptes/api`, refus non-admin rédigé) — `src/modules/rh/components/rh.essai.tsx`

### Règles métier

- [x] **AUTH-30** Six rôles, énumération `role_membre` : `admin` Administrateur, `secretaire` Secrétaire, `conducteur` Conducteur de travaux, `technicien` Technicien, `lecture` Lecture seule, `sous_traitant` Sous-traitant (entreprise). **Il n'existe pas de rôle `client`.** _Source : schéma §1.4, `app.js:84`_ — ✔ `domain/permissions.ts#ROLES_LIBELLES` (énumération `role_membre` des types générés), `app/Layout.essai.tsx`
- [x] **AUTH-31** Rôle **par société** (`membres_societe(profile_id, societe_id, role, actif)`, unique par couple) ; un compte peut appartenir à plusieurs sociétés avec des rôles différents. _Source : schéma §1.3_ — ✔ `api/session.ts` (rôle lu par ligne de `membres_societe`), `domain/selection.essai.ts` (admin d'ALPHA, technicien de BETA)
- [x] **AUTH-32** Accès effectif ⇔ `membres_societe.actif` **et** `profiles.actif` ; désactiver le profil coupe toutes les sociétés d'un coup. _Source : schéma §5.2 `role_ — ✔ accès désactivé/réactivé par société (`definirAcces`), badge « compte coupé » si `profiles.actif` faux ; la base décide (`mes_societes`) — `tests/rls/comptes.essai.ts` (« désactiver l'accès coupe la société ») ; un compte ne se réactive plus lui-même (proposition 20260926010000)
- [x] **AUTH-33** La matrice `role_permissions(role, module, action)` (actions `voir|creer|modifier|supprimer`) est **lue en base** au démarrage ; lecture tronquée ou vide = refus de démarrer ; `peut()` lève tant qu'elle n'est pas installée. _Source : `integrations/permissions.ts`, `queries/acces.ts#listRolePermissions`_ — ✔ matrice lue en base à l'ouverture, EN PREMIER, avec `count: exact` : vide ou tronquée → refus de démarrer (`domain/demarrage.ts#verifierMatrice`) ; `peut()` n'a pas d'état « non installée », la matrice fait partie du type `Session` (D-AUTH-01) — `api/session.essai.ts`, `domain/demarrage.essai.ts`
- [x] **AUTH-34** Reproduire la matrice complète (184 lignes) du tableau §1.6 ; elle ne s'écrit que par migration. _Source : migrations `20260911110000`, `20260914120000` ; schéma §5.2_ — ✔ `src/test/fixtures/role_permissions.json` (184 lignes), `tests/rls/isolement.essai.ts` (« role_permissions n'a pas bougé »)
- [x] **AUTH-35** `voitLesPrix(role)` = rôle ∉ {technicien, sous_traitant} (miroir SQL `voit_les_prix`) : aucun montant affiché au terrain. _Source : `permissions.ts`, schéma §5.2_ — ✔ `domain/permissions.essai.ts` (« voitLesPrix »), `useVoitLesPrix`
- [x] **AUTH-36** `actionsFacturation` : `peutModifierPrefacture` = admin, secrétaire ; `peutValiderPrefacture` = admin ; `peutFacturer` = admin, secrétaire ; `peutFacturerHorsCircuit` = admin. _Source : `session.ts:624`_ — ✔ `auth-roles/domain/actions.ts#actionsFacturation`, réexportée par `commandes/domain/circuit.ts` (pré-facture, hors circuit, émission) — parité `tests/parite/actions.essai.ts` (source extraite de `integrations/session.ts`), `tests/parite/circuit.essai.ts` (D-AUTH-11)
- [x] **AUTH-37** `actionsTache(statut, rôle, appartenance)` : planifier = admin|conducteur ; saisir/clôturer = terrain **de l'équipe** (ou encadrement), tâche non validée ; arbitrer = admin|conducteur sur tâche `realisee`. _Source : `api/regles-taches.ts:121`_ — ✔ `auth-roles/domain/actions.ts#actionsTache` (+ `motifLectureSeule`, transitions), une seule règle réexportée par planning et commandes — parité `tests/parite/actions.essai.ts` (ancien `regles-taches.ts` importé tel quel, toutes combinaisons), `tests/parite/planning.essai.ts` (D-AUTH-11)
- [x] **AUTH-38** Tous les tests d'interface (`autorise`, `affichePrix`, `actionsTache`, `actionsFacturation`) utilisent le **rôle effectif** (simulé ?? réel). _Source : `session.ts`_ — ✔ `hooks/useSession.ts#usePermission` et `useVoitLesPrix` lisent `roleEffectif` ; `app/Layout.essai.tsx` (simulation technicien)
- [x] **AUTH-39** La base fait autorité : l'interface **masque** ce qui serait refusé, elle ne protège rien ; un refus de la base s'affiche avec son motif (`dernierRefus` = `details || hint || message`). _Source : `CLAUDE.md`, `html-adapter.ts`_ — ✔ masquage par `<Can>` / `RouteModule` ; un refus MOTIVÉ par la base (`details || hint || message`, rédigé en français) s'affiche avec son motif, un message natif anglais jamais (`lib/erreurs.ts#motifDeLaBase`, D-AUTH-04) — `lib/erreurs.essai.ts`
- [x] **AUTH-40** Dernier admin protégé : on ne retire pas son propre rôle admin, on ne supprime pas son propre accès, une société garde au moins un admin actif. _Source : déclencheur `trg_ — ✔ miroir d'affichage `comptes/domain/comptes.ts#verrouAdmin` + refus du déclencheur redits en français (`refusLisible`) — `comptes/domain/comptes.essai.ts`, `comptes.essai.tsx` (« verrouillé »), `tests/rls/comptes.essai.ts` (42501)
- [-] **AUTH-41** Premier profil créé quand `membres_societe` est vide → admin de **toutes** les sociétés. _Source : `amorcer_ — tenu par la base (`amorcer_premier_admin`), rien à reprendre à l'écran ; non testable sur la base partagée — D-SOC-12
- [x] **AUTH-42** Circuit d'inscription : profil créé par déclencheur (`nom` = métadonnée ou partie locale de l'e-mail) ; invitations appliquées à la confirmation de l'adresse (membre, rôle, `salaries.profile_id` ou `sous_traitants.contact_profile_id`, invitation `acceptee`). _Source : schéma §1.3_ — ✔ `tests/rls/comptes.essai.ts` (« une invitation en attente s'applique à l'inscription confirmée : membre, rôle, statut « acceptée » »)
- [x] **AUTH-43** Invitation : refus si le salarié a déjà un compte, si l'adresse appartient à un autre salarié, ou renvoi à moins de 10 min ; une seule invitation `en_attente` par salarié ; unicité `(societe, lower(email))`. _Source : Edge `inviter-salarie`, schéma §1.3, §8.3_ — ✔ unicité `(societe, lower(email))` : `tests/rls/comptes.essai.ts` (« casse comprise ») ; refus de la fonction de bord (compte existant, adresse d'un autre, 10 min) relayés tels quels — `comptes.essai.tsx` (« motif d'un refus »)
- [x] **AUTH-44** Le compte d'une personne se relie par `salaries.profile_id` (équipe du technicien), `conducteurs.profile_id` (unique par société : tableau de bord du conducteur), `sous_traitants.contact_profile_id`. _Source : schéma §1.3, `CLAUDE.md`_ — Preuve : `conducteurs.profile_id` (Réglages › Intervenants, fiche RH) ; `salaries.profile_id` posé par l'invitation, jamais écrasé par la fiche ; `sous_traitants.contact_profile_id` choisi parmi les comptes `sous_traitant` libres (`comptesSousTraitantLiables`) — `src/modules/rh/domain/rh.essai.ts`, `tests/rls/rh.essai.ts`

### Données

- [x] **AUTH-50** Tables `profiles` (id = `auth.users.id`, nom, email, actif), `membres_societe`, `invitations` (statut `en_attente|acceptee|annulee|expiree`, `invitee_le`), `role_permissions`. _Source : schéma §1.3, §2.8_ — ✔ `profiles`, `membres_societe`, `role_permissions` lus ; `invitations` lues et écrites (`comptes/api/comptes.ts`) — `tests/rls/comptes.essai.ts`
- [x] **AUTH-51** Fonctions : `mon_role(p_societe)`, `role_dans_societe`, `mes_societes()`, `est_membre`, `est_admin`, `peut_ecrire`, `voit_les_prix`, `a_permission(societe, module, action)`, `est_affecte_au_chantier`, `est_de_l_equipe`, `tache_a_une_equipe`. _Source : schéma §1.4, §5.2_ — ✔ chaque fonction appelée par chacun des six rôles et comparée à ce que croit l'écran : `mon_role`, `role_dans_societe`, `mes_societes`, `est_membre`, `est_admin`, `peut_ecrire`, `voit_les_prix` (= `voitLesPrix`), `a_permission` (= fixture), `est_affecte_au_chantier`, `tache_a_une_equipe`, `est_de_l_equipe` — `tests/rls/auth-roles.essai.ts` ; cas positif d'équipe : `tests/rls/planning.essai.ts`
- [x] **AUTH-52** Edge Function `inviter-salarie` : `{salarie_id, email, role}` → `{etat: invitee|confirmation_renvoyee|rattachee}` ou `{erreur}` (400/403/404/409/429/500/502). _Source : schéma §8.3_ — ✔ de bout en bout contre la base locale : la fonction historique chargée TELLE QUELLE dans le test sert le `functions.invoke` de `comptes/api#inviterSalarie` — invitée, renvoi < 10 min (429), secrétaire (403), déjà relié (409), rôle hors liste (400) ; edge-runtime inaccessible au réseau de l'agent (D-AUTH-09) — `tests/rls/inviter-salarie.essai.ts`

### Cas limites et corrections cachées

- [x] **AUTH-60** Le parcours sous-traitant était inatteignable : 24 comparaisons testaient `"soustraitant"` au lieu de `"sous_traitant"`. Tester le rôle par sa valeur d'énumération, jamais par un libellé. _Source : `app.js:17929`_ — ✔ rôles testés par leur valeur d'énumération (`estRole`), `app/Layout.essai.tsx` (« sous-traitant ne voit aucun écran d'argent »)
- [x] **AUTH-61** Le planning et les réglages lisent `state.currentRole` directement, le reste `roleEffectif()` : un seul point d'accès au rôle dans la nouvelle app. _Source : `app-2.md §2.8`_ — ✔ un seul point : `useSession().roleEffectif` / `usePermission` (`hooks/useSession.ts`)
- [x] **AUTH-62** Salutation « Aissa Choumane » affichée pour tout le monde, puis nom refabriqué depuis l'e-mail : afficher `profiles.nom` (`nomAffichable`). _Source : `app.js:1665`_ — ✔ `MenuUtilisateur.tsx` affiche `profiles.nom` (repli : l'e-mail)
- [x] **AUTH-63** « Jean-Pierre » devenait « Jean Pierre » : la mise en forme de casse est réservée aux adresses. _Source : `app.js:964`_ — ✔ aucune mise en forme de casse : `profiles.nom` affiché tel quel

### Défauts connus de l'ancienne app

- [x] **AUTH-70** La **secrétaire n'est pas dans `peut_ecrire`** : elle ne peut pas appeler `prochain_numero` (donc **pas numéroter un devis**, alors qu'elle a `devis/creer`), ni écrire référentiels, fournisseurs, conducteurs, sous-traitants, métiers, équipes, documents légaux, interlocuteurs, le bucket `terrain`, les photos/contrôles d'intervention, les filles de véhicule. _Source : schéma §10.3-1_ — ✔ proposition `20260926110000` : écriture = `peut_ecrire()` OU matrice du module — la secrétaire tient équipes, sous-traitants et leurs documents, fiches conducteur (`rh`), contrôles et factures fournisseurs, cycle de vie des factures, filles de véhicule ; numérotation et interlocuteurs déjà corrigés (`20260925020000`, `20260925010000`) ; référentiels / métiers / documents légaux / fournisseurs suivent `reglages` (secrétaire : voir, comme la matrice) ; seau `terrain` : chemins RH et véhicules déjà ouverts par module, le reste est le terrain (D-AUTH-05) — `tests/rls/auth-roles.essai.ts`, `tests/rls/rh.essai.ts`
- [x] **AUTH-71** Suppression trop large dans les tables filles (`rls_table_fille`) : **tout membre, `lecture` compris**, peut supprimer photos et contrôles d'intervention, documents/inspections/todos de chantier, interlocuteurs, documents sous-traitant, filles de véhicule, prêts de matériel, `facture_cycle_vie`. _Source : schéma §10.3-2_ — ✔ relevé `pg_policy` automatisé : plus aucune politique DELETE sous `est_membre()` ; les dernières trop larges sous `peut_ecrire()` (référentiels, contrôle fournisseurs, cycle de vie, filles de véhicule) suivent « module / supprimer » (proposition `20260926110000`, D-AUTH-06) — `tests/rls/auth-roles.essai.ts` (« relevé de pg_policy », « [proposition] suppression »)
- [x] **AUTH-72** `chantier_documents|inspections|todos|comptes_rendus` et `planning_taches` ignorent `est_affecte_au_chantier` : technicien et sous-traitant lisent tout. _Source : schéma §10.3-3, §6.5_ — ✔ `planning_taches` : le sous-traitant ne lit que ses tâches (20260926102000, D-TRV-04) ; `chantier_documents|inspections|todos|comptes_rendus` : `est_affecte_au_chantier()` écrit dans la lecture (20260926110000) ; défaut trouvé et corrigé : l'admin ne relisait pas le chantier qu'il créait (D-AUTH-07) — `tests/rls/auth-roles.essai.ts` (« [proposition] filles du chantier »)
- [x] **AUTH-73** `workflow_journal` accepte l'INSERT de tout membre (fausses transitions possibles). _Source : schéma §10.3-9_ — ✔ proposition 20260926103000 : plus de politique INSERT ni de droit d'écriture, les RPC du circuit écrivent seules (D-TRV-05) — `tests/rls/transversal.essai.ts` (« journal du circuit »), `tests/rls/circuit.essai.ts`
- [x] **AUTH-74** Deux politiques SELECT identiques sur `profiles`. _Source : schéma §10.3-10_ — ✔ proposition `20260926010000` (politique `profiles_select_membres` supprimée) — `tests/rls/comptes.essai.ts` (« tout membre lit les membres … noms compris »)
- [x] **AUTH-75** Fonctions de déclencheur créées après le 24/09 : `EXECUTE` rendu à `PUBLIC` faute d'`ALTER DEFAULT PRIVILEGES`. _Source : schéma §5.4, §10.3-12_ — ✔ proposition 20260926104000 : EXECUTE retiré à toutes les fonctions de déclencheur (contrôle SQL : 0 ligne), privilèges par défaut inchangés (D-TRV-06)
- [x] **AUTH-76** `v_salaries_annuaire` a probablement perdu `security_barrier` (recréée sans `WITH` le 21/09) — à vérifier par `pg_class.reloptions`. _Source : schéma §4.2_ — ✔ vérifié : `reloptions` vide en base ; `security_barrier` remis par la proposition 20260926104000 (D-TRV-06)
- [-] **AUTH-77** `inviter-salarie` cherche le compte par `listUsers()` sur **une seule page** (50) : un compte existant peut ne pas être trouvé. _Source : schéma §8.3_ — défaut de la fonction de bord, hors de `web/` — D-SOC-13
- [x] **AUTH-78** Barre mobile (`MOBILE_NAV`) **non filtrée** par les droits (décidé : un seul menu filtré, D-014). _Source : `app.js:114`_ — ✔ D-014 : un seul menu filtré sur tous les écrans, `app/Layout.essai.tsx`
- [-] **AUTH-79** Un sous-traitant ne peut pas être invité par l'écran (Edge refuse `sous_traitant`) alors que `invitations.sous_traitant_id` existe : comment un sous-traitant obtient-il son compte ? _Source : schéma §1.3, §8.3_ — reproduit : sous-traitant non invitable — D-SOC-08
- [x] **AUTH-80** `docs/AUTHENTICATION.md` périmé (`membres_societe.user_id`, `created_at`, 4 rôles) : ne pas s'en servir. _Source : schéma §10.2_ — ✔ `web/` ne s'y réfère nulle part

### 1.6 Matrice `role_permissions` (état final des migrations)

V voir · C créer · M modifier · S supprimer · — rien. Total 184 lignes (admin 72, secrétaire 45,
conducteur 33, lecture 17, technicien 10, sous-traitant 7).

| Module | admin | secretaire | conducteur | technicien | sous_traitant | lecture |
|---|---|---|---|---|---|---|
| tableau_de_bord | VCMS | V | V | V | V | V |
| chantiers | VCMS | V | VCMS | V | V | V |
| planning | VCMS | V | VCMS | V | V | V |
| bons_commande | VCMS | V M | VCMS | — | — | V |
| devis | VCMS | VCMS | VCM | — | — | V |
| factures | VCMS | VCMS | V | — | — | V |
| facturation_electronique | VCMS | VCMS | — | — | — | V |
| reglements | VCMS | VCMS | — | — | — | V |
| clients | VCMS | VCMS | V | — | — | V |
| rapports | VCMS | V | VCMS | VCM | VCM | V |
| materiel | VCMS | V | VCMS | V M | V | V |
| controle_fournisseurs | VCMS | VCMS | V | — | — | V |
| rh | VCMS | VCMS | V | V | — | V |
| vehicules | VCMS | VCMS | V M | V | — | V |
| statistiques | VCMS | V | V | — | — | V |
| reglages | VCMS | V | V | — | — | V |
| articles | VCMS | VCMS | V | — | — | V |
| utilisateurs | VCMS | — | — | — | — | — |

Onglet → module (`MODULE_PAR_NAV`) : dashboard→tableau_de_bord · bonsCommande→bons_commande ·
piecesCommande→bons_commande · devis · factures · interventions (« Rapports »)→rapports ·
planning · chantiers · clients · catalogue→articles · rh · sousTraitants→rh · vehicules ·
materiel · controle→controle_fournisseurs · reglements · statistiques · parametres
(« Réglages »)→reglages · plus→tableau_de_bord.

Règles hors matrice, codées en dur : `voitLesPrix` (AUTH-35), `actionsFacturation` (AUTH-36),
`actionsTache` (AUTH-37), `peut_ecrire` = admin|conducteur|technicien (AUTH-70),
`bc_chiffrage_valide*` et `bc_cloturer_gratuit` = admin seul, `bc_generer_facture` =
admin|secrétaire. Les modules `tableau_de_bord`, `statistiques` et `utilisateurs` ne servent
qu'à l'interface (aucune politique RLS ne les cite).

- [x] **AUTH-90** La matrice ci-dessus est-elle toujours celle de la production ? Relire `role_permissions` en base avant de figer les tests (la table a pu évoluer depuis les migrations). _Source : `app-3.md §1.1`_ — ✔ fixture = base locale bâtie des migrations du dépôt (`tests/rls/isolement.essai.ts`) ; `tests/matrice-miroir.essai.ts` échoue si fixture, tableau §1.6, `MODULES` et l'ancien `ModuleId` divergent ; production relue par un humain via `scripts/comparer-matrice.mjs` (D-AUTH-08)

---

## 2. societes — multi-sociétés et identité légale

### Écrans et fonctionnalités

- [x] **SOC-01** Sélecteur de société (menu société) : liste des sociétés où l'on est membre, bascule ; refus si non membre. _Source : `app.js:872, 1320` ; `session.ts#choisirSociete`_ — ✔ `societes/components/SelecteurSociete.tsx`, `auth-roles/domain/selection.essai.ts` (société perdue → première)
- [x] **SOC-02** Changer de société vide les recherches, ferme les formulaires, recharge toutes les collections de la nouvelle société. _Source : `app.js:1320`_ — ✔ `app/Layout.tsx` : `<Outlet key={societeActive.id}>` remonte l'écran, clés de requête par `societe.id`
- [x] **SOC-03** Société courante au démarrage : la première par ordre alphabétique du nom (session) ; l'ancien écran partait de `'kta'` puis retombait sur la première. _Source : `session.ts#chargerSession`, `app.js:19126`_ — ✔ tri par nom dans `chargerSession`, `domain/selection.essai.ts` (« retombe sur la première »)
- [x] **SOC-04** Couleur d'accent par société appliquée à l'écran et aux documents (`paletteSociete`, contraste AA 4,5 ; défauts `#FF6A1A` / `#182233`). _Source : `app.js#appliquerCouleurSociete`, `api/regles-theme.ts`_ — ✔ écran : `<ThemeSociete>` ; pièces imprimées : `lireIdentiteDocument` décline la palette (titre et total au ton foncé, filet d'accent, bandeau du tableau à la seconde couleur) et télécharge le logo du seau en data-URL ; aperçu HTML par le modèle puis les variables `--color-*-societe*` (D-TRV-10) — `commandes/components/apercu.essai.tsx` (variables de la palette posées sur la fenêtre), mesure `tests/visuel/pdf/` ; parité `tests/parite/documents.essai.ts` (couleurs de `fusionnerReglages`) — les pièces lisent les variables de l'ancienne feuille (`--accent`, `--accent-rgb`…, `impression/palette.ts`, D-PDF-01)
- [x] **SOC-05** Réglages › Organisation : identité légale (raison sociale, forme, capital, RCS, NAF, SIREN/SIRET, TVA intracom, adresse, pays), régime de TVA, e-reporting, TVA sur encaissements, autoliquidation bâtiment, pénalités, indemnité de recouvrement (40 € par défaut), décennale, adresse électronique de réception, IBAN/BIC. _Source : `app.js:12354`_ — ✔ Réglages › Organisation (`reglages/components/SectionOrganisation.tsx`) : toutes les colonnes d'identité, régime de TVA, e-reporting, encaissements, autoliquidation, pénalités, indemnité (40 € par défaut), décennale, adresse de réception, IBAN/BIC — `reglages/components/reglages.essai.tsx`, `societes/domain/societe.essai.ts`
- [x] **SOC-06** Bandeau de complétude de la société (`completudeSociete`, `recommandationsSociete`). _Source : `app.js:12354`, `api/regles-efacture.ts`_ — ✔ `BandeauCompletude.tsx` (manques et recommandations séparés, pied de page personnalisé signalé) — parité `completudeSociete` / `recommandationsSociete` / `messageAnomalies` (`tests/parite/reglages.essai.ts`), `reglages.essai.tsx`
- [x] **SOC-07** Enregistrement par **fusion** : un champ absent de l'onglet affiché est conservé, pas vidé ; `verifierEntite` bloque ce qui est mal formé. _Source : `app.js:13030`_ — ✔ fusion : l'onglet n'écrit que ses colonnes (`reglages.essai.tsx` « seules les colonnes de l'onglet partent ») ; réglages fusionnés dans `infos_entreprise` (`infosAvecReglages`, `societe.essai.ts`) ; SIRET/SIREN/TVA mal formés bloquent (`reglages.essai.tsx`)
- [x] **SOC-08** Logo de la société (aujourd'hui data-URL dans les réglages). _Source : `app-2.md §1.13`_ — ✔ Réglages › Identité visuelle : logo déposé dans le bucket (`<societe>/societe/`), chemin dans `societes.logo_url`, ancienne data-URL montrée à défaut (D-SOC-05) — `societe.essai.ts` (« logo »)
- [x] **SOC-09** Documents légaux de la société (nom, date de validité, fichier) : alerte à ≤ 30 j, « EXPIRÉ » passé. _Source : `app.js:12957, 19111`_ — ✔ Réglages › Documents légaux : table `documents_legaux` + fichier au bucket, alerte au seuil `documentLegal` (30 j), « EXPIRÉ » passé — `reglages/domain/reglages.essai.ts`, `tests/rls/reglages.essai.ts`

### Règles métier

- [x] **SOC-20** Toute table racine porte `societe_id uuid NOT NULL` ; les filles héritent par jointure au parent ; `interlocuteurs` n'a pas de `societe_id` (lu via `clients!inner(societe_id)`). _Source : schéma §1.1_ — ✔ toutes les listes filtrent `societe_id` ; interlocuteurs par `client_id` ; `tests/rls/isolement.essai.ts`
- [x] **SOC-21** Les lectures sont restreintes à la **société active** (sinon la RLS rend toutes celles du membre) ; chaque liste filtre sur la société active. _Source : `html-adapter.ts#definirSocieteActive`, `app-1.md §2.9`_ — ✔ `.eq("societe_id", societe.id)` dans chaque `api/` ; `tests/rls/isolement.essai.ts` (« société A ne voit rien de B »)
- [x] **SOC-22** Stockage cloisonné par le 1er segment du chemin `<societe_id>/…` (lecture `est_membre`, écriture `peut_ecrire`). _Source : schéma §1.1, §6.3_ — ✔ pièces et logo sous `<societe_id>/…` (`cheminPiece`, `cheminLogo`) — `tests/rls/reglages.essai.ts` (« BETA ne la lit pas »)
- [x] **SOC-23** Champs d'identité en **colonnes** de `societes` (liste blanche `CHAMPS_SOCIETE`), le reste dans `societe_settings.infos_entreprise` (jsonb, dont `reglages` fusionné avec les défauts) ; `notifs_traitees` à part. _Source : `html-adapter.ts`, ts §3.3_ — ✔ identité en colonnes (`api/societe.ts`), réglages dans `infos_entreprise.reglages` lus par `fusionnerReglages` (parité) et écrits par fusion ; `notifs_traitees` jamais touché (upsert de `infos_entreprise` seul)
- [x] **SOC-24** L'identité de l'émetteur est **figée sur la facture** à l'émission (9 colonnes `emetteur_*`) : changer le SIRET de la société ne réécrit pas les anciennes factures. _Source : `api/regles-emetteur.ts`, `app.js:4032, 11871`_ — ✔ `facturation/api/factures.ts#creerFacture` (`identiteEmetteur`), `PageApercuFacture.tsx`, `tests/parite/facturation.essai.ts` (« identité de l'émetteur »)
- [-] **SOC-25** Création d'une société : aucune politique INSERT/DELETE (service_role seulement) ; à la création, 8 métiers standard et 28 entrées de référentiel sont posés par déclencheur. _Source : schéma §1.2_ — aucune création de société à l'écran (service seulement) — D-SOC-12
- [x] **SOC-26** Numérotation indépendante par (société, type, année). _Source : schéma §5.1_ — ✔ `tests/rls/numerotation.essai.ts` (« l'admin d'ALPHA en obtient un dans sa société, pas dans BETA »)

### Données

- [x] **SOC-30** `societes` (code unique court `kta`, nom, identité légale, `indemnite_recouvrement` 40, `regime_tva`, `tva_sur_encaissements`, `autoliquidation_batiment`, `ereporting_regime`, `adresse_electronique_*`, IBAN/BIC), `societe_settings(infos_entreprise, notifs_traitees)`. RLS : SELECT membre, UPDATE admin. _Source : schéma §1.2_ — ✔ `societes` lue et écrite (admin seul), `societe_settings` lue et écrite (`reglages/modifier`) — `tests/rls/reglages.essai.ts`
- [-] **SOC-31** Quatre sociétés en production : KTA, CHM, AKT, Alkia. _Source : schéma §1.2, `app.js:37`_ — une donnée de production, aucune liste en dur dans `web/` — D-SOC-12

### Cas limites et corrections cachées

- [x] **SOC-40** La couleur KTA était appliquée avant le chargement : appliquer la couleur **après** avoir su la société. _Source : `app-2.md §4-22`_ — ✔ `<ThemeSociete>` n'applique rien avant la lecture des réglages de la société active — `societes/components/theme.essai.tsx` (« rien n'est posé tant que … »)
- [x] **SOC-41** Changer de société pendant qu'un formulaire est ouvert : l'ancien code posait `editing = null` et un rendu pouvait lever. _Source : `app.js:1320`_ — ✔ `app/Layout.tsx` : changer de société démonte le formulaire ouvert (clé d'`Outlet`)
- [x] **SOC-42** Les chargements RH (dossiers, visites, invitations) sont indexés par société et ne partent pas pendant le chargement global (sinon dossiers vides toute la session). _Source : `app-3.md §4`_ — ✔ toutes les clés RH et d'invitation portent la société ; rien ne part avant l'ouverture de la session (le composant n'est monté qu'ensuite) ; changer de société relit les dossiers de l'autre — `rh/hooks/chargement.essai.tsx`

### Défauts connus de l'ancienne app

- [x] **SOC-50** Liste de repli des sociétés et société par défaut `'kta'` **codées en dur**. _Source : `app.js:37`, `app.js:19126`_ — ✔ aucune liste en dur : sociétés lues dans `membres_societe`, défaut = mémorisée sinon première (`domain/selection.essai.ts`)
- [x] **SOC-51** Logo et documents légaux stockés en data-URL dans le JSON des réglages (poids, pas de bucket). _Source : `app-2.md §1.13`_ — ✔ corrigé : logo et documents légaux au bucket, plus de data-URL écrite (D-SOC-02, D-SOC-05)

---

## 3. clients — clients et interlocuteurs

### Écrans et fonctionnalités

- [x] **CLI-01** Liste des clients ; la recherche porte aussi sur les interlocuteurs ; actions Modifier, + interlocuteur, Supprimer. _Source : `app.js:16779`_ — ✔ `clients/components/clients.essai.tsx` (« cherche aussi dans les interlocuteurs ») ; Modifier / Supprimer / interlocuteurs sur la fiche
- [x] **CLI-02** Fiche client : type de client (cadre de facturation), nom avec autocomplétion de l'annuaire des entreprises, SIRET/SIREN (`chercherSiret`), TVA intracom (bouton de calcul), adresse avec autocomplétion BAN, pays. _Source : `app.js:17044`_ — ✔ type, nom, SIRET/SIREN, TVA calculée, pays ; annuaire des entreprises sous le nom et « Rechercher » à côté des identifiants (`clients/api/annuaire.ts`, file sous le quota, D-CLI-01) ; adresse proposée par la BAN (`api/adresses.ts`) — `clients/components/annuaire.essai.tsx`, parité `tests/parite/annuaire.essai.ts` (source de `entreprise.ts` évaluée)
- [x] **CLI-03** Blocs affichés selon le cadre (`sectionsEfactureVisibles`) : immatriculation, facture électronique (schéma, adresse, code de routage, référence acheteur BT-10), marché public (code service, n° d'engagement, n° de marché), pays. Particulier (B2C) : ni annuaire ni SIRET. _Source : `app.js:17044`, `regles-efacture.ts`_ — ✔ blocs par `sectionsEfactureVisibles` : immatriculation, facture électronique (schéma, adresse déduite du SIRET, routage, référence acheteur), marché public, pays (D-TRV-11) — parité `tests/parite/clients-efacture.essai.ts`, `clients/components/fiche-efacture.essai.tsx`
- [x] **CLI-04** Règlement : délai prédéfini (réception, 30/45/60 net, 30/45/60 fin de mois) ou libre en jours + mode net/fin de mois ; mode de paiement. _Source : `app.js:3056-3110`_ — ✔ `components/ChampsDelai.tsx`, `clients.essai.tsx` (« Autre délai »), `domain/client.essai.ts` (« 0 jour »)
- [x] **CLI-05** Adresses de facturation et de livraison, service comptabilité (contact), notes, bandeau de complétude. _Source : `app.js:17044`_ — ✔ adresses de facturation et de livraison, service comptabilité (nom, e-mail, téléphone), notes, bandeau de complétude (`completudeClient`, phrase de `messageAnomalies`) — parité `tests/parite/clients-efacture.essai.ts`, `fiche-efacture.essai.tsx`
- [x] **CLI-06** Code postal (5 chiffres) → ville proposée par `geo.api.gouv.fr/communes`. _Source : `app.js:2900`_ — ✔ `clients/api/communes.ts` (geo.api.gouv.fr) + `VillesProposees` : communes proposées d'un clic, jamais imposées ; service muet → saisie manuelle tracée — `fiche-efacture.essai.tsx` (« ville proposée »)
- [x] **CLI-07** Interlocuteur : nom (obligatoire), fonction, téléphone, e-mail. _Source : `app.js:16996-17043`_ — ✔ `components/BlocInterlocuteurs.tsx` (nom obligatoire), `tests/rls/filles.essai.ts` (interlocuteurs)
- [x] **CLI-08** Import de clients (voir IMP-10 à IMP-15), bouton visible si `clients` créer **et** modifier. _Source : `app.js:16779, 16811-16995`_ — ✔ `/clients/import`, bouton « Importer un fichier » si `clients` créer ET modifier — `import-export/components/import.essai.tsx`

### Règles métier

- [x] **CLI-20** Nom obligatoire ; `verifierEntite` **refuse ce qui est mal formé** (clé SIRET fausse) mais jamais ce qui manque. _Source : `app.js:17514`_ — ✔ `domain/client.essai.ts` (« exige un nom », « sans aucun identifiant », « SIRET dont la clé est fausse »)
- [x] **CLI-21** SIREN/SIRET valides par Luhn, exception La Poste (SIREN `356000000`, somme % 5). _Source : `regles-efacture.ts#sirenValide/siretValide`, RM-31_ — ✔ `tests/parite/identifiants.essai.ts`
- [x] **CLI-22** TVA intracom FR calculée : clé = (12 + 3 × (SIREN mod 97)) mod 97, sur 2 chiffres. _Source : `regles-efacture.ts:185-197`, RM-31_ — ✔ `tests/parite/identifiants.essai.ts`, `clients.essai.tsx` (« calcule la TVA depuis le SIREN »)
- [x] **CLI-23** Annuaire : écrase nom, adresse, CP, ville, SIREN ; ne remplit TVA, NAF, forme juridique, gérant, adresse électronique **que si vides** ; entreprise radiée → avertissement, pas de blocage ; cadre « acheteur public » seulement **proposé**. _Source : `app.js#appliquerEtablissement`_ — ✔ `domain/annuaire.ts#appliquerEtablissement` : identité écrasée, TVA et adresse électronique si vides ; radiée → avertissement qui remplace « Champs remplis » ; « administration » proposée par un bouton (D-CLI-02) — parité `tests/parite/annuaire.essai.ts` (`appliquerEtablissement` d'`app.js` sur un faux formulaire), `annuaire.essai.tsx`
- [x] **CLI-24** Le délai de paiement n'est remis au défaut du cadre **que lorsqu'on change de type**, jamais à l'ouverture ; B2C → « Paiement à réception ». _Source : `app.js:17044`, `CLE_DELAI_PAR_CADRE`_ — ✔ `clients.essai.tsx` (« passer en Particulier propose le paiement à réception »)
- [x] **CLI-25** Délai du client prioritaire sur celui de la société, `0` (à réception) compris. _Source : `regles-efacture.ts#delaiPaiementRetenu`, RM-20_ — ✔ `tests/parite/identifiants.essai.ts` (« le délai retenu suit le client, puis la société, puis 30 jours net »)
- [x] **CLI-26** À l'écriture d'un document, `client_id`, `client_siret`, `client_siren`, `client_tva_intracom`, `client_pays_code`, `client_code_service`, `client_code_routage`, `cadre_facturation` sont recopiés depuis la fiche **de même nom**. _Source : `html-adapter.ts#rattacherClient`_ — ✔ `clients/domain/rattachement.ts#identiteClientDocument` recopié par `creerFacture` et `modifierBrouillon` (et le cadenas) ; seule `factures` porte ces colonnes, devis/bons/rapports n'ont que `client_id`/`client_nom` (D-CLI-03) — `tests/rls/clients-api.essai.ts` (« la facture recopie l'identité… »)

### Données

- [x] **CLI-30** `clients` : nom, adresse découpée, email, téléphone, siren, siret, tva_intracom, pays_code (FR), code_service, code_routage, reference_engagement, numero_marche, reference_acheteur, `facturation_*`, `livraison_*`, `contact_*`, `cadre_facturation`, `adresse_electronique_*`, `eligibilite_*`, `delai_paiement_jours` (≥ 0), `delai_paiement_mode`, `mode_paiement`. _Source : schéma §2.1_ — ✔ `api/clients.ts` lit et écrit toutes les colonnes saisissables (code service/routage, engagement, marché, référence acheteur, adresse électronique, livraison, contact) ; `eligibilite_*` lues nulle part et jamais écrasées (CLI-41) — `fiche-efacture.essai.tsx` (« … livraison et comptabilité partent aussi »)
- [x] **CLI-31** `interlocuteurs(client_id, nom, fonction, email, telephone)`. Sur les documents, l'interlocuteur est un **texte**, pas une clé. _Source : schéma §2.1-2.2_ — ✔ `api/interlocuteurs.ts` ; sur les documents l'interlocuteur reste un texte (`ChampsEnteteDocument.tsx`)
- [x] **CLI-32** Lecture `clientsRapprochables` (id, nom, siret, siren, cadre, délai) pour l'OCR et les imports ; `resolveClientByNom` = `ilike nom`. _Source : `queries/clients.ts`_ — ✔ `clients/api/clients.ts#listerClientsRapprochables` (id, nom, siret, siren, cadre, délai et mode), complète ou refusée ; lue par l'OCR (`useClientsRapprochables`) et les deux imports ; `resolveClientByNom` sans objet (liens par id, CLI-50) — `tests/rls/clients-api.essai.ts`, `ocr/components/lecture.essai.tsx`

### Cas limites et corrections cachées

- [x] **CLI-40** Recherche d'entreprise désactivée pour un particulier (« laurent johan » ramenait cinq SIRET). _Source : `app-3.md §4`_ — ✔ aucune requête à l'annuaire quand le type n'a pas d'immatriculation (`sectionsEfactureVisibles`, même règle que le bloc SIRET) — `annuaire.essai.tsx` (« laurent johan »)
- [x] **CLI-41** `eligibiliteStatut/Message/VerifieLe` n'ont aucun champ à l'écran : ils sont **reportés** à l'enregistrement, pas écrasés. _Source : `app.js:17514`_ — ✔ `api/clients.ts#modifierClient` n'envoie que les colonnes du formulaire : `eligibilite_*` jamais écrasés
- [x] **CLI-42** `client.delaiPaiement` n'existait pas : 30 j en dur, 428 factures sans échéance ; lire `delaiPaiementJours` + mode. _Source : `app.js:2924`_ — ✔ `domain/delais.ts#delaiPaiementRetenu` (jours + mode), `facturation/api/operations.ts#conditions`

### Défauts connus de l'ancienne app

- [x] **CLI-50** Rattachement document → fiche client **par nom** (`rattacherClient`, `resolveClientByNom ilike`) : homonymes et renommages cassent le lien. Proposer : sélectionner la fiche, écrire `client_id`, garder `client_nom` en étiquette. _Source : `html-adapter.ts`_ — ✔ choix de la fiche par id (`ChampsEnteteDocument.tsx`), `client_nom` en étiquette ; `devis.essai.tsx` (« enregistre l'en-tête »)
- [x] **CLI-51** Suppression d'un client : aucune garde d'usage dans l'écran (les clés des documents sont `SET NULL`). _Source : schéma §2.2_ — ✔ la confirmation de suppression dit combien de devis, factures, bons et chantiers perdront leur lien (`usagesDuClient`, `questionSuppression`) — `fiche-efacture.essai.tsx`

---

## 4. chantiers — chantiers, DPGF, achats, to-do

### Écrans et fonctionnalités

- [x] **CHA-01** Liste en cartes : filtres recherche, conducteur, type (réhabilitation / neuf) ; total DPGF, % d'avancement facturé, nombre de comptes-rendus, devis, factures. _Source : `app.js:13100-13230`_ — ✔ `PageChantiers.tsx` (tableau plutôt que cartes, D-CHA-01) : recherche, conducteur, type, statut, DPGF HT et % (qui voit les prix), CR / devis / factures (`api/chantiers.ts#compteursChantiers`) ; `components/fiche.essai.tsx` (« filtre par type et compte… »)
- [x] **CHA-02** Formulaire chantier : nom, client, conducteur, adresse, type, dates ; champs PPSPS (lot, maître d'ouvrage, maître d'œuvre, SPS, effectif) ; les tableaux annexes sont conservés. _Source : `app.js:13232`_ — ✔ `PageFormulaireChantier.tsx` : + statut, notes, fieldset PPSPS ; colonnes proposées (`20260926020000`, D-CHA-09) ; les annexes vivent dans leurs tables, le formulaire n'y touche pas ; `domain/regles.essai.ts`, `tests/rls/chantiers.essai.ts` (« statut, notes et PPSPS… »)
- [x] **CHA-03** Fiche chantier : comptes-rendus (drapeau `vu`), informations diverses (enregistrées au `blur`), inspections, PPSPS, DOE, DPGF, to-do, devis complémentaires, factures, achats. _Source : `app.js:13356`_ — ✔ `PageFicheChantier.tsx` en onglets (D-CHA-02) : `StatistiquesChantier`, `BlocInfosDiverses` (au blur, seulement si changé), `BlocAffectations`, `BlocComptesRendus` (pastille « non lu », marqué vu à l'ouverture), `BlocDocuments` (pièces du marché, sécurité : inspections, PPSPS, DOE, dates modifiables), `BlocDpgf`, `BlocTodo`, `BlocAchats`, `BlocDevisComplementaires`, `BlocFactures` ; `fiche.essai.tsx`
- [x] **CHA-04** Pièces du marché : DPGF (analyse automatique si xlsx/xls/csv, sinon archive), CCTP, CCAP, Avenant, DGD. _Source : `app.js:14099, 14131`_ — ✔ `BlocDocuments.tsx#BlocPiecesMarche` → `chantier_documents` + bucket `terrain` (D-CHA-10) ; un DPGF .xlsx/.csv déposé ouvre l'import, un PDF est archivé ; .xls refusé avec la marche à suivre (D-CHA-08) ; `tests/rls/chantiers-api.essai.ts` (dépôt/retrait), `tests/rls/chantiers.essai.ts` (stockage)
- [x] **CHA-05** Générer le PPSPS en Word à partir de la société et du chantier. _Source : `app.js:13538`_ — ✔ `domain/ppsps.ts` (contenu mot pour mot) + `fichiers/docx.ts` (Word sans bibliothèque, D-CHA-08), `BoutonPpsps.tsx` ; logo non repris (D-CHA-08) ; `domain/regles.essai.ts` (« le PPSPS reprend… »), `fichiers/fichiers.essai.ts` (docx lisible)
- [x] **CHA-06** Tableau « DPGF chiffré — suivi d'avancement », repliable : + Ligne, + Chapitre, Enregistrer les lignes, Facturer la sélection ; pied Total DPGF HT / Déjà facturé / Reste à facturer. _Source : `app.js:13966, 14008`_ — ✔ `BlocDpgf.tsx` (replier/déplier `aria-expanded`), `FormulaireAjoutDpgf.tsx`, « Enregistrer les lignes (n) », « Facturer la sélection » → `/chantiers/:id/situation?lignes=…` (composé dans `app/routes.tsx`, filtré par `PageSituation`) ; `fiche.essai.tsx` (« une ligne facturée à 100 %… »)
- [x] **CHA-07** Ligne DPGF : case de sélection (désactivée à 100 %), désignation, badge du devis source, quantité, PU, montant, % cumulé, métier, suivi « planifié/total », bouton 📅 Planifier. _Source : `app.js:14013`_ — ✔ `TableDpgf.tsx` : tout, métier en colonne proposée (`chantier_dpgf_lignes.metier`), suivi depuis `planning_taches` ; quantité et prix verrouillés sur une ligne figée (D-CHA-05)
- [x] **CHA-08** Import DPGF CSV/Excel avec correspondance des colonnes (voir IMP-30). _Source : `app.js:13848, 13938`_ — ✔ `domain/import-dpgf.ts` (port à comportement identique), `fichiers/xlsx.ts`, `ImportDpgf.tsx` (feuille, lignes d'en-tête, rôle par colonne, aperçu) ; remplace les lignes non figées (D-CHA-07) ; `tests/parite/chantiers.essai.ts` (800 tirages contre `guessAllColRoles` / `guessSkipRows` / `confirmDpgfMapping` extraits)
- [x] **CHA-09** « Planifier une quantité » d'une ligne DPGF → crée un bon de commande pour cette part. _Source : `app.js:14290, 14323`_ — ✔ `DialoguePlanifier.tsx`, `api/planification.ts#planifierQuantite` (bon + tâche liée, D-CHA-04) ; `tests/rls/chantiers-api.essai.ts`, `tests/parite/chantiers.essai.ts` (contre `confirmPlanifierQte`)
- [x] **CHA-10** Ouvrir la tâche dans le planning (bascule et filtre sur le n° du bon). _Source : `app.js:14356`_ — ✔ chaque part planifiée (« ✓ q ») ouvre son bon de commande (`/commandes/:id`) ; le planning n'existe pas encore dans `web/` (D-CHA-12)
- [x] **CHA-11** Achats : totaux par catégorie avec barre de %, filtre, ajout (catégorie, désignation, montant HT, date) ; catégorie « salarié » : salarié + heures → montant. _Source : `app.js:14173, 14257, 14278`_ — ✔ `BlocAchats.tsx`, `FormulaireAchat.tsx` (+ fournisseur) ; `fiche.essai.tsx` (« main-d'œuvre… »)
- [x] **CHA-12** Factures du chantier (`chantier_id`) : TTC, statut, PDF, envoi. _Source : `app.js:14368`_ — ✔ `BlocFactures.tsx` : numéro, date, TTC (`v_facture_totaux`), statut, « Imprimer / PDF » (aperçu) ; l'envoi par e-mail reste au module facturation (D-CHA-13)
- [x] **CHA-13** Devis complémentaires : création préremplie (client, adresse, CP, ville, chantier), fichiers. _Source : `app.js:14391, 14413`_ — ✔ `domain/liens.ts#lienDevisComplementaire` → `DevisLies` (`lienNouveau`) → `PageEditionDevis` lit adresse / cp / ville ; fichiers dans `chantier_devis_complementaires` (`BlocDevisComplementaires.tsx`) ; `domain/regles.essai.ts`
- [x] **CHA-14** To-do en kanban À faire / En cours / Fait, glisser-déposer, barre de progression, détail (texte, date prévue, salarié, notes) ; retard = date passée et non fait. _Source : `app.js:14429-14565`_ — ✔ `BlocTodo.tsx`, `CarteTodo.tsx` (glisser + ← → au clavier), `DetailTodo.tsx` ; table `chantier_todos` ; `fiche.essai.tsx` (« retard signalé… »), `tests/parite/chantiers.essai.ts` (`chantierTodoStatut`)
- [x] **CHA-15** Devis rattaché à un chantier : ses lignes (hors commentaires / sans désignation) sont copiées dans le DPGF avec `avancementCumule 0` et `devisSourceId`. _Source : `app.js:4697`_ — ✔ `domain/devis-vers-dpgf.ts`, `RepriseDevis.tsx` (geste explicite depuis le DPGF, D-CHA-06) ; `domain/regles.essai.ts` (« reprise d'un devis… »)

### Règles métier

- [x] **CHA-20** Total DPGF HT = Σ qte × PU hors chapitres ; déjà facturé = Σ qte × PU × avancement/100 ; % = arrondi entier. Exemple dans RM-60. _Source : `app.js:13162-13164, 13968`_ — ✔ `chantiers/domain/dpgf.essai.ts`
- [x] **CHA-21** Planifier une quantité : restante = max(0, total − déjà planifié) ; saisie ≤ 0 refusée ; plafonnée à la restante ; montant = qte × PU ; suffixe « (q/total) » si partiel ; métier obligatoire sur la ligne. Exemples : 10 × 45,5, déjà 5, saisie 8 → 5, 227,5, « (5/10) ». _Source : `app.js:14323`_ — ✔ `domain/planification.ts` ; `tests/parite/chantiers.essai.ts` (l'exemple + 300 tirages contre `confirmPlanifierQte` extrait) ; virgule française lue (D-CHA-03)
- [x] **CHA-22** Achat salarié : montant = heures × coût horaire chargé, affiché `toFixed(2)` (32,50 × 7,5 = « 243.75 ») ; répartition % = arrondi entier par catégorie. _Source : `app.js:14176-14178, 14241`_ — ✔ `domain/achats.ts#montantSalarie`, `#totauxParCategorie` ; `tests/parite/chantiers.essai.ts` (contre `onAchatSalarieHeuresChange` extrait, « 243.75 »)
- [x] **CHA-23** Catégories d'achat = référentiel `categorie_achat` (valeur stockée = **code**, libellé renommable), repli `fournitures | salarie | soustraitant`. _Source : `app.js:14141`_ — ✔ `domain/achats.ts#categoriesAchat`, `api/achats.ts#listerCategoriesAchat` ; `domain/regles.essai.ts`
- [x] **CHA-24** Terrain (technicien, sous-traitant) : ne voit que les chantiers **affectés** (`chantier_affectations`) ; tables financières du chantier fermées sans `chantiers/modifier`. _Source : schéma §1.4, §6.4_ — ✔ `tests/rls/isolement.essai.ts` (« le terrain ne voit que les chantiers où il est affecté »), `tests/rls/filles.essai.ts` (DPGF), `tests/e2e/parcours.e2e.ts` (technicien) ; affectation posée depuis la fiche (`BlocAffectations.tsx`), `tests/rls/chantiers.essai.ts` (« l'affectation ouvre le chantier au terrain »)
- [x] **CHA-25** Écriture = `chantiers/modifier` (admin, conducteur) ; lecture pour qui voit `chantiers`. _Source : `app-3.md §1.3`_ — ✔ `app/routes.tsx` (`RouteModule` chantiers créer / modifier), `<Can>` sur la fiche, `tests/rls/filles.essai.ts` (« le conducteur écrit et lit le DPGF »), `hooks/useDroitsChantier.ts` (D-CHA-11)

### Données

- [x] **CHA-30** `chantiers` + filles `chantier_dpgf_lignes` (`avancement_cumule` 0-100, `devis_source_id`), `chantier_todos`, `chantier_documents` (`famille` ∈ dpgf, cctp, ppsps, doe, ccap, avenant, dgd), `chantier_achats` (`date_achat`, `categorie`, `salarie_id`, `heures`), `chantier_inspections`, `chantier_comptes_rendus`, `chantier_devis_complementaires`, `chantier_affectations`. _Source : schéma §2.4_ — ✔ toutes lues et écrites par `chantiers/api/*` ; `tests/rls/chantiers-api.essai.ts` (chaque lecture passe son schéma Zod contre la base locale)
- [x] **CHA-31** Vue `v_chantier_avancement` (montant_total, montant_facture, reste_a_facturer, sans arrondi). _Source : schéma §4.1_ — ✔ `api/chantiers.ts#avancementsChantiers` (liste des chantiers)
- [x] **CHA-32** `chantier_avancement_factures(dpgf_ligne_id, facture_id, avancement_avant/apres, montant_facture)` existe mais **n'est pas écrite** par l'app. _Source : schéma §2.4_ — ✔ écrite par `facturation/api/operations.ts#facturerSituation`, `tests/e2e/facturation.e2e.ts` (situation)

### Cas limites et corrections cachées

- [x] **CHA-40** La date d'achat part sous `dateAchat` (`date_achat`) : sous le nom `date`, elle était écartée sans bruit. _Source : `app.js:14268`_ — ✔ `api/achats.ts#ajouterAchat` ; `fiche.essai.tsx` (« …partent sous date_achat »), `tests/rls/chantiers.essai.ts` (relue)
- [x] **CHA-41** Fichiers de chantier limités à 8 Mo. _Source : `app.js:13472`_ — ✔ `domain/fichiers.ts#verifierFichierChantier`, refus avant envoi dans `Fichiers.tsx#BoutonDepot` ; `domain/regles.essai.ts`

### Défauts connus de l'ancienne app

- [x] **CHA-50** **Perte silencieuse** : `dpgfLignes` et `todoList` n'ont pas de colonne dans `chantiers` et le pont ne déclare que la fille `achats` : DPGF, avancements, `tachesPlanifiees` et to-do **disparaissent au rechargement**. Les tables `chantier_dpgf_lignes` et `chantier_todos` existent. _Source : `app-3.md §4-1`_ — ✔ corrigé : DPGF dans `chantier_dpgf_lignes`, to-do dans `chantier_todos`, parts planifiées dans `planning_taches` ; `tests/rls/chantiers-api.essai.ts`
- [x] **CHA-51** Bon créé depuis le DPGF : `chantierId`, `dpgfLigneId`, `qtePlanifiee` sans colonne dans `bons_commande` (le lien est perdu ; `planning_taches` a `dpgf_ligne_id`/`quantite_planifiee`) ; `conducteur:''` sans `conducteurId`. _Source : `app-3.md §4-6`_ — ✔ corrigé (D-CHA-04) : tâche liée dans `planning_taches`, `conducteur_id` du chantier ; `tests/rls/chantiers-api.essai.ts` (« …planification d'une part »)
- [x] **CHA-52** Après « + Ligne » ou « ✕ », les boutons 📅 Planifier appellent `openPlanifierQteModal('', i)` et ne font rien (`chantierId` oublié). _Source : `app.js:14083`_ — ✔ corrigé par construction : chaque ligne porte son identifiant et le chantier vient de la fiche ; `fiche.essai.tsx` (« planifier sans métier… » ouvre bien la saisie)
- [x] **CHA-53** « + Ligne / + Chapitre » ne capturent pas le DOM : les saisies non enregistrées des autres lignes sont perdues. _Source : `app.js:14049-14098`_ — ✔ corrigé : saisies tenues à part (`BlocDpgf#modifiees`) ; `fiche.essai.tsx` (« une saisie en place survit à l'ajout d'une ligne… »)
- [x] **CHA-54** Achats, to-do, etc. ignorent le retour d'écriture (aucun message d'échec) ; `catch` muets 13566, 13733, 13880. _Source : `app-3.md §4`, `app-2.md §4`_ — ✔ corrigé : chaque mutation affiche son erreur (`Alert`) et fait relire la table (`useFiche#useEcriture`, `onSettled`) ; aucun catch muet (garde-fou)
- [x] **CHA-55** Pour un conducteur (`rh` voir), `coutHoraireCharge` arrive NULL par la vue : le calcul automatique d'un achat salarié ne se déclenche pas. _Source : `app-3.md §4`_ — ✔ traité (D-CHA-14) : l'écran le dit et le montant se saisit à la main ; la vue n'est pas élargie
- [x] **CHA-56** Fichiers de chantier en data-URL dans le JSON du chantier (au lieu du bucket). _Source : `app-2.md §1.14`_ — ✔ corrigé (D-CHA-10) : bucket `terrain`, chemin `<société>/chantiers/<chantier>/…`, URL signée ; `tests/rls/chantiers.essai.ts` (stockage), `tests/rls/chantiers-api.essai.ts`

---

## 5. devis

### Écrans et fonctionnalités

- [x] **DEV-01** Liste : filtres recherche, conducteur, logement, client, interlocuteur, statut ; carte avec Imprimer, E-mail, Dupliquer, Transformer en facture, Créer un BC, Supprimer. _Source : `app.js:4347, 4552`_ — ✔ filtres recherche, statut, conducteur (par id), logement, client, interlocuteur (`devis/domain/liste.ts#filtrerDevis`, `PageDevis.tsx`) ; Imprimer/PDF, E-mail, Dupliquer, Facturer, Créer un BC, Supprimer sur la fiche (`ActionsDevis.tsx`, D-FAC-17)
- [x] **DEV-02** Formulaire : client (obligatoire), interlocuteur, date, conducteur (par id), lieu & locataire (statut du logement), lignes, remise. _Source : `app.js:4586`_ — ✔ `ChampsEnteteDocument.tsx`, `SectionLieu.tsx`, `devis.essai.tsx` (« n'envoie rien sans client »)
- [x] **DEV-03** Éditeur de lignes commun (devis, facture, BC) : types `ligne`, `chapitre`, `commentaire` ; colonnes HT **et** TTC par ligne ; ajouter, dupliquer (copie profonde sans id), supprimer (au moins une ligne vide reste), glisser-déposer. _Source : `app.js:2493-3530`_ — ✔ types, colonnes HT et TTC, ajouter, dupliquer sans id, retirer (`documents/domain/lignes.essai.ts`) ; glisser-déposer remplacé par ↑ / ↓ au clavier (D-047)
- [x] **DEV-04** Ligne par défaut `{type:'ligne', qte:1, unite:'u', prixUnitaire:0, tva:tvaDefaut()}`. _Source : `app.js:2376`_ — ✔ `documents/domain/lignes.essai.ts` (« une ligne neuve : qté 1, unité u, TVA par défaut »)
- [x] **DEV-05** Sous-totaux de chapitre affichés, recalculés par index. _Source : `app.js:3300`, `regles-totaux.ts#sousTotauxChapitres`_ — ✔ `EditeurLignes.tsx`, `tests/parite/totaux.essai.ts` (`sousTotauxChapitres`)
- [x] **DEV-06** Détail de TVA par taux affiché **seulement s'il y a plus d'un taux** ; sinon « Total TVA 20 % ». _Source : `app.js:3236, 4182`_ — ✔ `BlocTotaux.tsx`, `devis.essai.tsx` (« Total TVA 20 % »), `tests/e2e/parcours.e2e.ts` (« TVA 10 % sur 171,00 € »)
- [x] **DEV-07** Remise saisie en %, en HT cible ou en TTC cible. _Source : `app.js:3253-3290`, RM-11_ — ✔ `BlocTotaux.tsx` (%, HT cible, TTC cible), `tests/parite/totaux.essai.ts` (« remise saisie par montant cible »)
- [x] **DEV-08** Unités : référentiel `unite`, repli `u, pièce, h, forfait, m, m², m³, ml, mm, jour` ; unité inconnue conservée. _Source : `app.js:2471, 2486`_ — ✔ référentiel `unite` de la société (ordre de `entreesDuDomaine`), sinon `u, pièce, h, forfait, m, m², m³, ml, mm, jour` ; unité inconnue conservée (`documents/hooks/useUnites.ts`, `tests/parite/documents.essai.ts` « unités proposées ») — D-FAC-15
- [x] **DEV-09** Taux proposés : réglage `tauxTva`, sinon `[tvaDefaut()]` ; un taux enregistré absent de la liste reste proposé. _Source : `app.js:12572`_ — ✔ réglage `tauxTva`, taux enregistré conservé (`LigneEditable.tsx`) ; repli 0 / 2,1 / 5,5 / 10 / 20 — D-048
- [x] **DEV-10** Code article dans la ligne : recherche au clavier (250 ms), sélection, création d'article depuis la ligne (voir ART-10). _Source : `app.js:2546-2690`_ — ✔ recherche au clavier et sélection (`ReferenceArticleLigne`) ; « Créer « X » dans le catalogue » ouvre la fiche article préremplie de la ligne puis revient au document (`articles/components/reference-ligne.essai.tsx`) — D-FAC-16
- [x] **DEV-11** Enregistrer / Enregistrer le brouillon (sans fermer). _Source : `app.js:2437, 4653`_ — ✔ « Enregistrer » garde le devis ouvert (`PageEditionDevis.tsx`), `devis.essai.tsx`
- [x] **DEV-12** Dupliquer → nouveau brouillon daté du jour. _Source : `app.js:4714`_ — ✔ `hooks/useDevis.ts#useDupliquerDevis` (brouillon, `todayISO`, lignes sans id), bouton `ActionsDevis.tsx`
- [x] **DEV-13** Transformer en facture → facture brouillon préremplie, `echeance ''` ; refus si une facture porte déjà ce `devisId`. _Source : `app.js:4754`_ — ✔ `facturation/api/operations.ts#factureDepuisDevis`, `tests/e2e/facturation.e2e.ts` (« Ce devis est déjà facturé ») — l'échéance est calculée, pas vide
- [x] **DEV-14** Créer un bon de commande depuis le devis ; refus si déjà lié ; montant = HT du devis. _Source : `app.js:4766`_ — ✔ `devis/api/operations.ts#bonDepuisDevis` : refus « déjà lié au bon de commande … », montant = HT de `v_devis_totaux` au centime, lignes, lieu et logement recopiés ; bouton « Créer un bon de commande » (`ActionsDevis.tsx`, droit `bons_commande/creer`) — D-FAC-08
- [x] **DEV-15** PDF du devis : « Valable jusqu'au », signature « Bon pour accord » du client seul, pas de mentions légales de facture. _Source : `app.js:3956, 3962, 4032, 4248`_ — ✔ gabarit de l'ancien porté (`documents/impression/gabarit.ts`, D-PDF-01), `PageApercuDevis.tsx` ; parité HTML `tests/parite/impression.essai.ts` ; PDF comparé à l'ancien : 0 px d'écart (`tests/visuel/pdf/`), `tests/e2e/parcours.e2e.ts` (« Valable jusqu'au »)
- [x] **DEV-16** Envoi par e-mail (téléchargement du PDF, `mailto:`, copie du texte). _Source : `app.js:11816-11949`_ — ✔ « Envoyer par e-mail » : objet et corps en parité (`tests/parite/documents.essai.ts` « envoyerDocumentEmail »), PDF à joindre, `mailto:`, copie du texte (`PanneauEmail.tsx`) — D-FAC-04
- [x] **DEV-17** Devis depuis un rapport d'intervention : une ligne de préconisation = une ligne de devis, « x25 m² » en fin de ligne lu comme quantité + unité. _Source : `app.js:4277`, `parsePreconisationsEnLignes`_ — ✔ `lignesDesPreconisations` en parité (`tests/parite/devis.essai.ts`), `devisDepuisIntervention` (refus si déjà transformé, lieu et logement recopiés), bouton `BoutonDevisDepuisRapport` à monter sur la fiche du rapport (module rapports, section 11) — D-FAC-08
- [-] **DEV-18** Sous-traitant : ne voit que ses devis (`sousTraitantEmetteur`), qui est posé sur ses créations ; les autres rôles ne voient que les devis sans émetteur sous-traitant. _Source : `app.js:4347, 4678`_ — Écarté : D-FAC-14 — ni colonne d'émetteur sous-traitant sur `devis`, ni droit `devis` pour le sous-traitant (RLS)

### Règles métier

- [x] **DEV-20** Totaux par `totauxDocument` : TVA par ligne, remise globale, **aucun arrondi** stocké. RM-01 à RM-12. _Source : `regles-totaux.ts:131`_ — ✔ `tests/parite/totaux.essai.ts`
- [x] **DEV-21** Numéro `DEV-AAAA-NNNNNN` attribué **à la première écriture**, brouillon compris (`prochain_numero(societe,'devis')`) ; trou de série toléré. RM-40. _Source : `app.js:4653`, schéma §5.1_ — ✔ `api/devis.ts#enregistrerDevis`, `tests/e2e/parcours.e2e.ts` (`DEV-AAAA-NNNNNN`), `tests/rls/filles.essai.ts`
- [x] **DEV-22** Statuts `brouillon | envoyé | accepté | refusé` ; défaut `brouillon` ; **aucun geste de transition** dans l'écran (le statut est reconduit). RM-50. _Source : `app.js:4514, 4653`_ — ✔ statuts et défaut `brouillon` repris (`domain/devis.essai.ts` « naît brouillon ») ; geste de transition ajouté (D-021)
- [x] **DEV-23** Validité = date + `validiteDevisJours` (30 par défaut) en jours nets ; rien si ≤ 0. RM-24. _Source : `app.js:3956`_ — ✔ `domain/validite.essai.ts`
- [x] **DEV-24** Adresse du devis = adresse du **client** ; lieu d'intervention = `adresseLocataire`, CP, ville. _Source : `app.js:4653`_ — ✔ `domain/devis.essai.ts` (« adresse = siège du client »), `devis.essai.tsx`
- [x] **DEV-25** Champs du logement nettoyés selon le statut : occupant si `occupé` ; étage + n° si `occupé`/`vacant` ; précision si `commune` ; ancien locataire si `vacant`. _Source : `app.js:4643`_ — ✔ `documents/domain/logement.essai.ts` (parité `cleanLogementFields`)
- [x] **DEV-26** Conducteur écrit par id **et** nom (sans le nom, retirer le conducteur laissait l'étiquette) ; ancien document sans id : retrouvé par nom, insensible à la casse. _Source : `app.js#conducteurDuSelect`, `conducteurIdDe`_ — ✔ `conducteur_id` écrit, libellé tenu par le déclencheur ; un ancien devis sans id retrouve son conducteur par le nom, sans casse (`devis/domain/liste.ts#conducteurIdDe`, parité `tests/parite/devis.essai.ts`, `PageEditionDevis.tsx`)
- [x] **DEV-27** Devis accepté → facture (opération TS) : recopie client, interlocuteur, conducteur, remise, `devis_id`, `intervention_id`, `chantier_id`, adresse et logement ; lignes sans id ; `date = aujourd'hui`. _Source : `operations/workflows.ts#accepterDevisEtCreerFacture`_ — ✔ `operations.ts#factureDepuisDevis` (en-tête, lignes sans id, date du jour), `tests/e2e/facturation.e2e.ts` (594,00 € avec remise)
- [x] **DEV-28** Taux de conversion du mois = acceptés / devis datés du mois, arrondi entier (3 dont 1 accepté → 33). _Source : `app-1.md §5.6`_ — ✔ `devis/domain/liste.ts#tauxConversion` (3 dont 1 accepté → 33), affiché en tête de la liste des devis (`tests/parite/devis.essai.ts` « taux de conversion »)

### Données

- [x] **DEV-30** `devis` (numero unique par société, `client_id` SET NULL, `client_nom` NOT NULL, interlocuteur texte, `chantier_id`, `intervention_id`, adresse + bloc locataire, `date`, `remise_pourcentage` 0-100, `statut devis_statut`, `conducteur`, `conducteur_id`). _Source : schéma §2.2_ — ✔ `devis/domain/devis.ts#schemaEnteteDevis`
- [x] **DEV-31** `devis_lignes` (`position`, `type`, `designation` NOT NULL '', `quantite` numeric(14,4), `prix_unitaire` numeric(14,4), `unite`, `tva` numeric(5,2) NOT NULL, `article_reference`, `commentaire`, `montant_ht` sans arrondi, `metier`). _Source : schéma §2.2_ — ✔ `schemaLigneDevis`, `montant_ht` exact (`lignes.essai.ts`), `tests/rls/lignes.essai.ts`
- [x] **DEV-32** Vue `v_devis_totaux` (ht_avant, tva_avant, ht, tva, ttc — sans arrondi). _Source : schéma §4.1_ — ✔ `api/devis.ts#totauxDesDevis`, `tests/e2e/parcours.e2e.ts` (216,00 € dans la liste)

### Cas limites et corrections cachées

- [x] **DEV-40** `articleReference` passait par `parseFloat` (« PLB-001 » → 0) : c'est un champ texte, comme `designation`, `commentaire`, `unite`, `metier`. _Source : `app.js:3524`_ — ✔ `documents/domain/lignes.essai.ts` (« PLB-001 reste une référence texte »)
- [x] **DEV-41** Sous-totaux de chapitre figés (classe CSS inexistante) : recalcul à chaque saisie. _Source : `app.js:3300`_ — ✔ `EditeurLignes.tsx` recalcule à chaque rendu
- [x] **DEV-42** Quantité saisie « 1,5 » en chaîne → `parseFloat` = **1** (virgule tronque) ; l'ancien champ était `type=number`. La nouvelle saisie accepte la virgule (D-013). _Source : `app-1.md §4.3`_ — ✔ D-013, `lib/money.essai.ts` (« admet la virgule »), `devis.essai.tsx` (« 420,50 »)
- [x] **DEV-43** Choisir un article ne change **jamais** la quantité ; la description de l'article devient le commentaire de ligne ; copie, pas lien. _Source : `app.js:2546`_ — ✔ `tests/e2e/parcours.e2e.ts` (« la quantité n'est jamais écrasée »), `articles/domain/article.essai.ts` (D-026)

### Défauts connus de l'ancienne app

- [x] **DEV-50** La secrétaire ne peut pas numéroter un devis (`prochain_numero` exige `peut_ecrire`). Voir AUTH-70. _Source : schéma §10.3-1_ — ✔ proposition `20260925020000`, `tests/rls/filles.essai.ts` (« la secrétaire obtient un numéro de devis ») — D-018
- [x] **DEV-51** Aucun geste pour passer un devis en `envoyé` / `accepté` / `refusé` dans l'écran relevé. _Source : `app-1.md §2.6`_ — ✔ D-021 : statut dans l'en-tête (`PageEditionDevis.tsx`), sous `devis/modifier`
- [-] **DEV-52** Colonne TVA du PDF imprimée brute « 5.5% » (point, sans espace) au lieu de « 5,5 % ». _Source : `app-1.md §4.3`_ — écarté : le client exige la pièce de l'ancien à l'identique, « 5.5% » compris — D-PDF-03
- [x] **DEV-53** `devisChapterTotals` fusionne deux chapitres homonymes ; `devisSelectOptions` exclut les devis déjà liés à un BC. _Source : `app.js:2820`, `app-1.md §4.2-11`_ — ✔ sous-totaux par position (`sousTotauxChapitres`) ; un devis déjà lié à un bon est refusé à la création du bon (`bonDepuisDevis`, message de l'ancien)

---

## 6. articles — catalogue

### Écrans et fonctionnalités

- [x] **ART-01** Liste paginée **côté serveur** : recherche code/désignation (250 ms), filtres Actifs / Retirés / Tous, type (prestation / bien), famille. _Source : `app.js:18739-19105`, `queries/articles.ts`_ — Preuve : `src/modules/articles/components/articles.essai.tsx` (« liste paginée côté serveur »), `tests/rls/articles.essai.ts` (« pagination serveur »).
- [x] **ART-02** Fiche article : code et désignation obligatoires, famille, description, type, unité, PV HT, prix d'achat, TVA (défaut `tvaDefaut()`), géré en stock. _Source : `app.js:18739-19105`_ — Preuve : `domain/article.essai.ts`, `src/modules/articles/components/articles.essai.tsx` (« fiche article »).
- [x] **ART-03** Retirer / Remettre — **jamais de suppression** (des documents citent le code). _Source : `queries/articles.ts#desactiverArticle`_ — Preuve : `src/modules/articles/components/articles.essai.tsx` (« retirer demande confirmation », « aucun bouton de suppression »), `tests/rls/articles.essai.ts` (« retirer cache l'article »).
- [x] **ART-04** Code en double → « Le code « X » existe déjà dans le catalogue. » _Source : `app-3.md §1.3`_ — Preuve : `src/modules/articles/components/articles.essai.tsx` (« code en double »), `tests/rls/articles.essai.ts` (« crée, refuse le code en double »).
- [x] **ART-05** Import du catalogue (voir IMP-01 à IMP-06), aperçu, rapport de rejets. _Source : `integrations/catalogue.ts`_ — Preuve : `components/import.essai.tsx`, `tests/parite/import-articles.essai.ts`, `tests/rls/articles.essai.ts` (« import par lots »).
- [x] **ART-06** Écriture si `articles/modifier` (admin, secrétaire). _Source : `app-3.md §1.3`_ — Preuve : `src/modules/articles/components/articles.essai.tsx` (« catalogue — droits »), `tests/rls/articles.essai.ts` (« qui lit le catalogue »).
- [x] **ART-10** Depuis une ligne de document : chercher un article (`chercherArticlesLigne`), l'appliquer, ou créer l'article à partir de la ligne. _Source : `app.js:2610, 2655, 2679`_ — ✔ branché dans l'éditeur de lignes : recherche, application, et « Créer « X » dans le catalogue » vers la fiche préremplie (`brouillonDepuisLigne`) puis retour au document (`reference-ligne.essai.tsx`, D-FAC-16)

### Règles métier

- [x] **ART-20** `getArticleParCode` ne rend que les articles **actifs**. _Source : `queries/articles.ts`_ — Preuve : `tests/rls/articles.essai.ts` (« retirer cache l'article à la saisie des lignes »).
- [x] **ART-21** Codes TVA de l'import : `INTER` → 10, `NORMA` → 20, `EXO` → 0, `"0"` → 0 (casse ignorée) ; inconnu → 20 **et** signalement ; colonne absente → 20 partout. RM-06. _Source : `api/regles-import-articles.ts`_ — Preuve : `tests/parite/import-articles.essai.ts` (« codes TVA du catalogue »).
- [x] **ART-22** Unicité `(societe_id, code)` ; import par upsert sur ce couple, par lots de 200. _Source : schéma §2.1, `queries/articles.ts`_ — Preuve : `tests/rls/articles.essai.ts` (« crée, puis met à jour », « un lot refusé n'arrête pas les autres »).

### Données

- [x] **ART-30** `articles` : code, designation (index trigramme), unite, `prix_unitaire numeric(14,4)`, `tva numeric(5,2)`, metier, description, `type_article` (`bien|service`, défaut service), `prix_achat`, actif, famille, gere_en_stock. RLS par la matrice `articles`. _Source : schéma §2.1_ — Preuve : schéma Zod `schemaArticle` (validé à chaque lecture), `tests/rls/articles.essai.ts`.

### Cas limites et corrections cachées

- [x] **ART-40** Le terrain n'a aucun accès aux articles (prix). _Source : schéma §6.4_ — Preuve : `tests/rls/articles.essai.ts` (« le technicien et le sous-traitant ne lisent AUCUN article »), `src/modules/articles/components/articles.essai.tsx` (« le technicien n'a pas accès »).

### Défauts connus de l'ancienne app

- [-] **ART-50** `articles.metier` existe mais n'est ni saisi ni recopié sur la ligne. _Source : schéma §2.1 (à confirmer à l'usage)_ — Reproduit : ni saisi ni recopié ; l'import ne l'envoie pas, donc ne l'efface pas (DECISIONS D-025).

---

## 7. commandes — bons de commande, SAV, pièces

### Écrans et fonctionnalités

- [x] **BC-01** Liste : filtres recherche, conducteur, type (BC / SAV), mode de création (normal / sans BC / en attente de BC), logement, métier, client, interlocuteur. _Source : `app.js:6617, 6707, 6736`_ — Preuve : les huit filtres (`domain/filtres.ts#filtrerBons`, `BarreFiltresBons.tsx`) ; `src/modules/commandes/components/commandes.essai.tsx` (« le filtre de mode trie », « filtre par logement et par métier »)
- [x] **BC-02** Carte repliée par défaut, une seule ouverte à la fois ; boutons contact 📞 / 💬 / 📅. _Source : `app.js:6776, 6796, 6808`_ — Preuve : liste en tableau, une ligne par bon ouvrant sa fiche (D-BC-01) ; 📞 / 💬 notent une tentative (`tentatives_contact`), 📅 programme un rappel (`rappel_date`) — `ContactsBon.tsx`, `src/modules/commandes/components/commandes.essai.tsx` (« 📞 note une tentative »), `tests/rls/circuit.essai.ts` (« contacts »)
- [x] **BC-03** Pastille d'étape : Facturé, À facturer, À valider — directeur, À valider — conducteur, Travaux à pointer ; stepper en 4 étapes. _Source : `app.js:7082, 7095, 7100`_ — Preuve : pastille (`BadgeEtape`) et stepper en 4 étapes (`StepperBon.tsx`, `domain/workflow.ts#etapesDuCircuit`) ; `src/modules/commandes/components/circuit.essai.tsx` (« le stepper dit l'étape »)
- [x] **BC-04** Formulaire, trois modes à la création : Nouveau BC / Sans BC / En attente de BC. _Source : `app.js:4783, 8275, 8301`_ — Preuve : `src/modules/commandes/components/commandes.essai.tsx` (« hors brouillon, adresse et ligne… »), `tests/e2e/commandes.e2e.ts` (« bon en attente de BC »).
- [x] **BC-05** Sections : client, interlocuteur, devis lié ; adresse de facturation différente (dépliée si remplie) ; n° de BC (texte multi-lignes), référence chantier, date de réception, date de fin de travaux ; pièce jointe ; lieu et locataire ; conducteur, nature, métiers, notes ; chiffrage (montant global ou par métier, puis lignes avec TVA par ligne). _Source : `app.js:8275`_ — Preuve : devis lié et adresse de facturation dépliée si remplie (`SectionDevisFacturation.tsx`), pièce jointe (`PieceJointe.tsx`), métiers (`SectionMetiers.tsx`), chiffrage global ou par métier (`BlocMontantBon.tsx`) ; `src/modules/commandes/components/commandes.essai.tsx` (« métiers, montant par métier, devis et facturation »)
- [x] **BC-06** Boutons Enregistrer / Enregistrer le brouillon (sans contrôle d'adresse ni de lignes) / Annuler ; anti double clic. _Source : `app.js:8431`_ — Preuve : `src/modules/commandes/components/commandes.essai.tsx` (brouillon enregistré sans adresse ni ligne) ; boutons désactivés pendant l'envoi (anti double clic) ; « Retour à la liste » tient lieu d'Annuler.
- [x] **BC-07** Bon verrouillé → formulaire en **consultation**. _Source : `app.js:8275`, `regles-verrouillage.ts#verrouBonCommande`_ — Preuve : `src/modules/commandes/components/commandes.essai.tsx` (« un bon facturé (facture numérotée) est figé… »).
- [x] **BC-08** « BC reçu » : saisir le n° du client sur un bon en attente → `enAttenteBC=false`, toast « le numéro partira sur sa facture ». _Source : `app.js:6998-7011`_ — Preuve : `src/modules/commandes/components/commandes.essai.tsx` (« BC reçu », « après « BC reçu », Enregistrer garde le numéro reçu », « Entrée … »), `tests/e2e/commandes.e2e.ts`.
- [x] **BC-09** Pièce jointe du bon du client : PDF/JPEG/PNG/WebP ≤ 14 Mo, HEIC converti en JPEG, rangée dans le bucket `terrain` ; retirer = `pieceJointeChemin=null` explicite ; aperçu flottant (image/PDF, URL signée). _Source : `app.js:4826-4853, 1143, 1166`, `regles-piece-jointe.ts`_ — Preuve : `api/documents.ts#remplacerPieceJointe` (bucket `terrain`, `<société>/bons-commande/<bon>/…`, ancien fichier effacé après), retrait = colonnes à NULL, aperçu image/PDF par URL signée (`ApercuPieceJointe`) ; même préparation que la lecture (HEIC converti quand le navigateur le décode, D-BC-09) ; `tests/rls/circuit.essai.ts` (« pièce jointe »), `tests/parite/circuit.essai.ts` (« pièce jointe »)
- [x] **BC-10** Lecture automatique (OCR) d'un bon — voir section OCR. _Source : `app.js:18275`_ — Preuve : « Importer un bon (PDF, photo) » dans la liste → `/commandes/lecture` → formulaire prérempli avec le document retenu ; `src/modules/commandes/components/commandes.essai.tsx` (« le préremplissage de la lecture »), `src/modules/ocr/components/lecture.essai.tsx`
- [x] **BC-11** Montant : simple, ou ventilé par métier (prérempli par les totaux des chapitres du devis lié, en mots entiers) ; lecture seule dès qu'une ligne a du contenu ; sans chapitre → « ⚠ Aucun chapitre … montant à saisir manuellement ». _Source : `app.js:18504, 18532`, `app-3.md §2.9`_ — Preuve : `BlocMontantBon.tsx` : global, ou par métier dès deux métiers, prérempli par les chapitres du devis lié en mots entiers (`domain/metiers.ts#montantDuMetierDansLeDevis`), « ⚠ Aucun chapitre … » ; lecture seule dès qu'une ligne a du contenu ; `src/modules/commandes/components/commandes.essai.tsx` (« le devis lié préremplit les montants par métier »), `src/modules/commandes/domain/circuit.essai.ts`
- [x] **BC-12** Métiers du bon (cases), métier d'un chapitre (« Déduit du titre » / « Aucun métier » / un nom), toast « le bon se planifiera en N interventions » si ≥ 2 métiers. _Source : `app.js:3178, 3335, 18569-18660`_ — Preuve : cases des métiers (`SectionMetiers.tsx`, déduction depuis les chapitres), métier d'un chapitre « Déduit du titre » / « Aucun métier » / un nom (`ChampMetierChapitre.tsx`), « Ce bon se planifiera en N interventions » ; `src/modules/commandes/components/commandes.essai.tsx` (« deux métiers… », « le métier d'un chapitre… »)
- [x] **BC-13** Créer un SAV depuis un bon (un seul SAV par bon) : « Ce qui ne va pas », jusqu'à 5 photos, sans n° de BC ni devis. _Source : `app.js:4950, 8275`_ — Preuve : `PageCreerSav.tsx` (« Ce qui ne va pas », 5 photos, un seul SAV par bon), `api/documents.ts#creerSav` ; `src/modules/commandes/components/circuit.essai.tsx` (« créer un SAV », « un bon qui a déjà son SAV »), `tests/rls/circuit.essai.ts` (« SAV »)
- [x] **BC-14** Clôturer sans facturation (SAV non terminé, admin) ; motif par défaut « Reprise sous garantie ». _Source : `app.js:4885`_ — Preuve : `ActionsCircuit.tsx` (SAV non clos, admin, motif « Reprise sous garantie ») → `bc_cloturer_gratuit` ; `src/modules/commandes/components/circuit.essai.tsx` (« un SAV non clos se clôture… », « le conducteur ne clôture pas »). D-BC-07
- [x] **BC-15** Créer la facture du bon (exige la validation directeur ; refus si déjà facturé). _Source : `app.js:4912`_ — Preuve : `tests/rls/commandes.essai.ts` (« chiffré → la secrétaire génère… »), `tests/e2e/commandes.e2e.ts` (« bon chiffré »), `src/modules/commandes/components/commandes.essai.tsx` (« Créer la facture »).
- [x] **BC-16** Modale « Validation conducteur » : tâches réelles, blocages affichés, bouton actif seulement sans blocage ; travaux supplémentaires (ajouter, retirer, demander le prix si l'on voit les prix). _Source : `app.js:7136-7353`_ — Preuve : panneau « Circuit du bon » de la fiche (D-BC-03) : tâches réelles (`TachesDuBon.tsx`), blocages et bouton actif sans blocage (`ValidationConducteur.tsx`), travaux supplémentaires ajoutés / retirés / chiffrés (`TravauxSupplementaires.tsx`) ; `src/modules/commandes/components/circuit.essai.tsx` (« tâches et validation conducteur », « travaux supplémentaires »)
- [x] **BC-17** Modale « Pré-facture / Validation directeur » : tableau de chiffrage éditable (métier, code, désignation, qté/unité, PU HT), travaux supplémentaires placés dans le chapitre de leur métier, reste en « Travaux supplémentaires constatés sur le chantier », glisser-déposer, totaux, sous-totaux par métier (si ≥ 2 groupes), comptes-rendus terrain, pièce de référence (bon du client / fiche interne / devis) en plein écran. _Source : `app.js:7366-8233`_ — Preuve : `/commandes/:id/prefacture` (D-BC-02) : lignes éditables avec métier des chapitres, travaux placés dans le chapitre de leur métier puis « Travaux supplémentaires constatés… », totaux et sous-totaux par métier (≥ 2 groupes), comptes-rendus terrain, pièce de référence (bon du client / fiche interne / devis) en plein écran ; `src/modules/commandes/components/circuit.essai.tsx` (« pré-facture »), `tests/parite/metiers.essai.ts` (placement)
- [x] **BC-18** Pré-facture : Valider (admin) ou « hors circuit » (admin, avec `confirm` du TTC) ; avertissement si le bon est encore « en attente de BC » (la référence sera figée sur la facture). _Source : `app.js:8146, 8204`_ — Preuve : « Valider la pré-facture » (admin) et « Valider sans passer par le planning » (admin, confirmation avec le TTC) ; avertissement « en attente de BC » ; `src/modules/commandes/components/circuit.essai.tsx` (« hors circuit… », « en attente de BC »)
- [x] **BC-19** Pièces à commander : date de commande, fournisseur, « commandée ». _Source : `app.js:6971, 6979`_ — Preuve : `src/modules/commandes/components/pieces.essai.tsx` (« marque commandée »), `tests/rls/commandes.essai.ts` (« commandée : écriture directe… »). Voir D-043.
- [x] **BC-20** Onglet « Pièces en commande » : À commander (sans date de commande) / Commandées, regroupées en dossiers par fournisseur (« — Fournisseur non renseigné — »). _Source : `app.js:14790-14848`_ — Preuve : `src/modules/commandes/components/pieces.essai.tsx` (« trois onglets ; les commandées rangées par fournisseur »). Onglet « Reçues » ajouté (D-043).
- [-] **BC-21** Pièce reçue → RPC `bc_piece_recue`, rechargement, bascule sur Planning › technicien. _Source : `app.js:7021`_ — Écarté : la bascule vers Planning › technicien n'existe pas : le planning n'est pas repris dans `web/` (section 11). La pièce reçue passe dans « Reçues » et le message dit que le bon retourne au planning — D-BC-03

### Règles métier

- [x] **BC-30** Hors brouillon, un bon exige une **adresse d'intervention** et **au moins une ligne de travaux** (type ligne, désignation non vide ; le prix n'est pas exigé) ; client obligatoire toujours. _Source : `regles-bc.ts:431` (`manquesBonCommande`), `app.js:8431`_ — Preuve : `tests/parite/commandes.essai.ts` (« manquesBonCommande »), `domain/enregistrement.essai.ts`, `src/modules/commandes/components/commandes.essai.tsx`.
- [x] **BC-31** `numero_bc` = référence **du client** (texte, plusieurs numéros possibles) ; vide → sentinelle « En attente de BC » ou « Sans BC » ; un numéro saisi fait sortir du mode sans BC / en attente. `numero_interne` `BC-AAAA-NNNNNN` posé par la base à la création. RM-41. _Source : `app.js:8271, 8431`, schéma §5.1_ — Preuve : `domain/regles.essai.ts` (« numéro du bon »), `tests/rls/commandes.essai.ts` (« numéro interne … posés par la base »).
- [x] **BC-32** Référence client BT-13 = **première ligne** de `numero_bc`, en écartant `""`, `SAV-…`, « Sans BC », « En attente de BC » (`'  BC-123 \nautre'` → `BC-123`). _Source : `regles-bc.ts#refBonCommandeClient`, SQL `ref_bc_client`_ — Preuve : `tests/parite/commandes.essai.ts` (« refBonCommandeClient »).
- [x] **BC-33** Montant du bon : Σ des montants par métier, ou montant global saisi ; **dès qu'une ligne est renseignée**, montant = HT des lignes (sans arrondi côté client). Un bon **sans ligne garde son montant saisi** (12 bons, 25 323,48 €). RM-13. _Source : `app.js:8431-8520`_ — Preuve : `domain/regles.ts#montantDuBon`, `domain/enregistrement.ts` : Σ des montants par métier, ou montant saisi, et HT des lignes dès qu'une est renseignée ; ventilation `montant_par_metier` enregistrée et relue (relecture 3, M9) — `tests/parite/commandes.essai.ts` (« montant du bon »), `src/modules/commandes/domain/circuit.essai.ts` (« enregistrement d'un bon ventilé par métier »). Arrondi au bord : D-044
- [x] **BC-34** Lignes enregistrées = toutes, s'il existe au moins une ligne renseignée ; sinon `[]`. _Source : `app.js:8431`_ — Preuve : `domain/enregistrement.essai.ts` (brouillon sans ligne renseignée → `[]`).
- [x] **BC-35** `statut` libre hérité (défaut « en attente » ; en cours, terminé, annulé) ; `metier` = premier métier coché. _Source : `app.js:8431`_ — Preuve : `statut` « en attente » posé à la création, jamais réécrit ni affiché (D-BC-13) ; `metier` = premier métier coché, jamais `""` (`src/modules/commandes/domain/circuit.essai.ts`, `src/modules/commandes/components/commandes.essai.tsx`)
- [x] **BC-36** Circuit `statut_workflow` : `en_cours → pret_a_chiffrer → chiffre → facture`, ou `cloture_gratuit` ; ne change **que par RPC** (déclencheur `bons_commande_etat_reserve`). RM-53. _Source : schéma §5.3, §7_ — Preuve : jamais envoyé par `api/bons.ts` ; `tests/rls/commandes.essai.ts` (« statut_workflow ne se modifie pas en direct ») ; à l'INSERT, seulement avec la proposition `20260925060000` (« naît quand même au début »).
- [x] **BC-37** Tâches : `planifiee → realisee` (équipe), `realisee → validee | refusee` (conducteur/admin, refus motivé), `refusee → realisee` ; une tâche validée ne se rouvre pas. RM-54. _Source : `regles-taches.ts`, schéma §5.3_ — Preuve : `api/circuit.ts` (`tache_marquer_realisee`, `tache_valider`, refus motivé) ; `tests/rls/circuit.essai.ts` (« une tâche par métier ; déclarée faite, arbitrée ; refus motivé ; une tâche validée ne se rouvre pas »), `tests/parite/circuit.essai.ts` (« tâches », `regles-taches` importé)
- [x] **BC-38** Blocages de la validation conducteur : `aucune_tache`, `metiers_sans_tache`, `taches_non_pointees`. _Source : `regles-bc.ts#blocagesValidationConducteur`_ — Preuve : `domain/circuit.ts#blocagesValidationConducteur`, `tests/parite/circuit.essai.ts` (`regles-bc` importé), `tests/rls/circuit.essai.ts` (« validation conducteur : refusée tant qu'un métier… »)
- [x] **BC-39** Blocages du chiffrage, dans cet ordre : `deja_facture` → `cloture_gratuit` → `deja_chiffre` → (sauf hors circuit) `aucune_tache` / `taches_non_validees` → `travaux_non_chiffres` → `lignes_sans_prix` ; message : 5 détails puis « et N autre(s) » ; `deja_chiffre` et `deja_facture` s'affichent comme gestes accomplis. _Source : `regles-bc.ts#blocagesChiffrage`, `app.js:7480, 8001`_ — Preuve : `domain/circuit.ts#blocagesChiffrage`, `messageBlocages` (5 détails puis « et N autre(s) »), `BLOCAGES_ACCOMPLIS` affichés comme gestes accomplis (`PagePrefacture.tsx`) ; `tests/parite/circuit.essai.ts`
- [x] **BC-40** Étape affichée (`etapeWorkflow`) : facture liée → Facturé ; `valideDirecteur` → À facturer ; `valideConducteur` → Directeur ; tâches terminées → Conducteur ; sinon Terrain. _Source : `app.js:7082`_ — Preuve : `tests/parite/commandes.essai.ts` (source d'`etapeWorkflow` extraite d'app.js, D-045), `domain/workflow.essai.ts`.
- [x] **BC-41** `bcTachesTerminees` : `nbTaches > 0` et aucune tâche non pointée (un bon **sans tâche n'est pas terminé**). _Source : `app.js:7055`_ — Preuve : `tests/parite/commandes.essai.ts` (`bcTachesTerminees`), `domain/workflow.essai.ts` (« un bon sans tâche n'est pas terminé »).
- [x] **BC-42** File Validation (`etapeValidation`) : `hors_file` si validé directeur ou 0 tâche ; `pret` si validé conducteur ; `travaux_en_cours` si au moins une tâche pointée ; sinon `hors_file`. « À facturer » = validé directeur sans facture liée. _Source : `regles-bc.ts#etapeValidation`_ — Preuve : `tests/parite/commandes.essai.ts` (`etapeValidation`).
- [x] **BC-43** Champs **dérivés des tâches** au chargement : `metiersFait`, `dateOrigineFait`, `nbTaches`, `tachesNonPointees`, `valideConducteur` (toutes validées, ≥ 1 tâche), `valideDirecteur` (`statut_workflow ∈ {chiffre, facture}`), état de la pièce ; **recharger** après tout geste sur une tâche. _Source : `html-adapter.ts#reconstituerWorkflow`, `app.js:9953`, `CLAUDE.md`_ — Preuve : `domain/workflow.ts#circuitDuBon`, recalculé à chaque lecture (`api/bons.ts#avecCircuit`) ; tout geste recharge la collection (`hooks/useBons.ts#useRecharger`) ; `domain/workflow.essai.ts`.
- [x] **BC-44** Circuit clos = `chiffre | facture | cloture_gratuit` ou une facture liée ; « en attente planning » = pas SAV, circuit ouvert, non validé conducteur. _Source : `app.js:1613, 8836`_ — Preuve : `domain/circuit.ts#circuitTermine`, `enAttentePlanning` ; `tests/parite/circuit.essai.ts` (`circuitTermine` extrait d'app.js), `src/modules/commandes/domain/circuit.essai.ts`
- [x] **BC-45** Verrou : un bon est figé dès qu'une facture **numérotée** le désigne (liste blanche : circuit, conducteur, agenda, suivi interne, `facturation_*`, pièce jointe, `numero_interne` ; ré-orthographe de métier tolérée) ; un bon facturé ne se supprime pas. RM-52. _Source : schéma §7, `regles-verrouillage.ts`_ — Preuve : `tests/parite/commandes.essai.ts` (`verrouBonCommande`), `src/modules/commandes/components/commandes.essai.tsx` (bon figé). Les LIGNES d'un bon facturé ne sont figées en base qu'avec la proposition `20260925050000` (relecture 3, I3) ; l'écran invalide la fiche du bon à chaque geste de facturation.
- [x] **BC-46** Travaux supplémentaires : `a_chiffrer` → `chiffre` (premier prix saisi, prix ≥ 0, virgule acceptée) → `integre` (devenu ligne du bon) ; `refuse` à la clôture gratuite ; origine `conducteur` si l'auteur est conducteur/admin, sinon `technicien` ; TVA 10 par défaut. _Source : `app.js:7266-7330, 8041`, schéma §2.3_ — Preuve : `api/circuit.ts#ajouterTravail` (« à chiffrer », TVA 10, origine selon le rôle), `chiffrerTravail` (premier prix, ≥ 0, virgule acceptée), statut `integre` à la validation, `refuse` à la clôture ; `tests/rls/circuit.essai.ts` (« travaux supplémentaires », « clôture »), `src/modules/commandes/components/circuit.essai.tsx`
- [x] **BC-47** Validation directeur : 1) prix des travaux chiffrés (quantité 1 et unité `u` par défaut), 2) lignes du bon **puis** statut `integre`, 3) `bc_chiffrage_valide` (après `bc_passer_pret_a_chiffrer` si `en_cours`). _Source : `app.js:8065, 8121, 8146`_ — Preuve : `api/circuit.ts#validerPrefacture` : 1) prix (quantité 1 et unité `u` par défaut), 2) lignes du bon puis `integre`, 3) `bc_passer_pret_a_chiffrer` si `en_cours`, puis `bc_chiffrage_valide` ; `tests/rls/circuit.essai.ts` (« dans le circuit… »)
- [x] **BC-48** `bc_chiffrage_valide` refuse s'il reste un travail `a_chiffrer` ; hors circuit : `en_cours|pret_a_chiffrer → chiffre` sans planning, tracé au journal. _Source : schéma §5.3_ — Preuve : `tests/rls/circuit.essai.ts` (« bc_chiffrage_valide » refusé s'il reste un travail à chiffrer ; hors circuit `en_cours → chiffre` tracé au journal)
- [x] **BC-49** `bc_generer_facture` (admin|secrétaire) : facture **brouillon sans numéro** ; `adresse` = client, `adresse_locataire` = lieu des travaux ; `ref_bon_commande_client` = `ref_bc_client(numero_bc)` ; délai client > société > 30 j net, échéance et conditions calculées ; émetteur figé ; lignes du bon dans l'ordre puis travaux `chiffre` ; sans ligne : forfait « Travaux - BC … » au montant du bon, TVA 10. _Source : schéma §5.3_ — Preuve : `tests/rls/commandes.essai.ts` (facture brouillon sans numéro, référence client, lieu en `adresse_locataire`, refus au conducteur et au second appel).
- [x] **BC-50** Clôture gratuite (admin) : toute valeur sauf `facture` → `cloture_gratuit`, `gratuite=true`, travaux `a_chiffrer` → `refuse`, motif journalisé. _Source : schéma §5.3_ — Preuve : `tests/rls/circuit.essai.ts` (« clôture sans facturation : admin seul ; travaux → refusé ; gratuité et motif gardés »)
- [x] **BC-51** SAV : nouveau bon `SAV-AAAA-NNNNNN` (`prochain_numero('sav')`), `bon_commande_parent_id` = bon d'origine, en-tête recopié (sauf id, dates, legacy_id, numéros). _Source : `queries/bonCommande.ts#createSAV`, `app.js:379`_ — Preuve : `domain/sav.ts#enteteSav`, `api/documents.ts#creerSav` (`prochain_numero('sav')`) ; `tests/rls/circuit.essai.ts` (« SAV »), `src/modules/commandes/domain/circuit.essai.ts` (« SAV »)
- [x] **BC-52** Pièce reçue (RPC) : refusée si une tâche est validée ; retire le drapeau pièce, vide `date_tache` des tâches et les dates de planification du bon (y compris par métier), journalise `piece_en_commande → a_replanifier`. _Source : schéma §5.3_ — Preuve : `tests/rls/commandes.essai.ts` (drapeau levé, `date_tache` vidée, refus sur un bon chiffré avec le motif de la base).
- [x] **BC-53** Métier d'une ligne : `NULL` = lu sur le titre du chapitre ; un nom ; ou `(aucun)` = refus délibéré ; **jamais `""`** ; précédence « choisi > exact > contenu en mots entiers > approchant (Levenshtein ≤ 2 sur mots ≥ 6) » dans `metierDeLaLigne`, lue par l'écran **et** le planning. _Source : `regles-metiers.ts`, `CLAUDE.md`_ — Preuve : `domain/metiers.ts` (port de `regles-metiers`, `METIER_AUCUN`, `metierChoisi` rend NULL et jamais `""`), lu par le formulaire ET la pré-facture ; `tests/parite/metiers.essai.ts`, `src/modules/commandes/components/commandes.essai.tsx` (« le métier d'un chapitre… »)
- [x] **BC-54** Métiers disponibles = métiers déclarés + métiers employés sur les bons de la société. _Source : `app.js:18569`_ — Preuve : `hooks/useBons.ts#useMetiersDisponibles` (table `metiers` + métiers employés, `referentielMetiers`) ; `tests/parite/metiers.essai.ts`, `tests/rls/circuit.essai.ts` (« les métiers déclarés… »)
- [x] **BC-55** Montants par métier = `montantsParMetier` (arrondi au centime à chaque ajout). _Source : `regles-metiers.ts`_ — Preuve : `domain/metiers.ts#montantsParMetier` (arrondi au centime à chaque ajout, en décimal) ; sous-totaux par métier de la pré-facture ; `tests/parite/metiers.essai.ts`
- [x] **BC-56** Le terrain ne voit ni `montant`, ni `montant_par_metier`, ni `montant_sous_traitant`, ni PU des lignes, ni prix des travaux supplémentaires (vues `*_terrain`). _Source : schéma §4.2_ — Preuve : `tests/rls/commandes.essai.ts` (« le technicien lit les bons et leurs lignes, sans AUCUN montant », sous-traitant idem), `src/modules/commandes/components/commandes.essai.tsx` (« le technicien voit les bons SANS montant »).

### Données

- [x] **BC-60** `bons_commande` (61 colonnes) : `numero_interne`, `numero_bc`, `sans_bc`, `en_attente_bc`, client, interlocuteur, `devis_id`, `bon_commande_parent_id`, lieu + locataire (`telephone_locataire`), `facturation_*`, dates (réception, planifiée, fin, fin de travaux, terminée, rappel), `metier`, `metiers` jsonb, `schedule_par_metier`, `montant_par_metier`, `montant numeric(14,2)`, `montant_sous_traitant`, `statut`, `statut_workflow`, `gratuite(_motif)`, `tentatives_contact` jsonb, `reference_chantier`, `nature_travaux`, `piece_jointe_*`, `conducteur(_id)`. _Source : schéma §2.3_ — Preuve : `domain/bon.ts#schemaBon` lit toutes les colonnes du bon par la vue ; `enteteAEnregistrer` écrit client, lieu, logement, `facturation_*`, dates, `metier`/`metiers`, `montant_par_metier`, `devis_id`, `probleme_description` ; `piece_jointe_*` par `remplacerPieceJointe`, `tentatives_contact`/`rappel_date` par `ecrireContacts`, `gratuite*` par la RPC. Les colonnes de planification (`date_planifiee`, `schedule_par_metier`, `montant_sous_traitant`…) ne sont jamais envoyées : elles appartiennent au planning (D-BC-03)
- [x] **BC-61** Lecture par les vues `v_bons_commande_terrain` et `v_bon_commande_lignes_terrain` (masquage des prix) ; écriture dans les tables. _Source : schéma §4.2, `CLAUDE.md`_ — Preuve : `api/bons.ts` (lecture par les vues, écriture par les tables), `tests/rls/commandes.essai.ts`. D-040.
- [x] **BC-62** `bon_commande_lignes` (+ `montant_ht`, `metier`), `bon_commande_photos(chemin, legende, position)`, `planning_taches`, `tache_travaux_supplementaires` (+ vue terrain), `workflow_journal`. _Source : schéma §2.3_ — Preuve : lignes (`metier` à trois états), `bon_commande_photos` (`creerSav`, `listerPhotos`), `planning_taches` (`listerTaches`, `creerTachesManquantes`), `tache_travaux_supplementaires` + `v_travaux_supplementaires_terrain` (`listerTravaux`), `workflow_journal` (`journalDuBon`) ; `tests/rls/circuit.essai.ts`
- [x] **BC-63** RPC : `bc_piece_recue`, `tache_sauvegarder_terrain`, `tache_marquer_realisee`, `tache_valider`, `bc_passer_pret_a_chiffrer`, `bc_chiffrage_valide`, `bc_chiffrage_valide_hors_circuit`, `bc_generer_facture` (→ uuid), `bc_cloturer_gratuit`. _Source : schéma §5.3_ — Preuve : toutes appelées : `bc_piece_recue`, `tache_marquer_realisee`, `tache_valider`, `bc_passer_pret_a_chiffrer`, `bc_chiffrage_valide`, `bc_chiffrage_valide_hors_circuit`, `bc_generer_facture`, `bc_cloturer_gratuit` (`api/*.ts`) ; `tests/rls/circuit.essai.ts`, `tests/rls/commandes.essai.ts`. `tache_sauvegarder_terrain` relève de la saisie terrain (planning, D-BC-03)

### Cas limites et corrections cachées

- [x] **BC-70** Valider la dernière tâche n'apparaissait pas dans Facturation › Validation : recharger la collection des bons après chaque geste de tâche. _Source : `app.js:9953`, `CLAUDE.md`_ — Preuve : `hooks/useBons.ts#useRecharger` invalide toute la collection après chaque geste ; `tests/e2e/commandes.e2e.ts` (la pièce reçue passe aussitôt dans « Reçues »).
- [x] **BC-71** Les boutons Valider / Hors circuit de la pré-facture sont refermés à chaque ouverture et à chaque erreur (le contournement restait visible sur le bon suivant). _Source : `app.js:7366`_ — Preuve : la confirmation « hors circuit » naît fermée (état du composant) et se referme à chaque erreur ; `src/modules/commandes/components/circuit.essai.tsx` (« … refermé après un refus (BC-71) »)
- [x] **BC-72** Pré-facture : « PLB-001 » et « PEINTURE » passaient par `parseFloat` ; métier vidé = suppression de la clé, jamais `""`. _Source : `app.js:7652`_ — Preuve : `domain/prefacture.ts#lirePrixTravail` (« PLB-001 » refusé, négatif refusé), `metierChoisi` (métier vidé = NULL) ; `src/modules/commandes/domain/circuit.essai.ts` (« pré-facture »), `src/modules/commandes/components/circuit.essai.tsx`
- [x] **BC-73** Pièce du client : `pieceJointeChemin` (depuis le 15/09) **ou** `pieceJointeData` historique ; URL signée une seule fois. _Source : `app.js:7824, 7838`_ — Preuve : le schéma n'a que `piece_jointe_chemin` (la data-URL d'avant le 15/09 n'a jamais eu de colonne) ; l'URL signée est demandée une fois par chemin (`useUrlPieceJointe`, `staleTime`)
- [x] **BC-74** `numeroBC` éditable même en attente ou sans BC (sinon le bon en attente n'avait aucune issue). _Source : `app-2.md §4-11`_ — Preuve : `SectionBon` (numéro saisissable dans les trois modes), `tests/e2e/commandes.e2e.ts` (« BC reçu » sur un bon en attente).
- [x] **BC-75** Une colonne dérivée envoyée à l'écriture faisait voir toutes les lignes comme modifiées → delete + insert refusé par la facture figée : `montant_ht` exclu de la comparaison des lignes. _Source : `CLAUDE.md`, `html-adapter.ts#enfantsIdentiques`_ — Preuve : `documents/api/lignes.ts#synchroniserLignes` met à jour les lignes gardées par leur id (upsert) et ne supprime que celles qui ont disparu : aucune comparaison, donc aucun delete + insert ; les lignes gardent leur uuid après enregistrement (relecture 3, M12) — `tests/rls/lignes.essai.ts`, `commandes.essai.tsx` (« … la ligne ajoutée n'est pas réinsérée »)
- [x] **BC-76** Retour de pièce : vider les champs côté client ne servait à rien (les tâches gardaient leur date) → RPC. _Source : `app.js:7012`_ — Preuve : « Pièce reçue » passe par `bc_piece_recue` (`api/pieces.ts#pieceRecue`), qui vide `date_tache` — `tests/rls/commandes.essai.ts` (« pièces »)
- [x] **BC-77** Facture depuis un BC : recopiait l'adresse du chantier dans `adresse` (client) et perdait CP/ville ; naissait « impayée ». _Source : `app.js:4912`_ — Preuve : la facture naît par `bc_generer_facture` : brouillon, adresse du client en `adresse`, lieu en `adresse_locataire` (`tests/rls/commandes.essai.ts`).
- [x] **BC-78** Rapport lié à un BC : facturait en forçant `valideDirecteur` ; passage obligé par la pré-facture (hors circuit : admin). _Source : `app.js:4277`_ — Preuve : aucun chemin de `web/` ne force `valideDirecteur` : il se DÉRIVE de `statut_workflow` (`circuitDuBon`), et la facture d'un bon ne naît que par `bc_generer_facture` sur un bon chiffré (pré-facture, hors circuit réservé à l'admin). Les rapports ne sont pas repris
- [x] **BC-79** BC sans tâche éternellement « à valider » : exclu par `circuitTermine`. _Source : `app.js:1629`_ — Preuve : `commandes/domain/files.ts#fileValidation` écarte les circuits clos (`circuitTermine`) ; `src/modules/commandes/domain/circuit.essai.ts` (« file de validation »), `src/modules/commandes/components/circuit.essai.tsx` (« un bon clos gratuitement n'y est pas ») — D-BC-11
- [x] **BC-80** Référence client du BC imprimée seulement si numéro interne (788/826 muets) → toujours. _Source : `app.js:3932`_ — Preuve : `/commandes/:id/apercu` (`PageApercuBon.tsx`, `commandes/domain/impression.ts#contexteBon`, gabarit de l'ancien) : Réf. client, conducteur, métiers ; parité HTML `tests/parite/impression.essai.ts`, `commandes/components/apercu.essai.tsx` ; PDF comparé à l'ancien : 0 px (D-PDF-06)

### Défauts connus de l'ancienne app

- [-] **BC-90** Cases « Métiers réalisés » (`toggleBCMetierFait`) écrivent `metiersFait`, **sans colonne** : rien ne persiste. _Source : `app.js:8234, 8247`_ — Écarté : pas de case « Métiers réalisés » qui n'écrirait rien : l'état par métier se lit sur les tâches (`TachesDuBon.tsx`), et se dérive au chargement (`metiersFait`) — D-BC-08
- [x] **BC-91** Validation **hors circuit** n'intègre pas les travaux chiffrés : ils arrivent en fin de facture sans chapitre. _Source : `app.js:8204`_ — Preuve : hors circuit, les travaux chiffrés sont intégrés aux lignes comme dans le circuit (`validerPrefacture`) ; `tests/rls/circuit.essai.ts` (« hors circuit : … les travaux chiffrés sont intégrés aussi ») — D-BC-05
- [x] **BC-92** `chiffrerTravailSupplementaire(id, prix)` (7316) ne transmet ni quantité ni unité, contrairement à l'appel de 8074. _Source : `queries/planning.ts:533`_ — Preuve : `api/circuit.ts#chiffrerTravail` envoie toujours prix, quantité et unité ; `src/modules/commandes/components/circuit.essai.tsx` (« … chiffre »), `tests/rls/circuit.essai.ts`
- [-] **BC-93** `v_bons_commande_terrain` **n'expose pas `telephone_locataire`** : écrit, jamais relu. _Source : schéma §10.1-1_ — Écarté côté écran : le téléphone n'est ni lu ni écrit (pour ne pas l'effacer) ; correction de la vue à proposer — DECISIONS D-041.
- [x] **BC-94** Préfixe `BC` défini par une ligne `compteurs` de **2026** seulement : en 2027, `BON-2027-000001`. _Source : schéma §10.3-4_ — Preuve : proposition `20260926030000_le_prefixe_bc_ne_depend_pas_de_l_annee.sql` ; `tests/rls/circuit.essai.ts` (« [proposition] préfixe des bons »)
- [-] **BC-95** `bc_generer_facture` force `mode_paiement = 'virement'` (ignore `clients.mode_paiement`), ne recopie pas `conducteur_id`, ligne forfait TVA 10 codée en dur. _Source : schéma §10.3-6_ — Écarté : `bc_generer_facture` est tenue par la chaîne de facturation (reprise récente de l'émetteur) : sa correction (mode de paiement du client, `conducteur_id`, TVA du forfait) est une migration à écrire, listée dans `migrations-proposees.md` — D-BC-14
- [x] **BC-96** Vue Validation incohérente : le compteur inclut `travaux_en_cours`, le filtre ne garde que `valideConducteur && !valideDirecteur` : filtrer fait disparaître des bons. _Source : `app.js:5152, 5541`_ — Preuve : `commandes/domain/files.ts#fileValidation` / `filtrerFile` : le compteur de chaque filtre est celui de sa liste (`PagesFacturationBons.tsx`) ; `src/modules/commandes/components/circuit.essai.tsx` (« le compteur de chaque filtre… »), `src/modules/commandes/domain/circuit.essai.ts`
- [x] **BC-97** `updatePieceCommandeChamp` ignore l'échec d'écriture. _Source : `app.js:6971`_ — Preuve : `api/pieces.ts#marquerCommandee` lève sur 0 ligne écrite ; `tests/rls/commandes.essai.ts` (refus à la secrétaire). D-043.
- [x] **BC-98** `montant` calculé en flottant brut côté client, arrondi silencieusement par `numeric(14,2)` à l'écriture. _Source : `app.js:8081, 8130, 8493`, schéma §2.3_ — Preuve : `tests/parite/commandes.essai.ts` (« montant du bon ») ; DECISIONS D-044.
- [-] **BC-99** `statut` texte libre hérité à côté de `statut_workflow` : deux statuts pour un bon. _Source : schéma §2.3_ — Écarté : `statut` reste « en attente » à la création, n'est ni réécrit ni affiché ; seul `statut_workflow` (et l'étape dérivée) dit où en est le bon — D-BC-13

---

## 8. facturation — factures, avoirs, règlements, situations de travaux

### Écrans et fonctionnalités — factures

- [x] **FAC-01** Sous-onglets : Factures, Avoirs, Validation, À facturer, Règlements ; pour un sous-traitant : Mes factures, Factures « <société> », Règlements. Filtres propres à chaque vue. _Source : `app.js:5125-5166, 5427`_ — ✔ Factures, Avoirs, Validation, À facturer, Règlements (`OngletsFacturation.tsx`, onglets filtrés par les droits) ; vues sous-traitant écartées (D-FAC-09)
- [x] **FAC-02** Liste : filtres + étiquettes `payee` / `partiel` / `impayee` / `retard` ; avoirs : `avoir`, `avoir_impute`, `avoir_disponible` (jamais « impayée » ni « retard ») ; badges règlement, avoir, retard. _Source : `app.js:5550, 5859`_ — ✔ `PageFactures.tsx` (filtre d'état, badges), `domain/etat.essai.ts` (« un avoir n'est jamais impayé ni en retard »)
- [x] **FAC-03** Badge de délai : « En retard de N j », « Échéance aujourd'hui », « Échéance dans N j » ; rien si reste ≤ 0,01. _Source : `app.js:6513, 6520`_ — ✔ `domain/etat.essai.ts` (« retard : reste > 0,01 et échéance passée »)
- [x] **FAC-04** Formulaire facture : client (obligatoire), interlocuteur, date, conducteur, lieu & locataire, lignes, remise, conditions de paiement (délai préréglé ou libre, mode net/fin de mois), mode de paiement, échéance calculée **ou** saisie à la main, date de fin d'exécution, réf. marché, réf. BC client, devis d'origine. _Source : `app.js:5949, 3001-3110`_ — ✔ client, interlocuteur, date, conducteur, lieu, lignes, remise, conditions (délai préréglé ou libre, net / fin de mois : `ChampDelai.tsx`), mode, échéance calculée ou saisie, fin d'exécution, réf. marché, réf. BC, devis d'origine (`ChampsReglementFacture.tsx`)
- [x] **FAC-05** Enregistrer / Enregistrer le brouillon ; « Enregistrer » **masqué** sur une facture émise ; refus local si verrou `emise`. _Source : `app.js:6013, 6067`_ — ✔ « Enregistrer le brouillon » ; une facture émise s'ouvre en `VueFactureEmise` sans bouton ; `modifierBrouillon` filtre `numero is null` ; `tests/e2e/facturation.e2e.ts`
- [x] **FAC-06** Bouton « 🧾 Émettre » : facture sans numéro, pas un avoir, `peutFacturer` (admin, secrétaire). _Source : `app.js:5922, 6242`_ — ✔ `FormulaireFacture.tsx` (« Émettre la facture », `factures/modifier`), `tests/e2e/facturation.e2e.ts`
- [x] **FAC-07** Dupliquer (pas un avoir) → brouillon sans numéro, liens d'origine coupés, échéance recalculée. _Source : `app.js:6170`_ — ✔ `domain/duplication.ts#copieDeFacture` (brouillon du jour, liens coupés, échéance recalculée, conducteur par id ; refus sur un avoir), `duplication.essai.ts`, bouton « Dupliquer » (fiche émise et brouillon)
- [x] **FAC-08** Supprimer : désactivé si émise ; motif du refus de la base affiché, rechargement. _Source : `app.js:3581`_ — ✔ « Supprimer le brouillon » absent d'une facture émise, refus affiché (`messageErreur`), liste rechargée (`useSupprimerBrouillon`)
- [x] **FAC-09** Déverrouiller (verrou `telechargee` seulement), avec confirmation. _Source : `app.js:11913`_ — ✔ « Déverrouiller » avec confirmation sur un brouillon sous cadenas (`FormulaireFacture.tsx`, `api/factures.ts#deverrouillerBrouillon`, refusé sur une émise) — D-FAC-05
- [x] **FAC-10** PDF : titre AVOIR / FACTURE D'ACOMPTE / FACTURE ; émetteur **figé** prioritaire (nom, adresse + CP + ville, SIRET, TVA, IBAN ; tél./e-mail courants) ; méta (échéance, devis d'origine, marché, facture rectifiée, motif) ; totaux (détail TVA si > 1 taux, remise, acompte, retenue, **Net à payer toujours**) ; bloc règlement (IBAN/BIC si réglage `afficherIban`, échéance, conditions ou « Règlement par <mode> ») ; mentions légales **sur facture seulement** ; aucune signature. _Source : `app.js:3962, 4032, 4141, 4182, 4222, 4248`_ — ✔ `documents/impression/gabarit.ts` (port de `renderPrintDoc`) + `facturation/domain/impression.ts#contexteFacture` : titre, émetteur figé prioritaire (tél./e-mail/site du jour), échéance, devis d'origine, marché, facture rectifiée et motif, détail TVA si > 1 taux, remise, acompte, retenue, net à payer toujours, IBAN/BIC selon `afficherIban`, conditions ou « Règlement par <mode> », mentions sur facture seulement, sans signature ; parité HTML `tests/parite/impression.essai.ts`, `duplication.essai.ts` ; PDF comparés à l'ancien (facture, avoir, facture longue) : 6 px d'écart, « 40,00 € » des mentions (`tests/visuel/pdf/`) — D-PDF-01, D-PDF-03
- [x] **FAC-11** Génération PDF A4 : pied légal en texte réel sur chaque page (police 7 → 4,5 pt), pas de 2e page vide, resserrement si la dernière page < 12 %. _Source : `app.js:3765, 3794, 3840`_ — ✔ `documents/impression/pdf.ts` : port de `lancerGenerationPdf` / `decoupagePdf` / `resserrerSiPageDeTrop` / `dessinerPiedDePage` avec html2pdf.js 0.14.0 et les options de l'ancien ; facture de 45 lignes : 2 pages des deux côtés, 0 px d'écart (`tests/visuel/pdf/`) — D-PDF-01
- [x] **FAC-12** Imprimer ou ouvrir l'e-mail d'une facture **non numérotée** pose le cadenas `verrouillee` et fige l'instantané d'identité ; facture numérotée : rien (déjà figée). _Source : `app.js:4325, 11871, 11898`_ — ✔ PDF, e-mail et impression d'un brouillon posent le cadenas et figent émetteur + client (`api/factures.ts#verrouillerBrouillon`, `hooks/useImpression.ts#useOptionsPdfFacture` — aperçu et fiche) ; rien sur une facture émise — D-FAC-05
- [x] **FAC-13** E-mail : un avoir est intitulé « avoir », montant `|TTC|` « en votre faveur ». _Source : `app.js:11932`_ — ✔ `documents/domain/email.ts#brouillonEmail` : « avoir », montant absolu « en votre faveur » (parité `envoyerDocumentEmail`, `tests/parite/documents.essai.ts`)
- [x] **FAC-14** Validation / À facturer : bons regroupés par client (dossiers) avec leur étape. _Source : `app.js:5658`_ — ✔ une seule file de chaque (relecture 4, B3, D-R4-02) : `commandes/components/PagesFacturationBons.tsx` (`PageValidation`, `PageAFacturer`, règle `commandes/domain/files.ts`), sous les onglets de Facturation, triée par client avec son étape (`BadgeEtape`) et une recherche ; `/factures/validation` et `/factures/a-facturer` redirigent — `src/app/files-facturation.essai.tsx`
- [x] **FAC-15** Facture depuis un devis, depuis un BC (pré-facture validée), depuis un rapport, depuis une vente de véhicule, depuis une situation (FAC-60). _Source : `app.js:4754, 4912, 4277, 15344, 13315`_ — ✔ depuis un devis (`BoutonFacturerDevis`), un BC (`commandes/ActionsBon` « Créer la facture », `bc_generer_facture`), un rapport (`operations.ts#factureDepuisIntervention`, `BoutonFactureDepuisRapport`), une situation (`PageSituation`) ; vente de véhicule : D-FAC-11
- [-] **FAC-16** Factures sous-traitant (vue ST) : factures « KTA » prêtes (montant ST défini, non couvertes), création unitaire ou groupée mensuelle, « Marquer payée ». _Source : `app.js:5717-5832`_ — Écarté : D-FAC-09 — pas de colonnes d'émetteur ST ni de lien aux bons ; facture d'achat, relève de la réception (section 16)

### Écrans et fonctionnalités — avoirs

- [x] **FAC-20** « Établir un avoir » sur une facture **numérotée** : motif choisi dans la liste ou libre (≥ 5 caractères). _Source : `app.js:6297, 6331`_ — ✔ `FormulaireAvoir.tsx`, `tests/parite/facturation.essai.ts` (« refus d'avoir »)
- [x] **FAC-21** Motifs proposés : Erreur de facturation (quantité ou montant) ; Prestation non réalisée ; Travaux non conformes ; Remise commerciale accordée après facturation ; Erreur de destinataire ; Double facturation ; Annulation de la commande ; + saisie libre. _Source : `regles-avoir.ts#MOTIFS_AVOIR`_ — ✔ `domain/avoir.ts#MOTIFS_AVOIR` + « Autre motif… » (`FormulaireAvoir.tsx`)
- [x] **FAC-22** « Régler par un avoir » depuis une facture (avoir numéroté, même client, reste > 0). _Source : `app.js:6411, 6458`_ — ✔ « Régler par un avoir » sur la fiche et dans le dossier (`FormulaireImputation.tsx` : avoirs émis du même client avec crédit, montant = plus petit reste) ; écriture par la base (`imputer_avoir`, `tests/rls/facturation.essai.ts`)
- [x] **FAC-23** Lettrage dans le dossier client : cocher exactement 1 avoir + 1 facture numérotée → « 🔗 Lettrer ». _Source : `app.js:10975, 11000`_ — ✔ cocher 1 avoir + 1 facture émise → « Lettrer » (`lettrageDeLaSelection` en parité, `tests/parite/reglements.essai.ts` ; `reglements.essai.tsx` « Lettrer »)

### Écrans et fonctionnalités — règlements

- [x] **FAC-30** Vue **Par client** : total dû par client (sans avoirs), badge Retard, filtre d'état. _Source : `app.js:10593, 10767`_ — ✔ `VueParClient.tsx` / `domain/solde.ts#dossiersClients` : dû sans avoirs, badge Retard / dû / à jour, filtre d'état partagé, recherche par n° de facture (`solde.essai.ts`, `reglements.essai.tsx` « Par client »)
- [x] **FAC-31** Vue **Par facture** : filtres état (`non_reglee`, `partiellement_reglee`, `reglee`, `en_retard`), client, échéance du… au (sur `echeance || date`) ; tris retard, reste, échéance, client ; cartouche « reste à encaisser » ; avoirs exclus. _Source : `app.js:10838`_ — ✔ `VueParFacture.tsx` : états, client, échéance du… au (sur `echeance || date`), tris retard / reste / échéance / client, cartouche « reste à encaisser » de la liste affichée, avoirs exclus, critères dans l'adresse (`solde.essai.ts`, `reglements.essai.tsx`)
- [x] **FAC-32** Vue **Tous les règlements** : filtres du, au, client, mode (dont « Avoir »), chantier, rapproché (référence présente) ; critères dans l'URL `#factures/reglements?…` relus au démarrage ; total affiché. _Source : `app.js:10677, 10881`, `regles-filtres-reglements.ts`_ — ✔ `VueTousReglements.tsx` : du, au, client, mode (dont « Avoir »), chantier, rapproché ; critères dans l'adresse `/factures/reglements/tous?…` relus au chargement ; total de la liste affichée (parité `regles-filtres-reglements.ts`, `tests/parite/reglements.essai.ts`)
- [x] **FAC-33** **Dossier client** : cases sur les factures payables et les avoirs à imputer ; barre de sélection (total, « Règlement » groupé ou « Lettrer ») ; « + Règlement », « Régler par un avoir » ; historique modifier ✎ / supprimer ✕. _Source : `app.js:11034`_ — ✔ `PageDossierClient.tsx` : cases sur factures payables et avoirs à imputer, barre de sélection (total, « Règlement » ou « Lettrer »), « + Règlement », « Régler par un avoir », historique ✎ / ✕ (`CartePieceDossier.tsx`, `ListeReglements.tsx`, `reglements.essai.tsx`)
- [x] **FAC-34** Règlement **unitaire** : avoirs exclus du sélecteur, montant proposé = reste, mode tiré de la facture sinon défaut, aide « Total · déjà réglé · reste ». _Source : `app.js:11315, 11355`_ — ✔ `BlocReglements.tsx` (pas sur un avoir, reste proposé, mode de la facture), `tests/e2e/facturation.e2e.ts` (« déjà réglé 200,00 € · reste 394,00 € »)
- [x] **FAC-35** Règlement **groupé** : montant modifiable, répartition affichée avant validation, un règlement par facture servie (même date, mode, référence). _Source : `app.js:11168, 11203, 11227`_ — ✔ `PanneauReglementGroupe.tsx` : montant modifiable, répartition affichée avant validation (règle `imputer`), un règlement par facture servie (même date, mode, référence), écrit tout ou rien par `enregistrer_reglement_groupe` (`tests/rls/facturation.essai.ts`, `reglements.essai.tsx`) — D-FAC-02

### Règles métier

- [x] **FAC-40** Totaux par ligne et taux, remise globale, aucun arrondi stocké ; signe négatif des avoirs appliqué au calcul seulement (montants stockés positifs). RM-01 à RM-15. _Source : `regles-totaux.ts`, `regles-avoir.ts`_ — ✔ `tests/parite/totaux.essai.ts` (« avoirs : même signe »), `BlocTotaux` `signe`
- [x] **FAC-41** Numéro attribué **par la base à l'émission** (passage hors `brouillon`), jamais demandé à l'écran ; `FAC-`, `AV-`, `ACO-` + année **de la pièce** + 6 chiffres ; refus sans ligne de type `ligne` (BG-25) ; numéro immuable ; facture numérotée insupprimable. RM-42. _Source : schéma §5.1_ — ✔ `api/factures.ts#emettreFacture`, `tests/rls/numerotation.essai.ts`, `tests/e2e/facturation.e2e.ts` (`FAC-AAAA-NNNNNN`)
- [x] **FAC-42** `factures.statut` a pour **défaut `impayée`** : ne rien préciser = émettre. Toute création passe par `brouillon` → lignes → changement de statut. _Source : ts §3.4, `app.js:6134`_ — ✔ `api/factures.ts#creerFacture` écrit toujours `brouillon`, `tests/e2e/facturation.e2e.ts` (« Facture brouillon »)
- [x] **FAC-43** Verrous : `emise` (numéro présent, irréversible, art. L441-9) > `telechargee` (`verrouillee`, réversible) > aucun. En base : en-tête figé sauf liste blanche (statut, cycle PDP, `verrouillee`, conducteur, interlocuteur, chantier, `facturation_*`…), identité client complétable si vide, lignes figées même pour un admin. RM-51. _Source : `regles-verrouillage.ts`, schéma §7_ — ✔ verrou `emise` (lecture seule, base) ; cadenas `telechargee` posé à l'envoi (FAC-12) et levé avec confirmation (FAC-09) ; saisie figée sous cadenas (`FormulaireFacture.tsx`)
- [x] **FAC-44** Statut en base ∈ {brouillon, impayée, envoyée, payée} : réglée → `payée`, partielle ou non réglée → `impayée` ; écrit seulement s'il change. RM-17. _Source : `app.js:11389`, `regles-reglements.ts#statutEnBase`_ — ✔ `api/factures.ts#synchroniserStatut` (écrit s'il change), `tests/parite/facturation.essai.ts` (« statut, reste, refus »)
- [x] **FAC-45** Statut de règlement (affiché) : pièce historique `payée` → « Réglée (reprise) » ; avoir → statut d'imputation ; sinon `statutReglement`. RM-16. _Source : `app.js:6482`_ — ✔ `domain/etat.essai.ts` (« réglée par reprise », avoir)
- [x] **FAC-46** Retard ⇔ facture (pas avoir), reste > 0,01 et jours depuis `echeance || date` > 0. RM-18. _Source : `app.js:10741, 6513`_ — ✔ `domain/etat.essai.ts` (« retard »)
- [x] **FAC-47** Échéance = f(date, délai, mode) en UTC ; délai client > société > 30 j net ; libellé figé dans `conditions_reglement` (BT-20) ; dépassement de L441-10 **signalé, jamais bloqué**. RM-20 à RM-23. _Source : `regles-efacture.ts`_ — ✔ `FormulaireFacture.tsx` (échéance calculée, `conditions_reglement`, « Signalé, jamais bloqué »), `tests/parite/identifiants.essai.ts` (délais et échéances)
- [x] **FAC-48** Net à payer = max(0, TTC − acomptes − retenue), retenue sur le **TTC** ; `acomptesDeduits` et `retenueGarantiePourcentage` sont **reconduits**, plus saisis (`null` = pas de retenue ≠ 0). RM-14. _Source : `regles-totaux.ts#soldeAPayer`, `app.js:6128`_ — ✔ `tests/parite/totaux.essai.ts` (« net à payer »), acomptes et retenue reconduits (`modifierBrouillon` n'y touche pas)
- [x] **FAC-49** Toutes les clés de la facture sont **toujours envoyées** (une clé absente serait écrite NULL). _Source : `app.js:6067`, `CLAUDE.md`_ — ✔ `domain/facture.ts#enteteAEnregistrer` donne toutes les clés de l'en-tête ; statut toujours écrit
- [x] **FAC-50** Avoir : refusé si facture sans numéro (« modifiez-la directement »), si c'est déjà un avoir (« il faut refacturer »), si motif < 5 caractères ; copie de l'en-tête (sans devis/BC/intervention), identité émetteur **de la facture d'origine**, mêmes lignes positives, `facture_rectifiee_id`, `motif_rectification`. RM-43. _Source : `regles-avoir.ts#refusAvoir`, `queries/factures.ts#createAvoir`_ — ✔ `operations.ts#etablirAvoir` : refus en parité, en-tête sans devis / BC / intervention ni cycle PDP, émetteur d'origine, lignes positives, `facture_rectifiee_id`, motif (`tests/parite/facturation.essai.ts`)
- [x] **FAC-51** Imputation d'un avoir : contrôles et messages dans l'ordre de `refusImputationAvoir` ; écrit **deux règlements** liés (facture : mode `avoir`, réf. = n° avoir ; avoir : mode `imputation`, réf. = n° facture), même montant, même date. RM-19. _Source : `queries/factures.ts:423`, `regles-avoir.ts`_ — ✔ `imputer_avoir` (proposition 20260926041000) : contrôles et messages de `refusImputationAvoir` dans l'ordre, deux règlements liés (facture : `avoir`, réf. n° avoir ; avoir : `imputation`, réf. n° facture), même montant, même date (`tests/rls/facturation.essai.ts`)
- [x] **FAC-52** Règlement : refus si montant ≤ 0, facture soldée, montant > reste + 0,005 ; en modification, le règlement modifié est exclu. RM-17. _Source : `regles-reglements.ts#refusReglement`_ — ✔ `tests/parite/facturation.essai.ts` (« statut, reste, refus »), `tests/e2e/facturation.e2e.ts` (« dépasse le reste à payer »)
- [x] **FAC-53** Virement groupé : imputé de la plus ancienne à la plus récente (date, puis numéro) ; jamais plus que le reste ; pas de part nulle ; trop-perçu **refusé**. RM-17. _Source : `regles-reglements.ts#imputer/refusImputation`_ — ✔ règle portée (`imputer` / `refusImputation`, parité) et imputée par la base (`enregistrer_reglement_groupe`) : plus ancienne d'abord, jamais au-delà du reste, pas de part nulle, trop-perçu refusé avec le message de l'ancien (`tests/rls/facturation.essai.ts`)
- [x] **FAC-54** Modes de règlement : virement (défaut), chèque, prélèvement, carte, espèces ; libellés historiques (« Virement », « CB ») lus sans reprise. _Source : `regles-efacture.ts#MODES_REGLEMENT`, `app-2.md §4-19`_ — ✔ `clients/domain/delais.ts#MODES_REGLEMENT`, libellé historique affiché tel quel (`BlocReglements.tsx`)
- [-] **FAC-55** Facture ST unitaire `FST-<numeroBC sans espaces>` (ou `FST-<6 car. id>`) ; groupée `FST-MAAAA-MM` puis `-2`, `-3`… ; naissent `impayée`. RM-44. _Source : `app.js:5717-5800`_ — Écarté : D-FAC-09 — numéros FST hors série légale ; une facture de sous-traitant est une facture d'achat

### Situations de travaux (avancement DPGF)

- [x] **FAC-60** « Facturer la sélection » (lignes DPGF non soldées) : modale avec % par ligne, boutons 25/50/75/100 % au-dessus du déjà facturé, total rafraîchi. _Source : `app.js:13261, 13299`_ — ✔ `PageSituation.tsx` (paliers au-dessus du déjà facturé, total), `tests/e2e/facturation.e2e.ts` (« situation de travaux »)
- [x] **FAC-61** Nouveau % = max(déjà, min(100, saisie)) ; à facturer = montant ligne × (nouveau − déjà)/100 ; refus « Aucun avancement supplémentaire » si rien. RM-61. _Source : `app.js:13305, 13326`_ — ✔ `tests/parite/facturation.essai.ts` (« situation de travaux »), `refusSituation`
- [x] **FAC-62** Chaque ligne de situation : `{ligne, "<désignation> (avancement X% → Y%)", qte 1, PU = à facturer, tva = tvaDefaut()}` ; facture **brouillon** non numérotée, remise 0, échéance vide, `chantierId`, notes « Situation de travaux — <nom> ». RM-62. _Source : `app.js:13315`_ — ✔ ligne « (avancement X% → Y%) », qté 1, TVA par défaut, brouillon, remise 0, `chantier_id` ; échéance calculée (mention obligatoire) et pas de note faute de colonne — D-FAC-13
- [x] **FAC-63** Le cumul vit dans `chantier_dpgf_lignes.avancement_cumule` ; pas de rappel des situations précédentes ni de retenue sur la facture. _Source : `app-2.md §2.5`_ — ✔ `operations.ts#facturerSituation`, `tests/e2e/facturation.e2e.ts` (« le DPGF le cumule »)

### Données

- [x] **FAC-70** `factures` (86 colonnes : numéro, liens, client figé, adresse client vs `adresse_locataire`, émetteur figé, dates et conditions, `remise_pourcentage`, `acomptes_deduits`, `retenue_garantie_pourcentage`, `statut`, `type_document` ∈ facture|avoir|acompte|note_frais, `cadre_facturation`, `statut_cycle`, `pdp_*`, `verrouillee`, `ref_bon_commande_client`, `ref_marche`, `motif_rectification`, `facture_rectifiee_id`). _Source : schéma §2.2_ — ✔ `schemaEnteteFacture` : numéro, liens, client figé (`client_siret`…), adresses, émetteur figé, dates et conditions, remise, acomptes, retenue, statut, type, cadre, `statut_cycle`, `pdp_*`, `verrouillee`, références, rectification ; vestiges `total_*` volontairement non lus (FAC-72)
- [x] **FAC-71** `facture_lignes` (comme `devis_lignes` + `tva_motif_exoneration`) ; `tva` écrite **0 si absente, jamais NULL** ; `unite_code` et `tva_categorie` non écrits (dérivés). _Source : ts §3.3_ — ✔ lignes comme les devis, TVA toujours numérique ; `tva_motif_exoneration`, `unite_code`, `tva_categorie` jamais écrits — l'ancien ne les écrivait pas non plus, et l'écriture des lignes ne les envoie pas, donc ne les efface pas
- [x] **FAC-72** Totaux lus dans `v_facture_totaux` (les colonnes `total_*`, `net_a_payer`, `ventilation_tva` sont des **vestiges** : 351/410 factures à 0). _Source : schéma §2.2, §4.1_ — ✔ le TTC des listes, fiches et dossiers est celui de la base, lu par `v_facture_solde` qui le tient de `v_facture_totaux` (`api/soldes.ts`) ; les vestiges `total_*` ne sont jamais lus
- [x] **FAC-73** `v_facture_solde` (ttc, paye, reste ≥ 0, etat Impayée/Payée/Partiel à 0,004/0,01, jours_retard). _Source : schéma §4.1_ — ✔ `api/soldes.ts#soldesDesFactures` lit `v_facture_solde` corrigée (proposition 20260926040000) : ttc, payé, reste, état, retard, dû, crédit (`tests/rls/facturation.essai.ts`) — D-FAC-01
- [x] **FAC-74** `reglements(facture_id, societe_id, date, montant numeric(14,2) > 0, mode texte, reference)`. _Source : schéma §2.2_ — ✔ `api/factures.ts#ajouterReglement`, `tests/e2e/facturation.e2e.ts` (règlement partiel)

### Cas limites et corrections cachées

- [x] **FAC-80** `emettreFacture` n'était appelée par aucun bouton : 46 brouillons sans numéro. _Source : `app.js:5922`_ — ✔ bouton « Émettre la facture », `tests/e2e/facturation.e2e.ts`
- [x] **FAC-81** Facture sans statut partait « impayée » → numérotée à la 1re sauvegarde : défaut `brouillon` côté écran. _Source : `app.js:6134`_ — ✔ `creerFacture` : statut `brouillon` toujours écrit
- [x] **FAC-82** « Enregistrer » actif sur facture émise → 23001 silencieux ; et chaque impression d'une facture émise donnait 23001 (cadenas). _Source : `app.js:6013, 11898`_ — ✔ aucun « Enregistrer » sur une émise, l'impression ne pose pas de cadenas ; `tests/e2e/facturation.e2e.ts` (lignes `readonly`)
- [x] **FAC-83** Pièces historiques (`legacy_id`) au statut `payée` : 362 792 € de créances fantômes → « Réglée (reprise) », aucun règlement fabriqué. _Source : `app.js:6482`_ — ✔ `domain/etat.essai.ts` (« pièce historique payée »)
- [x] **FAC-84** Un avoir mesuré comme une facture affichait « RÉGLÉE, reste 0 » (`statutReglement(-682, [])` = réglée) : statut d'imputation séparé. _Source : `app.js:6482`_ — ✔ `domain/etat.essai.ts` (« un avoir n'est jamais impayé »), `statutImputation`
- [x] **FAC-85** Avoir compté dans les impayés du tableau de bord (il naît `impayée` en base) ; facture à moitié réglée absente du dû. _Source : `app.js:1588, 2043`_ — ✔ la vue sépare `du` et `credit` : un avoir n'est jamais dû, une facture à moitié réglée l'est (`tests/rls/facturation.essai.ts` « un avoir n'est jamais une dette », `solde.essai.ts`) ; le bandeau « à relancer » de la liste en dépend
- [x] **FAC-86** Règlements : le partiel était rangé « envoyée » ; encaissement posé sur un avoir ; reste d'avoir ajouté aux dus ; formulaire unitaire ouvert par rien ; règlement groupé soldait tout. _Source : `app-2.md §4-18`_ — ✔ partiel → `impayée` (`statutEnBase`), aucun encaissement sur un avoir, formulaire toujours visible (`BlocReglements.tsx`), parité règlements
- [x] **FAC-87** Filtrer dans l'onglet Avoirs vidait l'écran ; 5 fonctions de filtre en double écrasaient les bonnes. _Source : `app.js:5612, 5859`_ — ✔ onglet Avoirs filtrable par recherche (`PageFactures.tsx`), un seul filtre
- [x] **FAC-88** Adresse de l'émetteur sans CP/ville, IBAN courant sur une facture ancienne : colonnes `emetteur_*`. _Source : `app.js:4032`_ — ✔ `PageApercuFacture.tsx` (CP / ville / IBAN figés), `tests/parite/facturation.essai.ts` (« identité de l'émetteur »)
- [-] **FAC-89** `legacy_id` des factures unique **globalement** (préfixe `compta:` pour l'import). _Source : ts §3.4_ — Écarté : D-FAC-12 — `web/` n'écrit jamais `legacy_id` ; l'unicité (préfixe `compta:`) relève de l'import historique (section 17)

### Défauts connus de l'ancienne app

- [-] **FAC-90** Numéros `FST-…` calculés **côté client**, hors compteur, non transactionnels ; `FST-M` compte **toutes** les factures en mémoire (toutes sociétés, tous sous-traitants). _Source : `app.js:5725-5727`_ — Écarté : D-FAC-09 — pas de numéros FST dans `web/`
- [-] **FAC-91** Factures ST créées directement `impayée` sans passer par l'émission ; « Marquer payée » écrit `payée` **sans règlement**. _Source : `app.js:5786, 5832`_ — Écarté : D-FAC-09 — ni facture ST émise d'emblée, ni « Marquer payée » sans règlement
- [x] **FAC-92** `calculerSoldeFacture` / `ajouterReglementEtMajStatut` recalculent le solde **côté client** (remise appliquée au TTC, `toFixed(2)`) au lieu de lire la base ; `efacture.preparerEmission` s'en sert pour BT-113. _Source : `operations/workflows.ts`, ts §0-3_ — ✔ plus aucun solde recalculé à l'écran : liste, fiche, dossiers, espace client lisent `v_facture_solde` ; le statut stocké est recalé par la base (déclencheur) — D-FAC-01, D-FAC-02
- [x] **FAC-93** `v_facture_solde` ignore acomptes, retenue, escompte et le **signe des avoirs**. _Source : schéma §4.1, §10.3-7_ — ✔ corrigé en base : signe des avoirs, acomptes, retenue (exigible ≠ due), reprise, retard à l'heure de Paris (proposition 20260926040000, `tests/rls/facturation.essai.ts`) ; escompte non géré par l'une ou l'autre app — D-FAC-01
- [x] **FAC-94** Deux définitions de l'avoir : `estAvoir` (`includes("avoir")`) vs `estAvoirDocument` (`trim() === "avoir"`). _Source : `regles-avoir.ts`, `regles-verrouillage.ts`_ — ✔ une seule définition (`estAvoir`), le verrou compris (`duplication.essai.ts` « une seule définition de l'avoir ») — D-FAC-06
- [x] **FAC-95** Deux arrondis au centime : `arrondiCentime` (1,005 → 1,01) vs `centimes` (1,005 → 1). RM-10, décidé : D-006. _Source : `regles-reglements.ts`, `regles-avoir.ts`_ — ✔ un seul arrondi (`lib/money.ts#arrondiCentimes`), `tests/parite/facturation.essai.ts` (« écart assumé sur le demi-centime ») — D-006
- [-] **FAC-96** Vente de véhicule : facture **émise d'emblée** (`impayée`), client en texte libre sans fiche, TVA 20 ou 0 seulement. _Source : `app.js:15344`_ — Écarté ici : D-FAC-11 — relève du module véhicules (section 13), règle de facturation fixée ; repris par VEH-04 (D-VEH-06)
- [x] **FAC-97** Situation : avancement du chantier écrit **avant** la facture, sans contrôle ; PU non arrondi (4074.0710999999997) ; saisie 0 = revient au déjà facturé ; ni retenue ni rappel des situations ; `chantier_avancement_factures` jamais écrite. _Source : `app.js:13315`, schéma §2.4_ — ✔ D-027 : facture, trace puis cumul, gardé et défait (`operations.ts#facturerSituation`), `tests/parite/facturation.essai.ts`, e2e situation
- [x] **FAC-98** `note_frais` numérotée `NOT-AAAA-…` faute de préfixe. _Source : schéma §10.3-5_ — ✔ proposition `20260926043000` : `note_frais` → NDF (et `bon_commande` → BC) (`tests/rls/facturation.essai.ts` « NDF- ») — D-FAC-07
- [-] **FAC-99** 7 factures réelles FAC-2026-0007 à 0013 (ALPES ISERE HABITAT) encore dans `kv_store` fermée : trou dans la série légale. _Source : schéma §2.8, §10.3-13_ — Écarté du code : D-FAC-12 — reprise en production par un humain, avec `legacy_id` « compta: »
- [x] **FAC-100** `refBonCommandeClient` est figée dès la numérotation, même vide (d'où l'avertissement « en attente de BC »). _Source : `app-2.md §4-9`_ — ✔ reproduit (la base fige l'en-tête), et dit avant d'émettre : « Émettre sans réf. de bon de commande client ? Elle sera figée vide » (`FormulaireFacture.tsx`)

---

## 9. ocr — lecture automatique des bons de commande

### Écrans et fonctionnalités

- [x] **OCR-01** « Importer un bon » : PDF / image / HEIC ; ouvre un formulaire vierge puis lance la lecture. _Source : `app.js:18275, 18378`_ — Preuve : « Importer un bon (PDF, photo) » dans la liste des bons ; PDF, JPEG, PNG, WebP, HEIC (converti par le navigateur s'il le décode) ; la lecture précède le formulaire prérempli (D-BC-10) — `src/modules/ocr/components/lecture.essai.tsx`
- [x] **OCR-02** Écran de lecture : étapes (Préparation du document → Envoi → Lecture par le modèle), chronomètre à la seconde sans re-rendu, durée annoncée, « Annuler la lecture ». _Source : `app.js:18341`, `regles-ocr.ts`_ — Preuve : étapes affichées (Préparation → Envoi → Lecture par le modèle), chronomètre à la seconde (`formaterDuree`), durée annoncée, « Annuler la lecture » (`PageLectureBon.tsx`) ; `src/modules/ocr/components/lecture.essai.tsx` (« montre les étapes »), `tests/parite/ocr.essai.ts` (« étapes et délais »)
- [x] **OCR-03** Trois issues distinctes, avec toast et écran persistant : annulé, délai dépassé, échec ; « ↻ Réessayer » ou « Saisir à la main ». _Source : `app-3.md §4`_ — Preuve : annulé / délai / échec ont chacun leur écran (`etatAnnule`, `etatDelaiDepasse`, `etatEchec`), avec « ↻ Réessayer » et « Saisir à la main » ; alerte persistante au lieu d'un toast (D-BC-10) — `src/modules/ocr/components/lecture.essai.tsx` (« trois issues distinctes »)
- [x] **OCR-04** Résultat : formulaire du bon **prérempli**, avertissements affichés ; **rien n'est enregistré automatiquement** ; pièce jointe retenue pour le bon. _Source : `integrations/ocr.ts#versSaisieBonCommande`_ — Preuve : formulaire prérempli, avertissements affichés, rien d'enregistré ; le document lu (préparé) part dans l'état de navigation et devient la pièce jointe du bon — `src/modules/ocr/components/lecture.essai.tsx`, `src/modules/commandes/components/commandes.essai.tsx` (« le préremplissage de la lecture »)
- [x] **OCR-05** Rapprochement du client lu avec les fiches : exact → inclusion unique → score de mots ≥ 0,8 unique ; sinon 8 suggestions. _Source : `integrations/ocr.ts#rapprocherClient`_ — ✔ `domain/rapprochement.ts`, `tests/parite/ocr.essai.ts` (« rapprochement de client »)

### Règles métier

- [x] **OCR-10** Préparation : PDF, JPEG, PNG, WebP acceptés ; image hors format ou > 3 Mo → JPEG qualité 0,85, côté max 2200 px ; PDF > 14 Mo refusé. _Source : `integrations/ocr.ts#preparer`_ — Preuve : `ocr/api/preparer.ts` : image hors format ou > 3 Mo → JPEG 0,85, côté max 2 200 px ; PDF > 14 000 000 octets refusé ; `tests/parite/ocr.essai.ts` (« préparation », `preparer` extrait), `ocr/domain/contrat.essai.ts`
- [x] **OCR-11** Délais : bascule envoi → analyse à 2 s ; « plus long que d'habitude » à 45 s ; durée habituelle 30 s ; délai client 120 s (`TimeoutError`). _Source : `regles-ocr.ts`_ — Preuve : 120 s, 45 s, 30 s et bascule envoi → analyse à 2 s (`api/extraire.ts`, `DELAI_BASCULE_ANALYSE_MS`) ; `tests/parite/ocr.essai.ts` (« mêmes seuils »)
- [x] **OCR-12** Correspondance : `sansBC = !numeroBC` ; `dateReception = dateBC ?? aujourd'hui` ; `logementStatut` ∈ {occupé, vacant, commune} sinon ignoré ; `montant = montantTotalHT` ; lignes `id = ocr-<ts>-<i>`. _Source : `integrations/ocr.ts`_ — Preuve : `ocr/domain/prefill.ts#versPreRemplissage` : « Sans BC » sans numéro, date du jour par défaut, statut de logement, montant, interlocuteur, notes, logement, facturation, TVA des lignes (relecture 3, M2) ; `tests/parite/ocr.essai.ts` (« parité du préremplissage », `versSaisieBonCommande` extrait)
- [x] **OCR-13** Avertissements complétés par `essentielsDeLecture` : n° de BC, adresse du chantier, ≥ 1 ligne de travaux. _Source : `regles-bc.ts#essentielsDeLecture`_ — ✔ `api/extraire.ts#essentielsManquants`, `tests/parite/ocr.essai.ts`, `domain/contrat.essai.ts` (« signale numéro, adresse et lignes manquants »)
- [x] **OCR-14** Normalisation des noms de client : sans accents, majuscules, retrait SA/SAS/SASU/SARL/EURL/SCI/OPH/HLM/SA HLM/OFFICE PUBLIC DE L HABITAT ; mots > 2 lettres hors DE/DU/DES/LA/LE/LES/ET/L/D. _Source : `integrations/ocr.ts`_ — ✔ `domain/rapprochement.ts#normaliser`, `tests/parite/ocr.essai.ts`

### Données

- [x] **OCR-20** Edge Function `extraire-bc` : `{fichierBase64, mimeType}` → `{extraction}` ; Mistral OCR (`mistral-ocr-latest`) puis extraction JSON stricte (`mistral-medium-latest`, `temperature 0`) ; budget 50 s + 45 s, 110 s total, une reprise sur 429/5xx ; erreurs 400/405/413/500/502/503/504. Ne touche pas la base. _Source : schéma §8.1_ — ✔ `api/extraire.ts` (`functions.invoke("extraire-bc")`, délai 120 s), `lecture.essai.tsx`
- [x] **OCR-21** Contrat `ExtractionBC` (client, numeroBC, dateBC, referenceChantier, natureTravaux, dateFinTravaux, interlocuteur, adresse/CP/ville du lieu, facturation*, numeroLogement, logementStatut, occupant, etage, notes, montantTotalHT, lignes[type, designation, qte, unite, prixUnitaire, tva], avertissements). _Source : `supabase/functions/_shared/contrat-bc.ts`_ — ✔ `domain/contrat.ts#schemaExtraction`, `domain/contrat.essai.ts`

### Cas limites et corrections cachées

- [x] **OCR-30** Le 14/09 : bouton relancé plusieurs fois, promesse jamais résolue → issues nommées (`AbortError`, `TimeoutError`) et bouton neutralisé. _Source : `app-3.md §4`_ — ✔ sélecteur neutralisé pendant la lecture, `AbortSignal.timeout` + annulation (`useLectureBon`), `lecture.essai.tsx`
- [x] **OCR-31** Écart au contrat détecté (`ecartsDeForme`) → avertissement « Lecture partiellement incertaine ». _Source : schéma §8.1_ — Preuve : `ocr/domain/contrat.ts#analyserReponse` : un champ hors contrat est vidé et nommé dans « Lecture partiellement incertaine : … », le reste est gardé — `ocr/domain/contrat.essai.ts` (« lecture partiellement incertaine ») — D-BC-12 (remplace D-049)

### Défauts connus de l'ancienne app

- [-] **OCR-40** `extraire-bc` ne vérifie ni l'utilisateur ni la société : un JWT `anon` suffit à consommer le quota Mistral. _Source : schéma §8.1, §10.3-11_ — Écarté : l'Edge Function vit hors de `web/` (`supabase/functions/extraire-bc`) : la vérification de l'utilisateur et de la société y est à faire avant la mise en service ; côté `web/`, la page exige le droit de créer un bon ET la fonctionnalité d'abonnement (relecture 3, M6) — D-BC-15
- [x] **OCR-41** L'OCR ne remplit que `numero_bc` (référence client), jamais `numero_interne` — conforme, mais à expliciter à l'écran. _Source : schéma §10.3-15_ — ✔ `ResultatLecture.tsx` (« N° de bon (client) »)

---

## 10. espace-client — accès client en lecture seule

État réel : **inexistant en base**. Aucun rôle `client` dans `role_membre`, aucune politique RLS
pour un tiers, aucun lien de partage. L'ancien `app.js` contient un **portail mort** (il teste
`state.currentRole === 'client'`, valeur jamais posée par `roleEffectif`).

### Écrans et fonctionnalités (portail mort, à décider)

- [x] **ESP-01** Portail « bons de commande du client » : bons de `currentClientNom` (+ interlocuteur), 4 tuiles couleur, liste triée. _Source : `app.js:6562, 6574`_ — ✔ `/espace-client/bons` (`PageBonsClient.tsx`) : bons du client (et de son interlocuteur), 4 tuiles qui comptent et filtrent, liste triée, pièce, tentatives de contact, date de réalisation (`bons-client.essai.tsx`, `tests/rls/espace-client-bons.essai.ts`)
- [x] **ESP-02** Couleur d'un bon : vert (travaux faits ou `dateInterventionTerminee`) > jaune (pièce à commander) > orange (date planifiée) > rouge ; tri rouge, jaune, orange, vert, puis `numeroBC`. _Source : `app.js:6529`_ — ✔ `espace-client/domain/bons.ts#statutClientBon` / `trierBonsClient` en parité avec `statutClientBC` (`tests/parite/espace-client.essai.ts`)
- [x] **ESP-03** Recherche restreinte à 13 champs (`CHAMPS_CHERCHES_PORTAIL`) pour ne pas révéler les notes internes. _Source : `app.js:6549`_ — ✔ `CHAMPS_CHERCHES_PORTAIL` : les 13 champs de l'ancien, aucune note interne (`tests/parite/espace-client.essai.ts`, `bons-client.essai.tsx`)
- [-] **ESP-04** Réglages du rôle client : choix de l'organisme et de l'interlocuteur dans une **liste libre, sans contrôle**. _Source : `app.js:12213`_ — Écarté : D-008 : accès par la table `acces_clients`, jamais par une liste libre

### Règles métier

- [x] **ESP-10** Un client ne voit **que ses** bons (et, par interlocuteur, que les siens), sans aucun montant ni note interne — à garantir **en base** (RLS ou vue dédiée), pas par l'écran. _Source : `app.js:12213` (intention), schéma §6.5_ — ✔ garanti EN BASE : chantiers, devis envoyés, factures émises (D-029), bons par vue réduite sans montant ni note, restriction par interlocuteur, règlements de ses seules factures (proposition `20260926042000`, `tests/rls/espace-client-bons.essai.ts`) — D-FAC-10
- [x] **ESP-11** Il n'y a pas de rôle client : créer un rôle, une table de rattachement compte ↔ client (et interlocuteur), des politiques, ou écarter le module. Décidé : table `acces_clients` + `mes_clients()`, lecture seule, pas de rôle de membre (D-008). _Source : schéma §1.4, ts §2.2_ — ✔ D-008 : proposition `20260925030000`, `tests/rls/espace-client.essai.ts`, `tests/e2e/parcours.e2e.ts` (« espace client »)

### Données

- [x] **ESP-20** Aucune table, vue ni politique aujourd'hui. Le seul tiers avec un compte est le **sous-traitant** (`sous_traitants.contact_profile_id`). _Source : schéma §6.5_ — ✔ `acces_clients`, `mes_clients()`, `v_mes_acces_clients`, `v_espace_client_chantiers` proposés (`20260925030000`, D-018)

### Défauts connus de l'ancienne app

- [x] **ESP-30** Le portail n'est atteignable par aucun chemin, et s'il l'était, le cloisonnement ne tiendrait que par un sélecteur libre. Ne pas reproduire tel quel (D-008). _Source : `app-1.md §4.2-7`, `app-2.md §4`_ — ✔ portail refait sur la RLS (D-008, D-029), `tests/rls/espace-client.essai.ts` (« lecture seule »), e2e « espace client »

---

# Hors périmètre de cette nuit

Modules à reprendre plus tard. Les lignes restent ici pour la parité ; les règles qu'ils
partagent avec les modules ci-dessus (tâches, prix masqués, numérotation) sont déjà citées.

## 11. planning, interventions, rapports

### Écrans et fonctionnalités

- [x] **PLN-01** Vues Planning technicien / sous-traitant / En attente technicien / En attente sous-traitant ; technicien : vue technicien imposée, sans « Non planifiés » ni glisser-déposer ; sous-traitant : « Mon planning », montants masqués sauf « Votre montant : X HT ». _Source : `app.js:8795-8830`_ — `planning/components/PagePlanning.tsx` (`ongletsDu`) — technicien : « Ma journée » + vue technicien, sans colonne ni glisser ; sous-traitant : « Ma journée » + « Mon planning » (ses seules cartes), « Votre montant » par `mes_montants_sous_traitant` (proposition 20260926050000). Preuves : `planning.essai.tsx` (technicien, sous-traitant, aucun autre montant affiché), `tests/rls/planning.essai.ts`.
- [x] **PLN-02** Calendrier 6 semaines, heures 8-16, pause à 12, week-ends et jours fériés grisés ; filtres conducteur, métier, équipe/sous-traitant, logement (dont « problème »), recherche multi-mots, client, interlocuteur ; la recherche saute à la semaine du premier résultat. _Source : `app.js:8631, 8850, 8868`_ — `Calendrier.tsx`, `domain/calendrier.ts`, `domain/filtres.ts`, `BarreOutils.tsx`, `ColonneNonPlanifies.tsx` ; saut de semaine `semaineDuResultat`. Preuves : `domain/planning.essai.ts` (filtres, recherche sans accents, saut), parité `tests/parite/planning.essai.ts` (grille, fériés).
- [x] **PLN-03** Un élément par (bon × métier) si plusieurs métiers (`bcId::METIER`, planification par `scheduleParMetier`, montant par métier). _Source : `app.js:8718, 8778`_ — `construireCartes` (`domain/cartes.ts`) : id `bcId::METIER`, `schedule_par_metier` lu et réécrit sans perdre ses autres clés, montant par métier. Preuve : `domain/planning.essai.ts` (« une carte par métier… »). Écart D-PLN-03.
- [x] **PLN-04** Poser au planning (glisser ou rapide) : équipe **obligatoire** (modale), durée 1 h par défaut ; échec d'écriture → toast et rechargement. _Source : `app.js:10241-10379`_ — `planPoser` + `ModaleAffectation` (équipe obligatoire, sauf filtre ou affectation déjà connue — D-PLN-16), glisser sur une case horaire ou date au clavier, 1 h par défaut ; tout échec remonte en message et recharge (`useAppliquerPlan`, `onSettled`). Preuves : `planning.essai.tsx` (clavier, glisser), `tests/rls/planning.essai.ts`.
- [x] **PLN-05** Déplanifier (refus si intervention faite, retire les dates supplémentaires sauf journées pointées), avancer d'un jour, régler heure / durée (1-8 h) / date de fin / équipe, étirer par la poignée (Échap annule). _Source : `app.js:10313-10550`_ — `planDeplanifier` (refus si faite, journées supplémentaires supprimées sauf pointées), `planAvancer` (←), `planCreneau` (heure, 1-8 h), `planDateFin`, `planAffectation`, poignée `Poignee.tsx` + `planEtirer` (Échap annule ; midi compté une fois — D-PLN-17). Preuves : `domain/planning.essai.ts`, `planning.essai.tsx`.
- [x] **PLN-06** Dates supplémentaires : ajout (refus doublon / date d'origine, tri), retrait par suppression de la tâche (refus si pointée). _Source : `app.js:10141-10206`_ — `planAjouterDate` (refus doublon / date d'origine ; journées dérivées des tâches donc triées), `planRetirerDate` (refus si pointée, commentée, pièce, croquis ou travail supplémentaire), `planCreneauJournee`. Preuves : `domain/planning.essai.ts`, `tests/rls/planning.essai.ts`. Par métier sur un bon multi-métiers (D-PLN-15).
- [x] **PLN-07** Contacts : tentatives (appel/SMS, date, heure), rappel programmé (≥ aujourd'hui), annulation ; lien `tel:` du locataire. _Source : `app.js:9247-9400`_ — `ZoneContacts.tsx` + `domain/contacts.ts` (tentatives appel/SMS horodatées à Paris, rappel ≥ aujourd'hui, annulation) ; lien `tel:` par `telephones_locataires` (proposition 20260926053000 — la vue ne sert pas le numéro). Gestes sous `bons_commande/modifier` (D-PLN-13). Preuves : `domain/planning.essai.ts`, `planning.essai.tsx` (lien `tel:`), `tests/rls/planning.essai.ts` (téléphone).
- [x] **PLN-08** Fiche technicien d'une carte : commentaire, pièce à commander, photos, croquis, contacts ; boutons Enregistrer mes constats / Travaux terminés / Valider-Refuser selon `actionsTache` ; motif de lecture seule. _Source : `app.js:9685-10134`_ — `FicheIntervention.tsx` + `BandeauTache.tsx` : travaux du bon (via `metierDeLaLigne`), commentaire, pièce, croquis, photos (persistées — D-PLN-06), contacts, travaux supplémentaires ; boutons selon `actionsTache` + `appartenanceDe` ; motif de lecture seule mot pour mot. RPC `tache_sauvegarder_terrain` / `tache_marquer_realisee` / `tache_valider`. Preuves : `planning.essai.tsx`, `tests/rls/planning.essai.ts` (circuit).
- [x] **PLN-09** Fiche sous-traitant : date faite, commentaire, photos, travaux supplémentaires. _Source : `app.js:9564-9639`_ — Même fiche pour le sous-traitant : « Travaux terminés » (au lieu d'une case « date faite » que la base refusait — D-PLN-05), commentaire, photos, travaux supplémentaires, avec les propositions 20260926050000/051000. Preuves : `planning.essai.tsx`, `tests/rls/planning.essai.ts` (« [proposition] le sous-traitant pointe… »).
- [x] **PLN-10** Annotation de photo : flèche, carré, cercle, texte, zone hachurée ; vert si préconisation, rouge sinon ; JPEG 0,85. _Source : `app.js:8945-9245`_ — `interventions/components/AnnotationPhoto.tsx` + `domain/annotation.ts` : flèche, carré, cercle, texte, zone hachurée ; vert si préconisation, rouge sinon ; JPEG 0,85. Preuve : `interventions/domain/rapport.essai.ts` (couleurs, zone, pointe) — le tracé sur toile n'est pas exécutable sous jsdom.
- [x] **PLN-11** Impression du planning en paysage. _Source : `app.js:5050`_ — HTML de `printPlanning` porté (`planning/domain/impression.ts`), zone `#printArea` paysage de l'ancien (`hooks/useImpressionPlanning.ts`, D-PDF-07). Preuves : `planning.essai.tsx` (« impression… »), parité `tests/parite/impression-zones.essai.ts`.
- [x] **PLN-20** Rapports d'intervention : liste (filtres sous-traitant, recherche, conducteur, logement), assistant 4 étapes (infos, contrôles par métier, photos ≤ 3 + signatures, rapport), lien/délien d'un BC (un rapport par BC), transformer en devis ou facture, PDF, e-mail. _Source : `app.js:11405-11985, 2729-2790`_ — `interventions/` : liste (recherche multi-mots, conducteur, logement, émetteur), assistant 4 étapes (`AssistantRapport.tsx`), lien / délien (un rapport par bon, index unique), transformer en devis ou facture (brouillons, lignes des préconisations ; un rapport lié à un bon renvoie à la facturation du bon), aperçu et PDF de l'ancien (`renderPrintIntervention` porté dans `domain/gabarit-rapport.ts`, html2pdf, D-PDF-01/06), courriel (`mailto` + copie). Preuves : `rapports.essai.tsx`, `tests/rls/interventions.essai.ts`, parité `tests/parite/rapports.essai.ts`. IA : voir PLN-51.
- [x] **PLN-21** Numéro `INT-AAAA-NNNNNN` à la création ; signature nulle si logement vacant ou partie commune ; statut « en cours » par défaut. _Source : `app.js:11951`_ — Numéro `INT-AAAA-NNNNNN` posé par la base (proposition 20260926052000 ; repli `prochain_numero`), signature du client écartée si vacant ou partie commune (`signatureClientDemandee`), statut « en cours » par défaut. Preuves : `tests/rls/interventions.essai.ts`, `rapport.essai.ts`.

### Règles métier

- [x] **PLN-30** Matérialisation d'une tâche par (bon, date, métier, équipe) ; tâches orphelines rattachées à la première carte (BC-2026-0866). _Source : `app.js:9730`_ — `planPoser` / `planAjouterDate` / `planMaterialiser` : une tâche par (bon, date, métier) avec son équipe ; tâches orphelines rattachées à la première carte (`tachesHorsMetier`). Preuve : `domain/planning.essai.ts`. La tâche naît à la planification (D-PLN-02).
- [x] **PLN-31** Appartenance : compte → salarié → `technicien_id` (équipe) ; tâche sans équipe : arbitrage au conducteur. _Source : `app.js:9833-9848`, SQL `est_de_l_equipe`_ — `appartenanceDe` (compte → salarié → équipe par `v_salaries_annuaire`, et sous-traitant par `mon_sous_traitant`) ; sans équipe : « son arbitrage revient au conducteur ». Preuves : `planning.essai.tsx` (autre équipe), `tests/rls/planning.essai.ts`.
- [x] **PLN-32** Créneau : heure 08:00 et 1 h par défaut, 1 à 8 h, modifiable seulement `planifiee|refusee` ; retour au planning refusé si une tâche est validée (« ouvrez un SAV »). _Source : `regles-taches.ts`_ — Port de `regles-taches.ts` (`domain/taches.ts`) : 08:00 et 1 h par défaut, 1 à 8 h, modifiable si `planifiee|refusee`, `refusRetourAuPlanning`. Parité exhaustive `tests/parite/planning.essai.ts`.
- [x] **PLN-33** Carte « faite » (`bcInterventionFaite`) : non déplaçable si datée ; déplaçable si faite sans date. _Source : `app.js:7069, 10217`_ — `planPoser` refuse de déplacer une carte faite et datée, pose une carte faite sans date ; `CartePosee` n'est pas déplaçable si faite. Preuve : `domain/planning.essai.ts`, parité `bcInterventionFaite`.
- [x] **PLN-34** Jours fériés calculés (Pâques) ; `calculerSpanRows` avec pause. _Source : `app-2.md §5.1`_ — `datePaques`, `joursFeries`, `calculerSpanRows`, `placementDuJour`. Parité 1990-2100 et toutes cases × durées (`tests/parite/planning.essai.ts`).

### Données

- [x] **PLN-40** `planning_taches` (date nullable = à replanifier, heure_debut/fin, équipe, sous-traitant, bon, chantier, dpgf_ligne, métier, statut, réalisation/validation, refus, pièce, croquis) ; `interventions` (+ `intervention_controles`, `intervention_photos`) ; `metier_type` à 3 valeurs. _Source : schéma §2.3-2.4_ — Lus et écrits : `planning_taches` (date nullable = à replanifier, heures, équipe, sous-traitant, bon, métier, statut par RPC, réalisation / validation, refus, pièce, croquis), `interventions` + `intervention_controles` + `intervention_photos`, `metier_type` à 3 valeurs (`METIERS_RAPPORT`). Schémas Zod `schemaTachePlanning`, `schemaRapport`.

### Défauts connus

- [x] **PLN-50** `dropUnsched` déplanifie sans contrôler l'intervention faite ni recharger ; plusieurs réglages ignorent l'échec d'écriture. _Source : `app.js:10313`, `app-2.md §4`_ — Le dépôt dans « Non planifiés » passe par `planDeplanifier` (mêmes contrôles) ; tout réglage passe par `appliquerPlan`, qui lève si la base n'écrit rien (0 ligne) et recharge. Preuve : `planning.essai.tsx` (« une carte faite ne se retire pas… »).
- [-] **PLN-51** `generateRapportIA` appelle `api.anthropic.com` **depuis le navigateur sans clé** : échoue par construction. _Source : `app.js:11678`_ — Non reprise : appel direct au fournisseur depuis le navigateur, sans clé (D-PLN-11). À refaire, s'il le faut, par une Edge Function (clé côté serveur).
- [x] **PLN-52** Recherche des rapports sans le filtre sous-traitant : un sous-traitant voit les rapports internes. _Source : `app.js:11422-11436`_ — Proposition 20260926052000 : `rapport_visible` — le sous-traitant ne lit que ses rapports ; l'émetteur est posé par la base. Preuve : `tests/rls/interventions.essai.ts` (« [proposition] … ne voit que ses rapports »).
- [x] **PLN-53** Jours fériés : ni Vendredi saint ni 26/12 (Alsace-Moselle) ; liste non triée. _Source : `app-2.md §4`_ — ✔ Liste triée ; Vendredi saint et 26/12 activés par `societes.feries_alsace_moselle` (proposition 20260926105000), case Réglages › Organisation › Jours fériés ; calendrier et impression la lisent (D-TRV-07) — `planning.essai.tsx` (« fériés d'Alsace-Moselle »), `reglages/components/feries.essai.tsx`, `tests/rls/transversal.essai.ts`
- [x] **PLN-54** `saveTechnicienIntervention` ne remet pas `dateInterventionTerminee` à vide quand on décoche. _Source : `app.js:10114`_ — Plus de colonne tenue à la main : « Terminée le » se lit sur les tâches (`termineeLe`, dernière réalisation quand toutes sont faites) — décocher (refus du conducteur) l'efface de fait (D-PLN-14). Preuve : `domain/planning.essai.ts`.

## 12. rh — salariés, équipes, documents, visites, congés

- [x] **RH-01** Salariés : recherche, filtre métier, badges (à vérifier, visite, absent, dossier incomplet, sans compte), coût horaire et salaire affichés si droit. _Source : `app.js:15400`_ — Preuve : `ListeSalaries` (recherche multi-mots, filtre métier référentiel d'abord, badges à vérifier / visite / absent / dossier incomplet / sans compte, coûts si `rh/modifier`) — `src/modules/rh/domain/rh.essai.ts`, `src/modules/rh/components/rh.essai.tsx`
- [x] **RH-02** Équipes = lignes `techniciens` ; membres = salariés actifs dont `technicien_id` pointe dessus ; « Aucun membre. Cette équipe ne peut rien déclarer. » _Source : `app.js:15443, 15496`_ — Preuve : `OngletEquipes` (membres = salariés actifs par `technicien_id`, rattacher/retirer, « sans compte », « Aucun membre… ») — `src/modules/rh/domain/rh.essai.ts`, `src/modules/rh/components/rh.essai.tsx`, `tests/rls/rh.essai.ts`
- [x] **RH-03** Registre unique du personnel (art. L.1221-13), tri par entrée, impression paysage. _Source : `app.js:15505, 15538`_ — Preuve : `/rh/registre` (`registreDuPersonnel`, tri par entrée) ; impression = feuille d'`imprimerRegistrePersonnel` portée (`rh/domain/impression.ts`, D-PDF-07) — `src/modules/rh/domain/rh.essai.ts`, parité `tests/parite/impression-zones.essai.ts`
- [x] **RH-04** Documents RH : matrice de conformité, filtres, dossier, dépôt dans `salarie_documents` + bucket `terrain` (`<societe>/salaries/<id>/`). _Source : `app.js:15739-15916`_ — Preuve : `OngletDocuments` + `SectionDossier` (matrice, filtres, dépôt `salarie_documents` + `terrain/<societe>/salaries/<id>/`) — `src/modules/rh/domain/rh.essai.ts`, `tests/parite/rh.essai.ts`, `tests/rls/rh.essai.ts`
- [x] **RH-05** Fiche salarié (identité, poste référentiel ou libre, équipe, « Conducteur de travaux », compte, contrat, coûts, dates d'entrée/sortie, carte BTP, habilitations, dossier, suivi médical, congés). _Source : `app.js:16201, 16639`_ — Preuve : `/rh/salaries/:id` (identité, poste référentiel ou « Autre… », équipe, conducteur, compte par invitation `comptes/api`, contrat, coûts, dates, carte BTP, habilitations, dossier, suivi médical, congés) — `src/modules/rh/components/rh.essai.tsx`
- [x] **RH-06** Cocher « Conducteur de travaux » crée/active une fiche `conducteurs` (jamais supprimée, `actif:false` au décochage) et propose le rôle `conducteur`. _Source : `app.js:16145, 16183`_ — Preuve : `planConducteur` (création/réactivation, `actif:false` au décochage) + `PropositionRole` — `src/modules/rh/domain/rh.essai.ts`, `src/modules/rh/components/rh.essai.tsx`, `tests/rls/rh.essai.ts` ; fiche retirée non cochée (D-RH-09)
- [x] **RH-07** Visites médicales : types, régimes (60/36/48 mois, intermédiaire 24), avis, échéance proposée, dépassement du plafond = avertissement ; la colonne `visite_medicale_prochaine` fait foi. _Source : `rh-visites.js`, `regles-visite-medicale.ts`_ — Preuve : `domain/visites.ts` (port), `FormulaireVisite` (échéance proposée, avertissement plafond), colonne `visite_medicale_prochaine` qui fait foi — `tests/parite/rh.essai.ts`, `src/modules/rh/domain/rh.essai.ts`, `src/modules/rh/components/rh.essai.tsx`
- [x] **RH-08** Congés : solde = initial − Σ CP ; jours ouvrés = hors sam./dim. (fériés comptés) ; fin < début refusée. _Source : `app.js:16009-16073`_ — Preuve : `domain/conges.ts` (solde, jours ouvrés fériés comptés, fin < début refusée) — `tests/parite/rh.essai.ts`, `src/modules/rh/domain/rh.essai.ts` ; en base : proposition `20260926060000`
- [x] **RH-09** Dossier incomplet si une pièce obligatoire manque (contrat, DPAE, pièce d'identité, carte BTP, RIB) **ou** si un document quelconque est expiré. _Source : `regles-documents-rh.ts`_ — Preuve : `conformiteRh` (manquant OU expiré OU suivi médical inconnu/dépassé) — `src/modules/rh/domain/rh.essai.ts`, `tests/parite/rh.essai.ts`
- [x] **RH-10** Seuils : documents 30 j, visites 45 j (réglages) ; carte BTP / habilitations 30 j codés en dur dans la liste. _Source : `app.js:15648`, `app-3.md §4`_ — Preuve : `useSeuilsRh` (documentLegal, visiteMedicale, carteBtp, habilitation des réglages) — `src/modules/rh/domain/rh.essai.ts` ; carte BTP et habilitations sur leurs seuils réglables au lieu de 30 j en dur (D-RH-04)
- [x] **RH-11** Données sensibles masquées par `v_salaries_annuaire` sans `rh/modifier`. _Source : schéma §4.2_ — Preuve : table `salaries` pour `rh/modifier`, sinon `v_salaries_annuaire` ; proposition `20260926060000` masque aussi visites et notes, et ferme `terrain/<soc>/salaries/` hors RH — `tests/rls/rh.essai.ts`
- [x] **RH-20** Défaut : `absences` sans colonne (table `salarie_absences` inutilisée) → perdues, solde faux ; écrit à chaque `onchange`. _Source : `app-3.md §4-4`_ — Corrigé : absences dans `salarie_absences` (relues, justificatif au seau), solde enregistré avec la fiche et non à chaque frappe — `src/modules/rh/components/rh.essai.tsx`, `tests/rls/rh.essai.ts` (D-RH-02)
- [-] **RH-21** Défaut : `nom2`, `nom3`, `type` des équipes perdus (seul `nom` a une colonne). _Source : `app-3.md §4`_ — Écarté : D-RH-06 — comme l'ancien écran corrigé, les membres viennent des salariés (`technicien_id`) ; l'équipe porte nom, métiers, couleur

## 13. vehicules et materiel

- [x] **VEH-01** Véhicules : filtres En service / Vendus / Tous, liste (plaque, type, motorisation, pneus, km, CT « EXPIRÉ » / « DANS n J » à ≤ 30 j), fiche, facture d'achat, télépéage, carte carburant. _Source : `app.js:14849-15130`_ — ✔ `/vehicules` : filtres avec compteurs, recherche après filtre, colonnes de l'ancien écran, étiquette CT au seuil des réglages (D-VEH-04), bloc « Échéances » ; fiche `/vehicules/:id` (identité, documents et facture d'achat au seau `terrain`, télépéage, carte carburant datée — D-VEH-05) — `vehicules/components/vehicules.essai.tsx`, parité `tests/parite/vehicules.essai.ts` (alertes, joursAvant), `tests/rls/vehicules-api.essai.ts`
- [x] **VEH-02** Immatriculation obligatoire, en capitales, unique par société ; libellé = plaque · marque modèle (ou surnom). _Source : `app.js:15065, 15127`_ — ✔ `schemaSaisieVehicule` (obligatoire, capitales), doublon refusé par l'index et dit en clair ; `libelleVehicule` en parité (300 tirages) — `tests/rls/vehicules.essai.ts` (23505), `vehicules-api.essai.ts`
- [x] **VEH-03** Entretien (le km monte si supérieur), prêts avec schéma SVG, retour. _Source : `app.js:15167-15280`_ — ✔ entretiens (ajout avec facture jointe, modification, suppression), compteur qui monte posé en base (parité `addVehiculeEntretien`) ; prêts avec schéma SVG au départ et au retour, marques aussi au clavier (D-VEH-02) — `vehicules-api.essai.ts`, `vehicules.essai.tsx`
- [x] **VEH-04** Vente → facture (voir FAC-96). _Source : `app.js:15331-15374`_ — ✔ « Vendre ce véhicule » : fiche client, taux des réglages, désignation en parité (`confirmVendreVehicule`), brouillon → vendu → émission par la base, une seule fois — D-VEH-06 — `vehicules-api.essai.ts` (« FAC- »)
- [x] **VEH-05** Matériel : liste, fiche (catégorie, état, n° de série, achat), prêts (actif tant que pas de retour réel), rendu. _Source : `app.js:14566-14790`_ — ✔ `/materiel` : recherche, Disponible / En prêt, emprunteur et retour prévu ; fiche, prêt (état au prêt = état de la fiche), rendu, historique ; états et catégories en parité (`etatMaterielOptions`, `referentielCompose`) — `materiel/components/materiel.essai.tsx`, `materiel/domain/materiel.essai.ts`
- [x] **VEH-20** Défaut : `prets`, `entretiens` sans colonne (tables `vehicule_prets`, `vehicule_entretiens`, `materiel_prets` inutilisées) → perdus. _Source : `app-3.md §4-2`_ — ✔ corrigé : prêts et entretiens écrits dans leurs tables ; proposition `20260926070000` (durée du prêt, un seul prêt en cours, filles sous la matrice, le rôle lecture ne supprime plus) — D-VEH-01 — `tests/rls/vehicules.essai.ts` (échoue avant la proposition)
- [x] **VEH-21** Défaut : `prochainCT` → `prochain_c_t` inexistant (colonne réelle `date_controle_technique`) : date de CT jamais conservée. _Source : `app-3.md §4-3`_ — ✔ corrigé : le formulaire écrit `date_controle_technique` — `tests/rls/vehicules.essai.ts` (« date de CT a sa colonne »), `vehicules.essai.ts` (domaine)

## 14. statistiques et tableaux de bord

- [x] **STA-01** Tableau de bord par rôle : sous-traitant, technicien (aucun montant, journée ≤ 8 cartes, à pointer), conducteur (aucun montant, hors délai / SAV / à valider / sans RDV, mesures 90 j), pilotage (admin, secrétaire, lecture). _Source : `app.js:1395-2150`_ — ✔ `modules/statistiques` (`TableauDeBord` par rôle EFFECTIF : `TableauPilotage`, `TableauConducteur`, `TableauTerrain`) ; sous-traitant = tableau du terrain (D-STA-09) ; aucun montant au terrain ni au conducteur, prouvé par `components/tableaux.essai.tsx` (« sans aucun montant ») ; journée ≤ 8 (`JOURNEE_VISIBLE`) et à pointer testés dans `domain/domaine.essai.ts` ; conducteur par `conducteurs.profile_id` (`api/conducteur.ts`, `tests/rls/statistiques.essai.ts`, D-STA-10)
- [x] **STA-02** Pilotage : recherche globale, actions rapides, tuiles CA encaissé / devis en attente / impayés / à facturer, « À traiter », CA HT 6/12 mois vs N-1, activité récente, top 5 clients, période personnalisée. _Source : `app.js:1446-2043`_ — ✔ `TableauPilotage` : recherche globale (devis/factures/rapports, Entrée cycle, listes chargées à la 1re frappe), actions rapides sous « créer », quatre tuiles (encaissé = règlements du mois TTC, D-STA-04), « À traiter », CA HT 6/12 mois/depuis janvier vs N-1 en SVG + tableau (D-STA-03), plage libre, activité récente (6), top 5 clients, résumé du mois ; montants par les RPC `stats_*` (proposition 20260926080000, D-STA-01) ; parité `tests/parite/statistiques.essai.ts` (mois, N-1, temps relatif) ; destinations D-STA-07
- [x] **STA-03** Taux encaissé = max(0, round((1 − impayés/Σ TTC signés) × 100)). RM-70. _Source : `app-1.md §2.4`_ — ✔ `domain/indicateurs.ts#tauxEncaisse`, impayés et TTC émis rendus par `stats_indicateurs` ; parité contre `computeMonthSummary` évalué (300 tirages) ; dénominateur sans brouillons (D-STA-11)
- [x] **STA-04** Indicateurs conducteur : période 90 j, tentatives injoignable 3, taux SAV (< 10 bon), délai tenu (≥ 80 bon), prise en charge, exécution. _Source : `app.js:1829-1901`_ — ✔ `domain/conducteur.ts#statsConducteur` (`CONDUCTEUR`, `REPERES`) ; parité contre `statsConducteur` évalué (200 portefeuilles : toutes les listes et les quatre mesures)
- [x] **STA-05** Statistiques par conducteur et par équipe/mois (BC, SAV, retard, devis acceptés/transformés, CA HT). _Source : `app.js:11988-12212`_ — ✔ `/statistiques` (`PageStatistiques`) : par conducteur (`stats_par_conducteur`), par métier, par client, CA par équipe et par mois (`stats_ca_par_equipe`), période tout/année/mois/plage ; taux de `tauxConducteur` testés ; RLS `tests/rls/statistiques.essai.ts`
- [x] **STA-20** Défaut : « injoignables » jamais déclenché (`parseInt` d'un tableau). _Source : `app.js:1865`_ — ✔ corrigé : `nombreDeTentatives` compte le tableau ; `tests/parite/statistiques.essai.ts` (« STA-20 ») montre l'ancien à 0 et le nouveau à 1
- [x] **STA-21** Défaut : CA = factures **brouillons comprises** ; CA encaissé = statut `payée` daté du mois de la facture, pas du règlement. _Source : `app-1.md §4.2-9`_ — ✔ corrigé en base : CA sans brouillon ni acompte, avoirs en négatif (D-STA-02) ; encaissé = règlements datés du mois hors lettrage (D-STA-04) ; `tests/rls/statistiques.essai.ts` (« [proposition] … ne compte que des factures », « … entré en caisse »)
- [x] **STA-22** Défaut : statistiques groupées par **nom** de conducteur ; travaux supplémentaires lus sur un tableau sans colonne (toujours 0) ; retard compté même pour un bon facturé ; mois calculé par `new Date()` local. _Source : `app-2.md §4`_ — ✔ corrigé (D-STA-05) : groupement par `conducteur_id`, travaux supplémentaires lus dans `tache_travaux_supplementaires`, retard sur bon ouvert seulement (`stats_bon_ouvert`, testé en RLS), dates de Paris (`todayISO`, `at time zone 'Europe/Paris'`)

## 15. reglages

- [x] **PAR-01** Rubriques : Société (Organisation, Identité visuelle, Documents légaux) ; Documents (Devis & factures, Numérotation) ; Référentiels (Listes de choix, Intervenants, RH, Véhicules, Conduite, Notifications) ; Mon compte. _Source : `app.js:12277`_ — ✔ `/reglages/:rubrique` (`reglages/components/PageReglages.tsx`), rubriques filtrées par droit (`domain/rubriques.ts`), repli sur Organisation ; « Mon compte » à `/mon-compte` (D-SOC-06) — `reglages/domain/reglages.essai.ts`, `reglages.essai.tsx`
- [x] **PAR-02** Devis & factures : validité des devis (30), délai de paiement (30 net), TVA par défaut (10), taux proposés (`0, 2.1, 5.5, 10, 20`, valeurs ≥ 0), textes, IBAN, afficher l'IBAN ; relecture après enregistrement. _Source : `app.js:12867`, `integrations/reglages.ts`_ — ✔ Réglages › Devis & factures (validité, délai et mode, TVA par défaut, taux ≥ 0, mode de règlement, textes, site, IBAN), relecture après enregistrement — `societes/domain/societe.essai.ts`, parité `fusionnerReglages` (D-SOC-11)
- [x] **PAR-03** Numérotation : compteurs par type (DEV, FAC, INT, SAV — pas de série BC), aperçu `PREFIXE-AAAA-NNNNNN`, avertissement si le compteur baisse, modifiable avec `reglages/modifier`. _Source : `app.js:12784-12859`_ — ✔ Réglages › Numérotation : DEV, FAC, INT, SAV (pas de BC), aperçu `PREFIXE-AAAA-NNNNNN` (parité `apercuNumero`), baisse à confirmer, `reglages/modifier` — `reglages.essai.tsx`, `tests/rls/reglages.essai.ts` (D-SOC-10)
- [x] **PAR-04** Listes de choix (métiers, catégories/états de matériel, catégories d'achat, unités, pièces courantes) : code posé à la création puis figé, positions, déplacement. _Source : `app.js:17566-17730`_ — ✔ Réglages › Listes de choix (5 domaines) : code posé à la création puis figé, position, ↑/↓ par échange — `reglages/domain/reglages.essai.ts`, parité `normaliserEntree` / `entreesDuDomaine` / `prochainePosition`, `tests/rls/reglages.essai.ts`
- [x] **PAR-05** Métiers (palette de 19 couleurs, position ; suppression refusée si employé ; renommage propagé partout sauf factures émises). _Source : `app.js:17727-17830`, schéma §7_ — ✔ onglet Métiers (`ListeMetiers.tsx`) : palette de 19 couleurs, position ; suppression d'un métier employé refusée et renommage propagé par la base (motif relayé tel quel)
- [x] **PAR-06** Intervenants : conducteurs (compte lié), fournisseurs (`actif` toujours envoyé), sous-traitants (documents à échéance), équipes. _Source : `app.js:17848-18717`_ — Preuve : conducteurs et fournisseurs (Réglages, `reglages.essai.ts`) ; sous-traitants (identité contrôlée, métiers, compte relié, documents à échéance dans `sous_traitant_documents`) et équipes dans l'onglet RH — `src/modules/rh/components/rh.essai.tsx`, `tests/rls/rh.essai.ts`
- [x] **PAR-07** Seuils : véhicule carte 30, CT 30, document légal 30, carte BTP 60, visite 45, habilitation 60, conducteur sans RDV 7. _Source : `integrations/alertes.ts#SEUILS`_ — ✔ Réglages › RH / Véhicules / Conduite : les 7 seuils (défauts 30, 30, 30, 60, 45, 60, 7), entiers ≥ 0 — parité `fusionnerReglages` (seuils)
- [-] **PAR-20** Défaut : `sousTraitant.documents` et `document.notes` sans colonne → perdus. _Source : `app-3.md §4-5`_ — `web/` n'offre aucun champ sans colonne — D-SOC-14
- [x] **PAR-21** Défaut : repli du code de référentiel sans `normaliserEntree` casse les accents (« location_de_mat_riel »). _Source : `app-3.md §5`_ — ✔ corrigé : `codeDepuisLibelle` passe toujours par `normaliserEntree` — `reglages/domain/reglages.essai.ts` (« location_de_materiel »)

## 16. facturation électronique (PDP, Factur-X)

- [x] **EFA-01** Transmettre une facture numérotée à la plateforme (pas B2C, pas étranger, pas pièce historique), irréversible, `confirm`. _Source : `app.js:4735`_ — ✔ `efacture/components/BoutonTransmettre.tsx` (confirmation, masqué pour brouillon / déjà déposée / historique « compta: » / B2C / étranger / sans `factures modifier`), dépôt par `pdp-emit-invoice` inchangée (D-EFA-04) — `transmettre.essai.tsx`, `domain/dossier.essai.ts` (« qui passe par la plateforme »)
- [x] **EFA-02** Charge EN 16931 : types 380 / 381 / 386, codes unité UNECE (défaut C62), catégories TVA (`S` si > 0, sinon `Z`), remise → déductions BG-20 par taux (code 95), BR-CO-10 à 0,01 €, avoir en négatif. RM-08. _Source : `regles-en16931.ts`_ — ✔ `efacture/domain/norme.ts` — `tests/parite/efacture.essai.ts` (charge identique au caractère près hors demi-centimes, ±0,01 sinon — D-EFA-02)
- [x] **EFA-03** Manques pour émettre : BT-1, BT-2, SIREN/SIRET émetteur, BT-27, BT-44, BT-49 (si e-facture), BT-55, ≥ 1 ligne, BR-CO-10. _Source : `regles-en16931.ts#manquesPourEmettre`_ — ✔ `norme.ts#manquesPourEmettre` — `tests/parite/efacture.essai.ts` (« mêmes codes, mêmes libellés », BT-49 seulement si e-facture)
- [x] **EFA-04** XML CII (`factur-x.xml`, profil EN 16931) intégré au PDF (PDF/A, sRGB) au téléchargement d'une facture numérotée. _Source : `integrations/facturx.ts`_ — ✔ `efacture/domain/cii.ts` (octet pour octet — parité), `pdf/facturx.ts` (port littéral pdf-lib de `integrations/facturx.ts` : `AFRelationship Data`, XMP, sRGB — D-PDF-05), branché sur le PDF d'une facture numérotée — `domain/cii.essai.ts` (structure XSD + BR-CO), `pdf/facturx.essai.ts`, `hooks/enrichir.essai.ts`
- [x] **EFA-05** Mentions légales : pénalités, indemnité 40 €, franchise 293 B, autoliquidation 283-2 nonies, TVA sur encaissements, décennale. RM-07. _Source : `regles-efacture.ts#mentionsLegales`_ — ✔ `facturation/domain/mentions.ts` — `tests/parite/facturation.essai.ts` (« parité des mentions légales ») ; notes PMT/PMD/AAB du CII par `norme.ts`
- [-] **EFA-06** Edge Functions SUPER PDP : `pdp-oauth-start/callback`, `pdp-disconnect`, `pdp-check-eligibility`, `pdp-emit-invoice`, `pdp-sync-events`, `pdp-post-lifecycle`, `pdp-receive`, `pdp-invoice-file`, `pdp-ereporting`, `pdp-webhook`. _Source : schéma §8.4_ — seule `pdp-emit-invoice` était appelée par l'ancien écran ; appelée telle quelle, rien créé ni déployé — D-EFA-04
- [-] **EFA-07** Tables `pdp_connexions`, `pdp_connexion_secrets` (service seul), `pdp_oauth_etats`, `facture_cycle_vie`, `factures_entrantes(_lignes)`, `ereporting_depots`, `integration_journal`. _Source : schéma §2.7_ — tables déjà en production (types générés) et en base locale ; web/ n'en lit que `factures.pdp_identifiant` — D-EFA-04
- [-] **EFA-20** Défaut : les fonctions PDP ne vérifient **pas le rôle** (un compte `lecture` peut déposer, un technicien connecter/déconnecter). _Source : schéma §8.4_ — correction hors web/ (fonction) ; l'écran exige `factures / modifier` (`transmettre.essai.tsx`) — D-EFA-05
- [-] **EFA-21** Défaut : `pdp-webhook` compare le secret avec `!==` ; `verify_jwt=false` non déclaré dans `config.toml`. _Source : schéma §8.4_ — correction hors web/ (fonction, config.toml racine) — D-EFA-05
- [x] **EFA-22** Défaut : BT-113 calculé avec le solde recalculé côté client (FAC-92). _Source : ts §4.2_ — ✔ corrigé : BT-113 = `v_facture_solde.paye` + acomptes, lus en base (`efacture/api/emission.ts`) — `domain/dossier.essai.ts` (« BT-113 »), `tests/rls/import-export.essai.ts` (« déjà-réglé de la base »)

## 17. import / export

- [x] **IMP-01** Import d'articles : `;` uniquement, encodage constaté (BOM UTF-8/16, UTF-8 strict, sinon Windows-1252), colonnes par nom, requises `CodeArticle`, `Libelle1` ; nombre de champs = en-tête (sinon « les colonnes seraient décalées »). _Source : `regles-import-articles.ts`_ — Preuve : `tests/parite/import-articles.essai.ts`.
- [x] **IMP-02** Mapping articles : désignation = Libelle1 sinon 80 car. de BlocNote ; PV HT à virgule ; `BIEN` → bien ; `Actif = "1"` ; `GereEnStock = "1"` ; famille = FamilleArt1. _Source : idem_ — Preuve : `tests/parite/import-articles.essai.ts`.
- [x] **IMP-03** TVA `INTER`→10, `NORMA`→20, `EXO`/`0`→0, inconnu→20 signalé (RM-06). _Source : idem_ — Preuve : `tests/parite/import-articles.essai.ts` (cas nommés).
- [x] **IMP-04** Unités UNI→u, M→m, M2→m², M3→m³, HR→h, PC→pièce, MM→mm, JOUR→jour ; inconnue (`ML`) → vide + signalement. _Source : idem_ — Preuve : `tests/parite/import-articles.essai.ts` (« unités du fichier »).
- [x] **IMP-05** Code répété dans le fichier : premier gardé ; créés / mis à jour comptés. _Source : idem_ — Preuve : `tests/parite/import-articles.essai.ts`, `components/import.essai.tsx`, `tests/rls/articles.essai.ts`.
- [x] **IMP-06** Rapport de rejets CSV. _Source : `regles-csv.ts#rapportRejetsCsv`_ — Preuve : `tests/parite/import-articles.essai.ts` (« le rapport téléchargé »), `components/import.essai.tsx`.
- [x] **IMP-10** Import de clients : CSV RFC 4180 `;`, colonnes type Vertuoza (« Nom de l'entreprise » requise), colonnes versées en notes, colonnes écartées (BIC, IBAN, TVA ambiguë…). _Source : `regles-import-clients.ts`_ — ✔ `import-export/domain/clients.ts`, `csv.ts` — `tests/parite/import-clients.essai.ts` (500 fichiers, encodages, champs cités multilignes)
- [x] **IMP-11** Pays FRANCE→FR, BELGIQUE/BELGIUM→BE, SUISSE→CH, LUXEMBOURG→LU, ALLEMAGNE→DE, ESPAGNE→ES, ITALIE→IT, défaut FR. _Source : idem_ — ✔ `tests/parite/import-clients.essai.ts` (« pays (IMP-11) »)
- [x] **IMP-12** Conditions : « réception/comptant/immédiat » → 0 net ; sinon premier nombre ; « fin de mois »/« fdm » → fin de mois (« 30 jours fin de mois » → 30 fdm ; « 45j » → 45 net). _Source : idem_ — ✔ `tests/parite/import-clients.essai.ts` (« conditions (IMP-12) », parité `delaiDesConditions`)
- [x] **IMP-13** Rapprochement : SIRET unique → mise à jour ; sinon nom normalisé ; plusieurs → ambigu ; sinon création ; aperçu (à créer, à mettre à jour, rejetés, ambigus, signalés, corrections d'annuaire, types déduits). _Source : idem, `app.js:16811-16995`_ — ✔ `domain/apercu-clients.ts`, `components/PageImportClients.tsx` (à créer, à mettre à jour, rejetés, ambigus, signalés, types déduits) ; sans annuaire donc sans corrections d'annuaire (D-EFA-06), mise à jour sans effacement (D-EFA-07) — `components/import.essai.tsx`, parité `rapprocher`
- [x] **IMP-14** Écriture : créations par lots de 200 à **clés uniformisées** (piège `columns=`), mises à jour une par une ; rapport CSV. _Source : `queries/clients.ts`_ — ✔ `import-export/api/clients.ts` (lots de 200, clés uniformisées, mises à jour une par une, rapport CSV) — `tests/rls/import-export.essai.ts` (205 créations, ligne sans téléphone, refus du rôle lecture)
- [x] **IMP-20** Import de factures historiques : en-têtes (requis) + lignes (facultatif, sinon ligne unique « Facturation (historique) ») ; séparateur `;` ou `,` ; dates ISO seulement ; alias de colonnes. _Source : `regles-import-factures.ts`_ — ✔ `import-export/domain/factures.ts` — `tests/parite/import-factures.essai.ts` (400 couples, deux dialectes, avec ou sans lignes)
- [x] **IMP-21** Contrôles à 0,011 : signe du HT vs type ; HT×taux − TVA ; HT+TVA − TTC ; Σ lignes = HT ; numéro unique ; un rejet interdit toute écriture. _Source : idem_ — ✔ idem (verdict `incoherent` identique) ; écriture refusée à l'écran (`import.essai.tsx` « bloque TOUTE écriture »)
- [x] **IMP-22** Taux 0 % : catégorie obligatoire E / AE / Z / O ; montants stockés en valeur absolue ; `legacy_id = "compta:" + numero` ; statut `payée` ; écriture brouillon → lignes → numéro + statut. _Source : idem, `queries/factures-import.ts`_ — ✔ `domain/apercu-factures.ts`, `api/factures.ts` (brouillon → lignes → numéro + statut « payée », legacy « compta: », valeurs absolues) — `tests/rls/import-export.essai.ts` (pièce écrite, indestructible), `import.essai.tsx` (catégorie 0 %)
- [x] **IMP-23** Bouton visible si `factures` créer **et** modifier. _Source : `app.js:5195`_ — ✔ `import-export/components/Recapitulatif.tsx#BoutonImport` sur la liste des factures — `import.essai.tsx` (admin, secrétaire oui ; conducteur, lecture non)
- [x] **IMP-30** Import DPGF CSV/Excel : feuille avec le plus de cellules numériques, lignes à ignorer devinées, rôles de colonnes devinés par en-tête, ligne sans quantité ni prix = chapitre. _Source : `app.js:13848, 13938`_ — ✔ même import que CHA-08 (`chantiers/domain/import-dpgf.ts`) — `tests/parite/chantiers.essai.ts`
- [-] **IMP-31** Défaut DPGF : `"1.234"` lu 1,234 ; `;` retenu seulement si la 1re ligne n'a aucune virgule ; pas de `""` ; colonne prix jamais devinée par le contenu ; import remplace tout l'avancement sans confirmation. _Source : `app-2.md §4`_ — reproduit à l'identique (parité CHA-08), remplacement tempéré par D-CHA-07 — D-EFA-09
- [x] **IMP-40** Export JSON « sauvegarde » de la société (`terrain-sauvegarde-AAAA-MM-JJ.json`, `version: 2`) ; import JSON de restauration **sans garde de rôle** dans l'écran. _Source : `app.js:399-430`_ — ✔ export `terrain-sauvegarde-AAAA-MM-JJ.json`, `version: 2` (`/import-export`, `api/sauvegarde.ts`) — `tests/rls/import-export.essai.ts` (« sauvegarde »), `import.essai.tsx` ; restauration écartée — D-EFA-08

---

## 18. Transversal (formats, navigation, robustesse)

- [x] **TRV-01** Montants : `Intl.NumberFormat('fr-FR', EUR)` — milliers en espace fine insécable U+202F, virgule, « € » précédé de U+00A0 ; `money(undefined)` = « 0,00 € ». RM-80. _Source : `app.js:625`_ — ✔ `lib/money.ts#formatEuros` garde U+202F et U+00A0 (D-CLI-12) ; le PDF les convertit (`texteWinAnsi`) — parité `tests/parite/formats.essai.ts` (3 000 montants contre `money()` extrait d'`app.js`, `money(undefined)`)
- [x] **TRV-02** Dates : `JJ/MM/AAAA`, vide → « — » ; jamais `toISOString()` pour « aujourd'hui » (`todayISO`/`dateISO` en composantes locales). RM-81. _Source : `app.js:781`, `CLAUDE.md`_ — ✔ `lib/dates.essai.ts` (« date de Paris, pas celle d'UTC », `formatDateFr`)
- [x] **TRV-03** Taux : « 5,5 % » (`formaterTaux`). _Source : `regles-totaux.ts`_ — ✔ `lib/money.ts#formatTaux`, `tests/e2e/parcours.e2e.ts` (« TVA 10 % sur… »)
- [x] **TRV-04** `""` de l'écran → `null` pour énumérations, dates, numériques ; champ sans colonne filtré avant envoi ; valeur hors énumération → `null`. _Source : `html-adapter.ts#normaliser`, `CLAUDE.md`_ — ✔ `videEnNull` + Zod : `clients/domain/client.essai.ts` (« champs vides en null »), `devis.essai.tsx` (pas de `""`), `ocr/domain/contrat.essai.ts` (statut inventé écarté)
- [x] **TRV-05** Mode discret : tous les montants remplacés par `••• €`. _Source : `app.js:626, 773`_ — ✔ `lib/modeDiscret.ts` : « ••• € » sur tous les montants de l'écran (`formatEurosEcran`), bascule dans le menu, pièces imprimées intactes (D-CLI-04) ; la bascule redessine les montants SANS remonter l'écran, la saisie reste (D-R4-01) — parité `tests/parite/formats.essai.ts` (`moneyDisplay`), `app/Layout.essai.tsx`, `app/mode-discret-saisie.essai.tsx`, garde-fou `tests/garde-fous.essai.ts`
- [x] **TRV-06** Recherche insensible aux accents, multi-mots ; montants cherchables (formatés et `toFixed(2)`) ; Entrée fait défiler les résultats ; redessin différé 300 ms. _Source : `app.js:2151-2330, 4383-4460, 5585`_ — ✔ accents, multi-mots ; montants cherchables (« 1 234,50 € » et « 1234.50 ») sur devis, factures, bons ; Entrée fait défiler et met en évidence ; filtrage différé de 300 ms (`lib/useRecherche.ts`, D-CLI-08) — `facturation/components/recherche-croisee.essai.tsx`, `lib/useFiltresAdresse.essai.ts`
- [x] **TRV-07** Croisement facture ↔ BC par référence client normalisée, avec « d'où vient la correspondance ». _Source : `app.js:2179-2279`, `regles-liens-facture-bc.ts`_ — ✔ `facturation/domain/croisement.ts` (clé puis référence client normalisée) dans la recherche des factures et des bons, « 🔎 d'où vient la correspondance » (`OrigineRecherche`) — parité `tests/parite/croisement.essai.ts` (`regles-liens-facture-bc` et `origineDeLaCorrespondance` importés), `recherche-croisee.essai.tsx`
- [x] **TRV-08** Historique navigateur : « Précédent » revient à l'onglet / formulaire. _Source : `app.js:1086-1110`_ — ✔ une URL par écran (`app/routes.tsx`) ; `tests/e2e/facturation.e2e.ts` (deux « Précédent » ramènent à la fiche chantier)
- [x] **TRV-09** Notifications : véhicules, habilitations, documents légaux, dossiers RH, BC en retard, documents sous-traitants (≤ 30 j), rappels ; « fait » mémorisé par société. _Source : `app.js:627-760`_ — ✔ cloche de l'en-tête (`modules/notifications`) : véhicules, carte BTP / visite / habilitations, documents légaux, dossier RH, BC en retard, documents de sous-traitant, rappels ; « fait » par société (proposition 20260926120000, D-CLI-05) ; familles selon le rôle, trois écarts assumés (D-CLI-06) — parité `tests/parite/notifications.essai.ts` (`computeNotifications` évalué), `notifications/components/notifications.essai.tsx`, `tests/rls/notifications.essai.ts`
- [x] **TRV-10** Chargement tolérant : une table en échec garde ses données précédentes et s'annonce par un bandeau ; une lecture **tronquée** (plafond de lignes) est un refus, jamais une liste partielle. _Source : `app.js:523, 557`, `html-adapter.ts#chargerCollection`_ — ✔ lecture en échec : `<Erreur>` + données précédentes (TanStack), la cloche nomme la famille illisible ; lecture tronquée : `lib/lecture.ts#lireTout` (pages jusqu'au compte exact, sinon `ListeTronquee`) pour clients, chantiers, devis, factures, rapprochables (D-CLI-07) — `lib/lecture.essai.ts`, `tests/rls/clients-api.essai.ts`
- [x] **TRV-11** Menu épinglé mémorisé (préférence locale). _Source : `app.js:1267`_ — ✔ « Garder le menu ouvert », clé `erp.menu.epingle` de l'ancien écran ; non épinglé, le menu se replie sur le planning (D-CLI-11) — `app/Layout.essai.tsx`
- [x] **TRV-12** Aucune URL/clé en dur ; rien de secret en `VITE_`. _Source : `app.js:224`, `CLAUDE.md`_ — ✔ `tests/garde-fous.essai.ts` (« aucun secret ni URL de production »), `lib/env.ts`
- [x] **TRV-13** Échappement des attributs : `&` encodé en premier (injection prouvée) ; retours à la ligne non échappés dans l'ancien `jsAttr`. _Source : `app.js:835`_ — ✔ rendu React échappé, aucun `dangerouslySetInnerHTML` dans `web/src`
- [x] **TRV-14** Aucun `catch` muet ; aucun nombre magique (8 Mo, 30 j, 3/5 photos, 900 px, JPEG 0,6/0,85…). _Source : `CLAUDE.md`, `app-2.md §4`_ — ✔ `tests/garde-fous-syntaxe.essai.ts` (arbre syntaxique TypeScript) : aucun `catch` vide ni `.catch` qui avale l'erreur (src/ et tests/), aucun littéral numérique sans nom dans src/ — 130 littéraux nommés (`lib/durees`, `lib/dates`…), fichiers de format listés (D-AUTH-10). Accessibilité : capteur axe-core (WCAG 2.1 A/AA) — `src/test/accessibilite.essai.tsx` (D-TRV-12)
- [x] **TRV-15** Bibliothèques PDF/XLSX/Word embarquées (pas de CDN : faille xlsx 0.18.5, pas d'`integrity`). _Source : `app-3.md §0`_ — ✔ aucune bibliothèque par CDN (`index.html`, `package.json`) ; html2pdf.js 0.14.0 et pdf-lib 1.17.1 en dépendances, html2pdf chargé au premier PDF (D-PDF-01)

---

## 19. Tables, vues, RPC et Edge Functions — récapitulatif

« Lu / écrit » = colonnes réellement utilisées par l'ancienne app (pont `html-adapter`,
`queries/*`, RPC). ✗ = existe mais **inutilisé** par l'app.

| Objet | Usage | Colonnes réellement lues / écrites | Module cible |
|---|---|---|---|
| `societes` | Tenant, identité légale | L/E : colonnes `CHAMPS_SOCIETE` (adresse, CP, ville, tél., e-mail, siret, siren, nom, tva_intracom, raison_sociale_legale, forme_juridique, code_naf, capital_social, rcs_*, pays_code, regime_tva, ereporting_regime, tva_sur_encaissements, autoliquidation_batiment, indemnite_recouvrement, mention_penalites_retard, assurance_decennale_*, adresse_electronique_*, iban, bic) ; L : code, uuid | societes |
| `societe_settings` | Réglages jsonb | L/E : `infos_entreprise` (dont `reglages`), `notifs_traitees` | societes, reglages |
| `profiles` | Compte | L : id, nom, email ; E : nom | auth-roles |
| `membres_societe` | Appartenance + rôle | L : profile_id, societe_id, role, actif ; E : role | auth-roles |
| `invitations` | Invitations | L : tout ; E : statut (`annulee`) ; création par Edge | auth-roles |
| `role_permissions` | Matrice | L : role, module, action (count exact) | auth-roles |
| `clients` | Clients | L/E : toutes les colonnes du formulaire + `eligibilite_*` reportés ; L (rapprochement) : id, nom, siret, siren, cadre, délai | clients |
| `interlocuteurs` | Contacts | L/E : client_id, nom, fonction, telephone, email | clients |
| `devis` / `devis_lignes` | Devis | L/E : en-tête complet ; lignes type, designation, commentaire, quantite, prix_unitaire, tva, unite, article_reference, metier, position, `montant_ht` (écrit) | devis |
| `v_devis_totaux` | Totaux devis | L : ht, tva, ttc (queries seulement ; l'écran recalcule) | devis |
| `factures` / `facture_lignes` | Factures, avoirs | L/E : en-tête (voir FAC-70), `verrouillee`, `emetteur_*`, `client_*` ; ✗ `total_*`, `net_a_payer`, `ventilation_tva` ; lignes comme devis (`unite_code`, `tva_categorie` non écrits sauf import) | facturation |
| `v_facture_totaux` | Totaux facture | L : ht, tva, ttc (imputation, e-facture, Edge PDP) | facturation |
| `v_facture_solde` | Solde | L : queries (l'écran recalcule le statut) | facturation |
| `reglements` | Encaissements | L/E : facture_id, date, montant, mode, reference | facturation |
| `compteurs` | Numérotation | L/E : type, annee, valeur, prefixe | reglages |
| `bons_commande` | BC, SAV | E : voir BC-60 (+ planning : dates, heures, durée, schedule_par_metier, tentatives_contact, rappel_date, montant_sous_traitant, pièce) ; L par la vue | commandes |
| `v_bons_commande_terrain` | Lecture BC (prix masqués) | L : `select *` (sans `telephone_locataire`) | commandes |
| `bon_commande_lignes` / `v_bon_commande_lignes_terrain` | Lignes BC | E table, L vue | commandes |
| `bon_commande_photos` | Photos SAV | L/E : chemin, legende, position | commandes |
| `planning_taches` | Tâches | L : tout (dérivés du BC) ; E : via RPC + création/suppression | commandes, planning |
| `tache_travaux_supplementaires` / `v_travaux_supplementaires_terrain` | Travaux sup. | L/E : bon_commande_id, planning_tache_id, libelle, quantite, unite, prix_vente_ht, tva, origine, statut | commandes |
| `workflow_journal` | Journal du circuit | E par RPC ; ✗ en lecture à l'écran | commandes |
| `chantiers` | Chantiers | L/E : nom, client, conducteur(_id), adresse, type, dates, infos_diverses, PPSPS | chantiers |
| `chantier_achats` | Achats | L/E : categorie, designation, montant, date_achat, salarie_id, heures | chantiers |
| `chantier_dpgf_lignes`, `chantier_todos`, `chantier_documents`, `chantier_inspections`, `chantier_comptes_rendus`, `chantier_devis_complementaires` | Filles chantier | ✗ par l'écran (lues par `queries/chantiers.ts` seulement) — cause de CHA-50 | chantiers |
| `chantier_affectations` | Visibilité terrain | ✗ écran ; lue par RLS | chantiers |
| `chantier_avancement_factures` | Historique des situations | ✗ | facturation |
| `v_chantier_avancement` | Avancement | ✗ écran (recalculé) | chantiers |
| `articles` | Catalogue | L/E : toutes | articles |
| `interventions` (+ `_photos`, `_controles`) | Rapports | L/E : en-tête, constatations, preconisations, signature_chemin ; photos | planning |
| `conducteurs` | Conducteurs | L/E : nom, telephone, email, profile_id, salarie_id, actif | reglages |
| `techniciens` | Équipes | L/E : nom, metiers, metier, couleur | rh, planning |
| `metiers`, `referentiels` | Listes | L/E : libelle, couleur, position ; domaine, libelle, code, couleur, icone, position | reglages |
| `fournisseurs` | Annuaire | L/E : nom, specialite, contact_nom, telephone, email, adresse, CP, ville, siret, notes, actif | reglages |
| `sous_traitants` / `sous_traitant_documents` | Sous-traitants | L/E : nom, contacts, siret, siren, tva, adresse, metiers ; documents ✗ | reglages |
| `documents_legaux` | Documents société | L/E : nom, date_validite (`notes` perdu) | societes |
| `salaries` / `v_salaries_annuaire` | Salariés | E table, L vue | rh |
| `salarie_documents`, `salarie_visites_medicales` | Dossier RH | L/E | rh |
| `salarie_absences`, `_habilitations`, `_contrats`, `_formations`, `_contacts_urgence`, `_rdv` | RH | ✗ par l'écran | rh |
| `vehicules` / `materiels` | Parc | L/E : en-tête (sans CT) ; filles ✗ | vehicules |
| `pdp_*`, `factures_entrantes`, `facture_cycle_vie`, `ereporting_depots`, `integration_journal` | PDP | Edge Functions | e-facture |
| `fournisseurs_controle` (+ lignes) | Contrôle de prix | ✗ (dormante) | — |
| `kv_store`, `zz_obsolete_*` | Vestiges fermés | ✗ (7 factures à rapatrier) | import |
| RPC `prochain_numero(p_societe, p_type, p_annee)` | Numéro devis / SAV / intervention | exige `peut_ecrire` ; refuse facture/avoir/acompte | devis, commandes |
| RPC `mon_role`, `a_permission` | Session, droits | — | auth-roles |
| RPC `tache_*` (3), `bc_*` (6) | Circuit BC | voir BC-63 | commandes |
| Déclencheurs `facture_attribuer_numero`, `bc_attribuer_numero_interne` | Numérotation à l'émission / création | — | facturation, commandes |
| Edge `extraire-bc` | OCR | `{fichierBase64, mimeType}` | ocr |
| Edge `inviter-salarie` | Invitations | `{salarie_id, email, role}` | auth-roles |
| Edge `pdp-*` (11) | Facture électronique | voir EFA-06 | e-facture |
| Edge `prochain-numero` | **Cassée** (table `counters` absente, service_role sans contrôle, non atomique, préfixe RAP) : à ne pas utiliser | — | — |
| Storage bucket `terrain` | Pièces jointes BC, RH | chemin `<societe>/<domaine>/<id>/<ts>_<nom>`, URL signées 1 h | transversal |
| API `api-adresse.data.gouv.fr`, `recherche-entreprises.api.gouv.fr`, `geo.api.gouv.fr` | Annuaires publics | appel navigateur sans clé | clients |

---

## Décompte

Relevé le 25/09/2026, après la vague 3 (droits, démarrage et session ;
clients, annuaire, notifications, mode discret et transversal), d'après le code
de `web/src` et les tests. Parité = ([x] + 0,5 × [~]) / (total − [-]).

| Section | Préfixe | [x] | [~] | [-] | [ ] | Total | Parité |
|---|---|---|---|---|---|---|---|
| 1. auth-roles | AUTH | 51 | 0 | 3 | 0 | 54 | 100 % |
| 2. societes | SOC | 21 | 0 | 2 | 0 | 23 | 100 % |
| 3. clients | CLI | 23 | 0 | 0 | 0 | 23 | 100 % |
| 4. chantiers | CHA | 33 | 0 | 0 | 0 | 33 | 100 % |
| 5. devis | DEV | 37 | 0 | 1 | 0 | 38 | 100 % |
| 6. articles | ART | 12 | 0 | 1 | 0 | 13 | 100 % |
| 7. commandes | BC | 68 | 0 | 5 | 0 | 73 | 100 % |
| 8. facturation | FAC | 65 | 0 | 7 | 0 | 72 | 100 % |
| 9. ocr | OCR | 15 | 0 | 1 | 0 | 16 | 100 % |
| 10. espace-client | ESP | 7 | 0 | 1 | 0 | 8 | 100 % |
| 11. planning, interventions, rapports | PLN | 23 | 0 | 1 | 0 | 24 | 100 % |
| 12. rh | RH | 12 | 0 | 1 | 0 | 13 | 100 % |
| 13. vehicules et materiel | VEH | 7 | 0 | 0 | 0 | 7 | 100 % |
| 14. statistiques et tableaux de bord | STA | 8 | 0 | 0 | 0 | 8 | 100 % |
| 15. reglages | PAR | 8 | 0 | 1 | 0 | 9 | 100 % |
| 16. facturation électronique | EFA | 6 | 0 | 4 | 0 | 10 | 100 % |
| 17. import / export | IMP | 17 | 0 | 1 | 0 | 18 | 100 % |
| 18. Transversal | TRV | 15 | 0 | 0 | 0 | 15 | 100 % |
| **Total** | | **428** | **0** | **29** | **0** | **457** | **100 %** |
