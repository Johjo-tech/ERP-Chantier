import { TableauDeBord } from "@/modules/statistiques/components/TableauDeBord";

/** L'accueil est le tableau de bord du rôle effectif (module statistiques, STA-01). */
export function Accueil() {
  return <TableauDeBord />;
}
