/**
 * Point d'entrée des pages.
 *
 * La racine Vite est `src/pages`, donc un `src="../main.ts"` dans le HTML sort
 * de la racine et n'est pas servi. Ce fichier, lui, est dans la racine et peut
 * importer au-dessus normalement.
 */
import "../main";
