# Contrôles automatiques : un échec ici doit bloquer la fusion.
#
# Les variables Supabase sont factices : le build a seulement besoin qu'elles
# existent (le client lève une erreur à l'import si elles manquent). Aucun
# secret n'est nécessaire, et TEST_USER_* est volontairement absent — les
# suites d'intégration se désactivent au lieu d'écrire dans une vraie base.

name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  verifier:
    runs-on: ubuntu-latest
    env:
      VITE_SUPABASE_URL: https://exemple.supabase.co
      VITE_SUPABASE_ANON_KEY: cle-factice-ci

    steps:
      # Épinglé au SHA : un tag de version est mobile, un commit ne l'est pas.
      # Dependabot proposera les montées de version, visibles en revue.
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      # Node 22 minimum : @supabase/realtime-js exige un WebSocket natif
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm

      # `npm ci` impose le lockfile : le build de la CI est celui du poste
      - run: npm ci

      - name: Types
        run: npm run type-check

      - name: Tests
        run: npm run test:run

      - name: Build
        run: npm run build

  # Analyse statique de la couche TypeScript — et d'elle seule.
  #
  # `src/pages/index.html` échappe à Semgrep pour DEUX raisons cumulées :
  #   1. Semgrep n'extrait pas le JavaScript inline d'un fichier HTML : le code
  #      du monolithe et ses écritures `innerHTML` lui restent invisibles.
  #   2. Le fichier dépasse `--max-target-bytes` (1 000 000 octets par défaut) :
  #      mesuré à 1 030 853 octets, il est écarté AVANT toute règle. `scanned` revient
  #      vide, sans un mot dans le rapport.
  #
  # Un scan vert ne dit donc rien de cet écran. Ce travail-là revient à la relecture
  # des diffs.
  #
  # Ne pas relever le seuil pour « couvrir » le fichier : mesuré, le scan porte alors
  # bien index.html et ne remonte rien — une couverture de façade.
  securite:
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Semgrep
        run: >
          semgrep scan
          --config p/typescript
          --config p/javascript
          --config p/secrets
          --config p/owasp-top-ten
          --exclude=node_modules --exclude=dist
          --exclude=src/api/database.types.ts --exclude=src/api/columns.ts
          --error
          src/ scripts/
